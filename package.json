{
  "name": "archeo",
  "version": "0.1.0",
  "description": "Autonomously explores a running web application and produces a machine-readable JSON build spec — vendor escape, not competitor cloning.",
  "type": "module",
  "license": "Apache-2.0",
  "bin": {
    "archeo": "dist/index.js"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "node src/cli/index.ts",
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "test": "node --test 'test/**/*.test.ts'",
    "test:types": "node --test test/types/typecheck.guard.ts",
    "postinstall": "playwright install chromium"
  },
  "dependencies": {
    "cac": "^7.0.0",
    "playwright": "^1.61.1"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "tsup": "^8.5.1",
    "typescript": "^6.0.3"
  }
}
