---
phase: 08-differential-validation
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
autonomous: true
requirements: [VALID-01]
files_modified:
  - .planning/phases/08-differential-validation/08-02-live-verification/
  - .planning/phases/08-differential-validation/08-02-DOGFOOD-VERIFICATION.md
  - .planning/phases/08-differential-validation/08-02-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/PROJECT.md

must_haves:
  truths:
    - "The Phase 3 03-04 ORIGINAL target app and its spec-only REBUILD are stood up on SEPARATE localhost ports through their real code (original via a tiny createServer().listen(port) launcher — it only exports createServer(); rebuild via `PORT=<n> node server.js` using its CommonJS package.json shim); both boot on node:http, zero deps — CONFIRMED standable during context-gathering"
    - "`archeo compare http://127.0.0.1:<orig> http://127.0.0.1:<rebuild>` is run through the REAL, UNMODIFIED CLI (`node src/cli/index.ts compare …`), scripted provider, real headed Chromium, floor ON for BOTH targets — no mock routes, no src/ edits by the harness"
    - "VALID-01 MATCH: on endpoints the rebuild faithfully implemented, the divergence report is empty/near-empty (zero false positives on the identical surface)"
    - "VALID-01 FLAG: the report FLAGS the known real differences the 03-04 buildability report documented — the builder-ADDED `GET /api/settings` (CONFIRMED live: 404 on original vs 200 on rebuild), the GraphQL/JSON-RPC write-vs-read handling difference, and the convention-guessed held-mutation response shapes — appearing as newEndpoints (added) / changedShapes / heldStatusChanges under the original-vs-rebuild framing"
    - "SELF-COMPARE CONTROL: comparing the ORIGINAL against a SECOND INSTANCE of itself (two ports, same code) yields a NEAR-EMPTY divergence report — proving the comparison is not spuriously noisy (any residual divergence is attributable ONLY to frontier/path nondeterminism, per the D8-01a caveat, not to backend-contract differences: endpoint set / data models / held behavior identical)"
    - "FLOOR PROOF: both target servers' own ledgers show ZERO mutations and ZERO destructive hits reached the backend during the compare runs (both floor ON — VALID-01 is generated safely against live apps)"
    - "compare-report.json is produced for each compare run and carries the honest determinism caveat; the divergence findings are read from it (machine-checkable), not just from stdout"
    - "If the 03-04 rebuild will NOT stand or self-drive comparably in the sandbox, the FALLBACK is taken (two versions of a hardened 06-06-family target: original vs a deliberately-diverged rebuild) and the report STATES which path was used and why (context-gathering confirms the primary 03-04 path is viable)"
    - "Full suite green as BOTH pre-gate and post-gate: baseline 858 (857 pass + 1 documented skip) — the dogfood harness lives under .planning/ only (node built-ins, zero deps); no src/ or test/ file is touched"
    - "PHASE + PROJECT CLOSE: ROADMAP Phase 8 → 2/2 Complete (dated) + all-8-phases Complete; STATE completed_phases 8, milestone v1.0 COMPLETE; REQUIREMENTS VALID-01 + VALID-02 → Complete (checklist + traceability); PROJECT.md/STATE.md record milestone v1.0 COMPLETE + the v1.1 enhancement backlog"
  artifacts:
    - path: ".planning/phases/08-differential-validation/08-02-live-verification/"
      provides: "the reproducible dogfood harness: app copies + the original's launcher, the run script that stands up the pair, invokes `archeo compare` via the real CLI, and asserts MATCH/FLAG + self-compare + floor ledgers; the produced compare-report.json(s)"
      exports: []
    - path: ".planning/phases/08-differential-validation/08-02-DOGFOOD-VERIFICATION.md"
      provides: "the evidence: the exact commands, the compare-report.json divergence findings mapped to the 03-04-documented differences, the self-compare control result, the floor ledgers, and the PASS verdict (or the fallback path taken)"
      exports: []
  key_links:
    - from: "the dogfood harness"
      to: "`archeo compare` (src/cli/compare.ts via src/cli/index.ts) against the 03-04 original+rebuild pair"
      via: "stand up both apps on localhost ports → `node src/cli/index.ts compare <origUrl> <rebuildUrl> --i-have-authorization --model scripted` → read compare-report.json"
      pattern: "archeo compare|compare-report.json|target-app.mjs|rebuild/server.js"
    - from: "the compare-report.json divergence findings"
      to: "the 03-04 buildability report's documented real differences"
      via: "GET /api/settings (added) + GraphQL/JSON-RPC write-vs-read + held-mutation shape guesses map to newEndpoints/changedShapes/heldStatusChanges; identical surface stays empty"
      pattern: "/api/settings|graphql|rpc|held"
---

<objective>
Close the whole project by CLOSING THE LOOP live: run `archeo compare` (shipped in 08-01) against
the **real Phase 3 pair** — the 03-04 buildability ORIGINAL app and its spec-only REBUILD — through
the real, unmodified CLI in real headed Chromium with the scripted provider and the floor ON for
both. Prove VALID-01 end-to-end: the divergence report **MATCHES** on the faithfully-rebuilt
surface (zero false positives) and **FLAGS** the known real differences the 03-04 report already
documented (the builder-added `GET /api/settings` — confirmed live as 404-vs-200 during
context-gathering — plus the GraphQL/JSON-RPC write-vs-read handling and the convention-guessed
held-mutation shapes). Add a **self-compare control** (original vs a second instance of itself →
near-empty divergence) to prove the comparison isn't spuriously noisy, and a **floor proof** (both
target ledgers show zero mutations/destructive hits reached the backend). This is the last honest
test of the entire arc: capture → spec → rebuild → **differential validation of the rebuild against
the original**.

Then close Phase 8 AND the project: ROADMAP Phase 8 Complete + all 8 phases Complete; STATE
completed_phases 8 + milestone v1.0 COMPLETE; REQUIREMENTS VALID-01/VALID-02 Complete; PROJECT.md
records milestone v1.0 COMPLETE and the standing v1.1 enhancement backlog. Real-key smoke: N/A
(scripted). No `src/` or `test/` change — the harness is `.planning/`-only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/phases/08-differential-validation/08-CONTEXT.md
@.planning/phases/08-differential-validation/08-01-SUMMARY.md
@src/cli/index.ts
@src/cli/compare.ts
@src/spec/drift.ts

<fixture_reality>
Confirmed during context-gathering (2026-07-04):
- ORIGINAL: `.planning/phases/03-spec-generator-buildability/03-04-buildability/target-app.mjs`
  EXPORTS `createServer()` and does NOT self-listen — it needs a 3-line launcher:
  `import { createServer } from './target-app.mjs'; createServer().listen(Number(process.env.PORT||4100),'127.0.0.1')`.
  Its HTML pages (/app → /app/users → /app/users/{id} → /app/settings) self-fire fetches on load,
  so `runExplore` with the scripted provider drives it.
- REBUILD: `.../03-04-buildability/rebuild/server.js` self-listens on `PORT` at 127.0.0.1 (default
  3000) and has a CommonJS `package.json` shim in `rebuild/`. Its HTML pages also self-fire fetches.
- Live probe result: `GET /api/settings` → 200 on the rebuild, 404 on the original (the builder
  ADDED it). This is the marquee documented divergence the report must FLAG.
- Both are node:http, zero deps, boot on Node 26. Copy both into a scratch dir under
  08-02-live-verification/ (do NOT run from the source path so the harness owns its ports/cwd).
- Port hygiene: assign fixed free ports (e.g. 4100 original, 4200 rebuild, 4300 original-clone);
  each `archeo explore` child runs in its own isolated run root (compare handles this), so the two
  same-hostname (localhost) targets never collide.
</fixture_reality>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pre-gate — full suite green at baseline 858</name>
  <files></files>
  <read_first>
    - 08-01-SUMMARY.md (what shipped: the compare command + its flags + the injected-runner design)
  </read_first>
  <behavior>
    - Run `node --test 'test/**/*.test.ts'`; confirm baseline 858 (857 pass + 1 documented skip, 0
      fail). Record the pre-gate number. Confirm LICENSE/NOTICE intact.
  </behavior>
  <action>Run the suite; record the clean pre-gate baseline.</action>
  <acceptance_criteria>
    - Suite green at 858; LICENSE/NOTICE intact.
  </acceptance_criteria>
  <verify><automated>node --test 'test/**/*.test.ts'</automated></verify>
  <done>A green baseline before the live dogfood.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Stand up the 03-04 ORIGINAL + REBUILD and run `archeo compare` live (VALID-01 MATCH + FLAG)</name>
  <files>.planning/phases/08-differential-validation/08-02-live-verification/, .planning/phases/08-differential-validation/08-02-DOGFOOD-VERIFICATION.md</files>
  <read_first>
    - 08-CONTEXT.md D8-03 (the dogfood steps + fallback)
    - the 03-04-buildability report / spec (the documented real differences to FLAG):
      `.planning/phases/03-spec-generator-buildability/03-04-buildability/` (archeo-spec.json,
      ground-truth.json, and the 03-04/03-05 SUMMARY notes on GET /api/settings, GraphQL/JSON-RPC,
      held-mutation shapes)
    - src/cli/compare.ts (how compare locates each produced archeo-spec.json + writes compare-report.json)
  </read_first>
  <behavior>
    - Build a reproducible harness under `08-02-live-verification/` (node built-ins, zero deps):
      copy `target-app.mjs` + `rebuild/` (incl. its `package.json` shim) into the harness dir; add a
      tiny `orig-launch.mjs` (`createServer().listen(PORT)`); a run script that:
        1. Boots the ORIGINAL on port 4100 and the REBUILD on port 4200 (127.0.0.1), waits for both
           to answer (poll `GET /app`).
        2. Runs the REAL CLI: `node src/cli/index.ts compare http://127.0.0.1:4100
           http://127.0.0.1:4200 --i-have-authorization --model scripted [--max-steps <n>]` — real
           headed Chromium, floor ON both, scripted provider. (A=original=4100, B=rebuild=4200.)
        3. Locates the produced `compare-report.json`, parses it, and asserts:
           * **MATCH:** endpoints present on BOTH sides with identical shape produce NO divergence
             entry (zero false positives on the identical surface);
           * **FLAG (added):** `GET /api/settings` appears in newEndpoints ("only in the rebuild") —
             the builder-added endpoint (live-confirmed 404-vs-200);
           * **FLAG (contract):** the GraphQL/JSON-RPC write-vs-read difference and/or the
             convention-guessed held-mutation shapes surface as changedShapes and/or
             heldStatusChanges (map each finding to the 03-04-documented difference; record exactly
             which categories fired — the EXACT set is discovered live and reconciled against the
             03-04 report, not pre-asserted blind).
    - Capture VERBATIM the commands, the compare-report.json divergence sections, and the mapping
      table (each flagged finding → the 03-04-documented difference it corresponds to). Note any
      divergence attributable to frontier/path nondeterminism separately (the D8-01a caveat) so it
      is not miscounted as a false positive.
    - **Fallback:** if the 03-04 rebuild will not stand or self-drive comparably (it does in the
      probe, but if the sandbox differs), fall back to a hardened 06-06-family target (original vs a
      deliberately-diverged rebuild that adds one endpoint + changes one response shape) and STATE
      the fallback path + why in the verification doc.
  </behavior>
  <action>Write + run the harness; collect the compare-report.json(s) and the transcript; write the
    MATCH/FLAG evidence + mapping table into 08-02-DOGFOOD-VERIFICATION.md. Prefer the real 03-04
    pair; record the path taken.</action>
  <acceptance_criteria>
    - `archeo compare` ran via the real unmodified CLI against the live 03-04 pair (or the stated
      fallback) in real Chromium, floor ON both, scripted provider.
    - compare-report.json exists; the identical surface produced zero divergence (no false
      positives); `GET /api/settings` is flagged as added; the GraphQL/JSON-RPC + held-shape
      differences are flagged and mapped to the 03-04-documented differences.
    - The determinism caveat is present in the report and honored in the analysis (frontier noise
      not miscounted).
  </acceptance_criteria>
  <verify><automated>test -f "$COMPARE_REPORT" && node -e "const r=require(process.env.COMPARE_REPORT); if(!r.report) throw new Error('no report'); console.log('newEndpoints',r.report.newEndpoints, 'changed',r.report.changedShapes.length, 'held',r.report.heldStatusChanges.length)"</automated></verify>
  <done>The rebuild's real divergences from the original are surfaced by `archeo compare` and the
    faithful surface stays clean — VALID-01 proven live end-to-end.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Self-compare control + floor proof</name>
  <files>.planning/phases/08-differential-validation/08-02-live-verification/, .planning/phases/08-differential-validation/08-02-DOGFOOD-VERIFICATION.md</files>
  <read_first>
    - the target-app.mjs ledger export (ledger.mutations / ledger.destructiveHits — the floor proof source)
  </read_first>
  <behavior>
    - **Self-compare control:** stand up TWO instances of the ORIGINAL (ports 4100 and 4300, same
      code) and run `archeo compare http://127.0.0.1:4100 http://127.0.0.1:4300 …`. Assert the
      divergence report is NEAR-EMPTY: identical endpoint set, identical data models, identical
      held behavior; any residual entry must be page/flow (frontier) noise per the D8-01a caveat —
      NOT a backend-contract divergence. This proves the comparison isn't spuriously noisy.
    - **Floor proof:** the target apps keep their own ledger of mutating/destructive requests that
      actually reached the backend. After ALL compare runs (Task 2 + this task), assert each app's
      ledger shows `mutations = 0` and `destructiveHits = 0` — the floor held for both targets
      throughout (VALID-01 generated safely against live apps). Both compare runs used the shipped
      floor-ON explore path with no write-enabling flag.
    - Record both results (self-compare divergence summary + the ledgers) in the verification doc.
  </behavior>
  <action>Run the self-compare + collect ledgers; write the control + floor evidence into
    08-02-DOGFOOD-VERIFICATION.md.</action>
  <acceptance_criteria>
    - Self-compare divergence is near-empty on the backend-contract signal (endpoint set / models /
      held behavior identical); residual is only frontier noise, explicitly labeled.
    - Both target ledgers: mutations = 0, destructiveHits = 0 across all compare runs.
  </acceptance_criteria>
  <verify><automated>node -e "const r=require(process.env.SELF_COMPARE_REPORT); const bad=r.report.newEndpoints.length+r.report.removedEndpoints.length+r.report.changedShapes.length+r.report.heldStatusChanges.length; console.log('backend-contract divergence entries:',bad)"</automated></verify>
  <done>The comparison is proven trustworthy (identical apps → no backend divergence) and both
    live targets were explored read-only (floor held).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Post-gate + PHASE & PROJECT close (ROADMAP / STATE / REQUIREMENTS / PROJECT)</name>
  <files>.planning/phases/08-differential-validation/08-02-DOGFOOD-VERIFICATION.md, .planning/phases/08-differential-validation/08-02-SUMMARY.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md, .planning/PROJECT.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 8 section + Progress table + the Phases checklist at the top)
    - .planning/REQUIREMENTS.md (VALID-01/VALID-02 checklist rows + traceability table)
    - .planning/STATE.md (progress block; format from the Phase 7 close)
    - .planning/PROJECT.md (Key Decisions / milestone framing — where to record v1.0 COMPLETE + v1.1 backlog)
  </read_first>
  <behavior>
    - Post-gate: `node --test 'test/**/*.test.ts'` green at 858 (harness is .planning/-only; no
      src/test change → count unchanged from 08-01's post-gate). LICENSE/NOTICE intact; no-network
      guard green.
    - Finalize 08-02-DOGFOOD-VERIFICATION.md: commands, compare-report.json findings + the
      MATCH/FLAG mapping table, the self-compare control, the floor ledgers, path taken
      (primary 03-04 vs fallback), and the PASS verdict.
    - **ROADMAP:** Phase 8 → `[x]` in the Phases checklist; Phase 8 Details → 2/2 plans, both waves
      ticked, dated 2026-07-04; Progress row `8. Differential Validation | 2/2 | Complete |
      2026-07-04`. Note ALL 8 phases Complete / PROJECT COMPLETE.
    - **REQUIREMENTS:** VALID-01 + VALID-02 → checked in the Differential Validation section AND
      flipped in the traceability table (Phase 8 rows → Complete).
    - **STATE:** Phase 8 CLOSED; progress `completed_phases 8`, `completed_plans +2`, `percent 100`;
      status = milestone v1.0 COMPLETE (all 8 phases); stopped_at / current-focus updated with the
      dogfood PASS; record the v1.1 enhancement backlog in Accumulated Context.
    - **PROJECT.md:** record milestone v1.0 COMPLETE and the standing v1.1 enhancement backlog
      (GraphQL schema depth; flow back-edges; auth-semantics richness; the 18 pre-existing `tsc`
      typecheck diagnostics [AN-1, 07-03]; the CONTRIBUTING test-layout diagram fix [AN-2, 07-03]) —
      all NON-BLOCKING v1.1 candidates, not v1.0 gaps.
  </behavior>
  <action>Run the post-gate; finalize the verification doc + SUMMARY; flip ROADMAP + REQUIREMENTS +
    STATE + PROJECT for phase AND project close. Commit the bookkeeping.</action>
  <acceptance_criteria>
    - ROADMAP Phase 8 = 2/2 Complete + all-8-phases Complete; STATE = v1.0 COMPLETE,
      completed_phases 8, percent 100; REQUIREMENTS VALID-01/VALID-02 Complete in BOTH the checklist
      and the traceability table; PROJECT.md records v1.0 COMPLETE + the v1.1 backlog.
    - `node --test 'test/**/*.test.ts'` green at 858; LICENSE/NOTICE unchanged.
    - 08-02-DOGFOOD-VERIFICATION.md carries the MATCH/FLAG evidence + self-compare + floor ledgers +
      PASS verdict; 08-02-SUMMARY.md records the close.
  </acceptance_criteria>
  <verify><automated>node --test 'test/**/*.test.ts'; grep -nE "VALID-0[12]" .planning/REQUIREMENTS.md | head</automated></verify>
  <done>Phase 8 is closed with live dogfood evidence; VALID-01/VALID-02 Complete; milestone v1.0 is
    COMPLETE — the whole capture→spec→rebuild→differential-validation loop is proven end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compare explores TWO live targets ↔ floor | Both the original AND the rebuild are live apps explored read-only. The dogfood asserts both target ledgers show zero mutations/destructive hits reached the backend — the floor held for both. |
| divergence report ↔ trust | A false positive on the identical surface would erode trust. The MATCH assertion (identical surface → no divergence) + the self-compare control (identical apps → near-empty) prove the report is not spuriously noisy; the determinism caveat separates frontier noise from real backend divergence. |
| two-target isolation ↔ cross-contamination | Both targets are on localhost (only ports differ). compare's per-target isolated run roots keep the two capture stores + per-hostname profiles separate; the dogfood exercises this same-hostname case for real. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-08-05 | Tampering (a write reaches a live target during the dogfood) | the two live explorations | mitigate | Floor ON both (compare has no --allow-writes); both target ledgers asserted mutations=0 / destructiveHits=0 after all runs. |
| T-08-06 | Repudiation (claimed divergence that isn't real / missed real divergence) | compare-report.json findings | mitigate | Findings read from compare-report.json (machine-checkable) and mapped to the 03-04-documented differences; self-compare control bounds spurious noise; caveat labels frontier noise. |
| T-08-07 | Tampering (cross-target state bleed skews the diff) | isolated run roots, live | mitigate | The dogfood runs same-hostname targets through compare's distinct isolated run roots; the self-compare control (same code, two ports) would EXPOSE bleed as spurious divergence and asserts near-empty. |

No package installs; the harness is `.planning/`-only (node built-ins); no new outbound surface;
the no-network guard is re-run as part of the suite.
</threat_model>

<verification>
- Pre-gate + post-gate: `node --test 'test/**/*.test.ts'` green at 858; LICENSE/NOTICE intact.
- The real, unmodified `archeo compare` ran against the live 03-04 original+rebuild pair (or the
  stated fallback) in real Chromium, floor ON both, scripted provider.
- compare-report.json: zero divergence on the identical surface (no false positives); `GET
  /api/settings` flagged as added; GraphQL/JSON-RPC + held-shape differences flagged and mapped to
  the 03-04-documented differences; caveat present.
- Self-compare control: identical apps → near-empty backend-contract divergence.
- Floor proof: both target ledgers mutations=0 / destructiveHits=0 across all runs.
- ROADMAP Phase 8 2/2 Complete + all-8 Complete; STATE v1.0 COMPLETE (completed_phases 8, 100%);
  REQUIREMENTS VALID-01/VALID-02 Complete; PROJECT.md records v1.0 COMPLETE + v1.1 backlog.
</verification>

<success_criteria>
- VALID-01 proven LIVE end-to-end: `archeo compare` surfaces the rebuild's real divergences from
  the original and stays clean on the faithful surface, generated safely (floor ON).
- The self-compare control + floor proof establish the comparison is trustworthy and read-only.
- Phase 8 AND the project are closed: milestone v1.0 COMPLETE; the capture→spec→rebuild→
  differential-validation arc is closed; the v1.1 enhancement backlog is recorded (non-blocking).
</success_criteria>

<output>
Create `.planning/phases/08-differential-validation/08-02-SUMMARY.md` and
`.planning/phases/08-differential-validation/08-02-DOGFOOD-VERIFICATION.md` when done.
This plan closes Phase 8 AND the project: update ROADMAP + STATE + REQUIREMENTS + PROJECT.md.
Commit style: `docs(08-02): …` (harness under .planning/ + bookkeeping — no src/test change),
each with the `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` trailer.
</output>
</content>
