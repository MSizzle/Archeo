---
phase: 06-hardening
plan: 07
type: execute
wave: 7
depends_on: ["06-06"]
files_modified:
  - src/cli/explore.ts
  - src/cli/index.ts
  - src/agent/resume.ts
  - test/cli/explore-isolation.test.ts
  - test/agent/resume.test.ts
autonomous: true
requirements: [COST-06, COST-01, DRIFT-01]

must_haves:
  truths:
    - "Pressing Enter at the session-expired prompt RESUMES the run; only typing 'abort' aborts"
    - "--max-tokens 0 (and --max-cost 0) means a zero budget that stops immediately, not 'no ceiling'"
    - "--resume seeds from the most-recent PRIOR session for the host, never the freshly-created current session"
  artifacts:
    - path: "src/cli/explore.ts"
      provides: "promptAuthResume with resolve-before-close + answered guard (04-01 pattern)"
    - path: "src/agent/resume.ts"
      provides: "latestSessionForHost excludes a supplied current-session dir"
  key_links:
    - from: "src/cli/explore.ts onAuthExpired"
      to: "promptAuthResume"
      via: "Enter → 'resume' (answered guard beats the synchronous close event)"
      pattern: "answered|resolve\\('resume'\\)|promptAuthResume"
---

<objective>
Close the three real source bugs the 06-06 autonomous live verification surfaced (all missed by
the unit suites, caught only under the live CLI). Bug 1 (BLOCKING COST-06): the session-expired
resume prompt aborts on every Enter because rl.close() fires 'close' synchronously before
resolve('resume') runs — the exact readline race 04-01 fixed in promptReady with an 'answered'
guard, never applied to onAuthExpired when 06-04 added it. Bug 2 (COST-01): --max-tokens 0 /
--max-cost 0 are silently coerced to "no ceiling" by a Number(x)||undefined guard, so a literal
zero budget does not stop. Bug 3 (DRIFT-01): --resume's latest-prior-session selection includes
the freshly-created current session in its lexical sort, so it can seed from itself (cold start).

Purpose: make the hardening features actually hold under the real CLI, then re-run the failed
live stages and close Phase 6.

Output: fixed explore.ts / index.ts / resume.ts with regression tests, then a live re-verification
of the three behaviors and the phase-close bookkeeping.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-hardening/06-06-FINDINGS.md
@.planning/phases/06-hardening/06-CONTEXT.md
@src/cli/explore.ts
@src/cli/index.ts
@src/agent/resume.ts
@src/cli/login.ts

<interfaces>
# 04-01 precedent to mirror for Bug 1 — src/cli/login.ts promptReady uses an 'answered' flag so
# the synchronous 'close' event cannot pre-empt the real answer. Apply the SAME shape here:
#   resolve the real answer FIRST (or guard with answered=true), THEN close; the 'close'
#   handler only resolves 'abort' if no answer was given.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix the auth-resume readline race (COST-06, BLOCKING)</name>
  <files>src/cli/explore.ts, test/cli/explore-isolation.test.ts</files>
  <read_first>
    - src/cli/explore.ts lines 286-296 (the onAuthExpired closure — the bug)
    - src/cli/login.ts (promptReady — the 'answered'-guard pattern that fixes this class)
  </read_first>
  <behavior>
    - Extract a named, testable `promptAuthResume(input, output): Promise<'resume'|'abort'>` that:
      Enter/empty/anything-but-abort → 'resume'; 'abort' (trimmed, case-insensitive) → 'abort';
      stdin closed WITHOUT a line (non-TTY EOF) → 'abort' (fail-safe, matches 04-01 close-guard).
    - The synchronous 'close' emitted by rl.close() inside the 'line' handler must NOT override
      the real answer — use an `answered` flag (resolve real answer, set answered=true, then close;
      the 'close' handler resolves 'abort' only when !answered).
    - onAuthExpired delegates to promptAuthResume(process.stdin, process.stdout).
  </behavior>
  <action>
    Mirror login.ts promptReady exactly (createInterface, SIGINT-restore convention if login.ts
    uses one, answered guard). Replace the inline onAuthExpired body with a call to the new
    function. Add a unit test that constructs a fake input stream, emits a bare newline, and
    asserts the promise resolves 'resume'; a second test emits 'abort\n' → 'abort'; a third
    closes the stream with no line → 'abort'. These tests must FAIL against the current
    resolve-after-close code and PASS after the fix.
  </action>
  <acceptance_criteria>
    - node --test 'test/cli/explore-isolation.test.ts' PASSES including the three promptAuthResume cases
    - A bare Enter resolves 'resume' (the previously-broken path)
    - grep shows an `answered` guard (or resolve-before-close ordering) in the resume prompt
  </acceptance_criteria>
  <verify><automated>node --test 'test/cli/explore-isolation.test.ts'</automated></verify>
  <done>Enter resumes; only 'abort' aborts; EOF-without-line fails safe to abort.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix --max-tokens 0 / --max-cost 0 zero-budget coercion (COST-01)</name>
  <files>src/cli/index.ts, test/cli/explore-isolation.test.ts</files>
  <read_first>
    - src/cli/index.ts (the explore action's numeric flag parsing — find the Number(x)||undefined guard)
  </read_first>
  <behavior>
    - --max-tokens 0 → maxTokens === 0 (a real zero ceiling → immediate 'budget' stop), not undefined
    - --max-cost 0 → maxCost === 0 likewise
    - Absent flag → undefined (unchanged); non-numeric/NaN → undefined with the existing clean handling
    - --max-steps and --pace-ms parsing unchanged (regression)
  </behavior>
  <action>
    Replace the `Number(x)||undefined` idiom for these numeric options with a parser that returns
    a finite number when the input parses to a finite number (INCLUDING 0) and undefined only when
    the flag is absent or NaN. A tiny pure helper (e.g. parseFiniteFlag) is fine; unit-test it.
    Keep BudgetTracker.exceeded()'s >= semantics (06-01) so maxTokens:0 stops before the first
    over-budget action while still emitting the landing-page partial spec.
  </action>
  <acceptance_criteria>
    - Test proves parseFiniteFlag('0') === 0, parseFiniteFlag(undefined) === undefined, parseFiniteFlag('x') === undefined
    - A source-inspection or spawn test confirms --max-tokens 0 reaches runExplore as 0
    - Full suite stays green
  </acceptance_criteria>
  <verify><automated>node --test 'test/cli/explore-isolation.test.ts'</automated></verify>
  <done>Literal 0 budgets mean zero, not unlimited.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix --resume latest-prior-session self-selection (DRIFT-01)</name>
  <files>src/agent/resume.ts, test/agent/resume.test.ts</files>
  <read_first>
    - src/agent/resume.ts (latestSessionForHost — the lexical sort that can include the current session)
    - src/cli/explore.ts priorSessionSpecPath (lines 127-150 — the CORRECT pattern that already excludes currentDir; mirror it)
  </read_first>
  <behavior>
    - latestSessionForHost(capturesRoot, hostname, excludeDir?) never returns excludeDir
    - When only the current (just-created) session exists, it returns null → cold start (correct), not self-seed
    - With a genuine prior session present, it returns that prior session dir
    - Host filtering unchanged; most-recent selection unchanged apart from the exclusion
  </behavior>
  <action>
    Add an excludeDir parameter (the current session dir) and filter it out before selecting the
    latest, mirroring priorSessionSpecPath's `.filter(d => d !== currentDir)`. Thread the current
    session dir from the explore CLI wiring into the call. Unit-test with two fake session dirs
    (prior + current) asserting the prior is chosen, and with only-current asserting null.
  </action>
  <acceptance_criteria>
    - node --test 'test/agent/resume.test.ts' PASSES including the exclusion cases
    - Only-current-session → null (cold start); prior present → prior chosen
  </acceptance_criteria>
  <verify><automated>node --test 'test/agent/resume.test.ts'</automated></verify>
  <done>--resume seeds from a real prior session or cold-starts; never from itself.</done>
</task>

<task type="checkpoint:gate">
  <name>Task 4: Full-suite regression gate</name>
  <files></files>
  <action>Run the full suite; every prior test plus the new regression tests must pass.</action>
  <acceptance_criteria>
    - node --test 'test/**/*.test.ts' exits 0; count &gt;= 848 baseline + new tests, 0 fail (1 documented skip allowed)
  </acceptance_criteria>
  <verify><automated>node --test 'test/**/*.test.ts'</automated></verify>
  <done>All green.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-30 | DoS/availability | auth-resume prompt | mitigate | The race made every re-auth abort — a session-expiry became an unrecoverable stop. Fix restores resume; EOF-without-line still fails safe to abort (no hang). |
| T-06-31 | Correctness | zero-budget coercion | mitigate | A user setting --max-tokens 0 to dry-run intends a hard stop; silent "no ceiling" could run up cost. Fix honors literal 0. |
| T-06-32 | Correctness | --resume self-seed | mitigate | Self-seeding silently cold-starts, losing incremental coverage the user expects; exclusion restores DRIFT-01 intent. |

No package installs — no supply-chain checkpoint required.
</threat_model>

<verification>
- Unit: Enter→resume, abort→abort, EOF→abort; parseFiniteFlag 0/undefined/NaN; resume excludes current dir.
- Full suite green.
- Live re-verification (handled by the orchestrator's 06-06 re-run after this plan): the auth-expiry
  stage resumes and completes with monotonic state across the pause; --max-tokens 0 stops on 'budget';
  --resume seeds from the prior session.
</verification>

<success_criteria>
- The three 06-06 live gaps are fixed and regression-pinned; COST-06 resume works under the real CLI.
</success_criteria>

<output>
Create `.planning/phases/06-hardening/06-07-SUMMARY.md` when done. Do NOT close Phase 6 in this
plan — the orchestrator re-runs the 06-06 live stages after this fix and closes the phase then.
</output>
