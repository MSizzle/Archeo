---
phase: 06-hardening
plan: 06
type: execute
wave: 6
depends_on: ["06-05"]
files_modified:
  - .planning/phases/06-hardening/06-06-live-verification/target-app.mjs
  - .planning/phases/06-hardening/06-06-live-verification/target-app-v2.mjs
  - .planning/phases/06-hardening/06-06-live-verification/run-hardening-verification.mjs
  - .planning/phases/06-hardening/06-06-HARDENING-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "The REAL, unmodified CLI (node src/cli/index.ts explore|diff|<url>) drives a trapped target app in REAL headed Chromium with the scripted provider + real floor; the harness lives entirely under .planning/ (node built-ins, zero deps) — no src/ or test/ file is touched"
    - "Budget stop: `explore --max-tokens 0` (scripted zero usage) → the printed stopReason is 'budget' AND the generated spec's coverage.stopReason==='budget' AND a partial spec exists (COST-01/03)"
    - "Change-detector evidence: a cosmetic-churn page (ticker/counter) drives the model-call-skip counter up while coverage still completes (COST-02)"
    - "Error recovery on a target with REAL cross-document navigations (NOT client routing): one flaky endpoint (500 then 200) + one dead link → the run completes, the issues counter N>0, no loud/halting errors, dashboard evidence — proving the D6-03 context-destroyed fix real-world-grade (COST-05, DASH-08)"
    - "Auth expiry: the server expires the session cookie mid-run → pause detected → the harness re-logs-in (auto-submit + Enter) → the run RESUMES from the saved frontier with a MONOTONIC state count across the pause (COST-06)"
    - "Drift: v1 vs v2 (v2 adds an endpoint, removes a page, changes a field type) → the drift report catches all three with ZERO false positives on the unchanged surface; the --resume run is incremental (starts at the prior state count, discovers only new) (DRIFT-01/02)"
    - "--allow-writes: against the disposable app, banner + confirmation observed, a write LANDS (server ledger 1), the spec marks allowWrites:true; then a default run confirms the floor is back (ledger 0) (FLOOR-08)"
    - "Conditional real-key smoke: run only if ANTHROPIC_API_KEY is present; otherwise record deferred-pending-key and still close the phase"
    - "Full suite green as BOTH the pre- and post-gate; phase-closing docs updated (ROADMAP Phase 6 Complete, STATE → Phase 7, REQUIREMENTS rows incl. CAP-06 Complete-with-scope-note referencing D6-07)"
  artifacts:
    - path: ".planning/phases/06-hardening/06-06-live-verification/run-hardening-verification.mjs"
      provides: "the autonomous driver: spawns the real CLI against the target apps, asserts every hardening invariant, exits 0 on all-green"
      exports: []
    - path: ".planning/phases/06-hardening/06-06-HARDENING-VERIFICATION.md"
      provides: "the evidence report + invariant table"
      exports: []
  key_links:
    - from: "run-hardening-verification.mjs"
      to: "node src/cli/index.ts explore|diff|login|<url>"
      via: "child_process spawns of the REAL unmodified CLI against target-app.mjs / target-app-v2.mjs; assertions read server ledgers, the capture store, the generated spec, and /events SSE"
      pattern: "spawn|src/cli/index.ts"
---

<objective>
Prove the whole hardening layer end-to-end with the **real, unmodified CLI** — the same
autonomous-checkpoint class verified for Phases 2–5 (02-04 / 03-04 / 04-03 / 05-05), autonomous
per D6-08. Build a trapped target-app family (v1 + v2) whose **recovery stage uses REAL
cross-document navigations, not client routing**, so the D6-03 context-destroyed fix is proven
real-world-grade rather than demo-grade. Drive `archeo explore` / `diff` / `<url>` through **real
headed Chromium** with the **scripted provider** and the **real floor**, and assert every Phase-6
invariant: a token-budget stop with a partial spec; the change-detector skip counter climbing
while coverage completes; flaky-endpoint + dead-link recovery with a quiet surface across full-page
navigations; a mid-run session-expiry pause → harness re-login → resume from the saved frontier
with a monotonic state count; a v1→v2 drift report catching an added endpoint, a removed page, and
a changed field type with zero false positives plus an incremental `--resume` run; an
`--allow-writes` run that actually lands a write (ledger 1, spec marks the mode) followed by a
default run proving the floor is back (ledger 0); and a conditional real-key smoke (expect
deferred-pending-key). Then close the phase.

Output: `.planning/`-only harness (target apps + driver), a verification report, and the
phase-closing bookkeeping (ROADMAP / STATE / REQUIREMENTS). **No `src/` or `test/` file is
touched.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-hardening/06-CONTEXT.md
@.planning/phases/06-hardening/06-01-PLAN.md
@.planning/phases/06-hardening/06-02-PLAN.md
@.planning/phases/06-hardening/06-03-PLAN.md
@.planning/phases/06-hardening/06-04-PLAN.md
@.planning/phases/06-hardening/06-05-PLAN.md
@.planning/phases/05-autonomous-agent-loop/05-05-PLAN.md
@.planning/phases/05-autonomous-agent-loop/05-05-live-verification/target-app.mjs
@.planning/phases/05-autonomous-agent-loop/05-05-live-verification/run-explore-verification.mjs

<notes>
- The harness reuses the 05-05 family patterns (login wall via planted creds + persistent cookie;
  server-side ledgers; real headed Chromium; the driver spawns the REAL CLI). It extends them for
  Phase 6, and CRITICALLY the recovery/error stage serves **full HTML pages with `<a href>`
  full-page navigations** (NOT history.pushState) so `page.evaluate` genuinely races the
  cross-document navigation — the exact condition observeWithRecovery (06-03) must survive.
- Budget stop uses `--max-tokens 0`: the scripted provider reports zero usage, and
  BudgetTracker.exceeded() uses `>=`, so a zero budget forces `stopReason 'budget'` after the
  first observation. The landing-page load already captured traffic, so the partial spec is
  non-empty. (This reconciles D6-01's "scripted reports zeros" with D6-08's "tiny --max-tokens
  forces stopReason budget" — recorded plainly in the report; real usage-driven budgets are for
  the deferred real-key smoke.)
- Real-key smoke: check `process.env.ANTHROPIC_API_KEY` at runtime; if absent (expected), record
  deferred-pending-key and DO NOT fail the phase.
</notes>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build the Phase-6 trapped target-app family (v1 + v2), incl. a REAL cross-document-navigation surface</name>
  <files>.planning/phases/06-hardening/06-06-live-verification/target-app.mjs, .planning/phases/06-hardening/06-06-live-verification/target-app-v2.mjs</files>
  <read_first>
    - 05-05-live-verification/target-app.mjs (login wall, ledgers, SPA + endpoints — the base to extend)
    - 06-CONTEXT.md §D6-08 (every stage's target requirement; recovery MUST use real cross-document navigation)
  </read_first>
  <behavior>
    - v1 (target-app.mjs) serves, over node:http, a login wall (planted creds → persistent session cookie), authenticated pages reachable by REAL full-page `<a href>` navigations, plus: a cosmetic-churn page (a counter/ticker that updates text on each load but changes no structure); a flaky endpoint (`/api/flaky` → 500 on the 1st hit, 200 after); a dead link (`<a href="/gone">` → 404); the REST/GraphQL/JSON-RPC endpoints; server-side ledgers (mutations, destructiveHits, logoutHits, flakyHits, api401); a mutable session-expiry switch (an endpoint the harness pokes to make `/api/*` start returning 401 mid-run)
    - v2 (target-app-v2.mjs) is v1 with three deliberate drifts: a NEW endpoint (e.g. `/api/reports`), a REMOVED page (one authenticated page 404s), and a CHANGED field type on an existing endpoint's response (e.g. `id` number → string)
    - both apps expose an auto-submitting login form path so the harness can re-authenticate headlessly during the pause stage
  </behavior>
  <action>
    Extend a COPY of the 05-05 target app into target-app.mjs (v1) with the churn page, flaky endpoint, dead link,
    real cross-document navigations, and the mid-run 401 switch + ledgers. Create target-app-v2.mjs by importing/duplicating
    v1 and applying the three drifts. node built-ins only, zero deps. These are fixtures under .planning/ (GATE-03 scans
    src/ only — accepted, as in 02-04/03-04/04-03/05-05).
  </action>
  <acceptance_criteria>
    - `node .planning/phases/06-hardening/06-06-live-verification/target-app.mjs` boots and serves the login wall + full-page-navigation authenticated pages + flaky/dead/churn surfaces (a smoke curl in the driver confirms)
    - v2 differs from v1 by exactly the added endpoint, removed page, and changed field type
    - a page transition in the authenticated area is a REAL cross-document navigation (full HTML response), not pushState
  </acceptance_criteria>
  <verify><automated>node -e "process.exit(0)"</automated></verify>
  <done>A disposable, self-contained app family that exercises every Phase-6 hardening path — including the real cross-document navigation the recovery fix must survive.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: The autonomous driver — spawn the real CLI, assert every hardening invariant</name>
  <files>.planning/phases/06-hardening/06-06-live-verification/run-hardening-verification.mjs</files>
  <read_first>
    - 05-05-live-verification/run-explore-verification.mjs (the spawn+assert harness pattern, /events SSE subscription, ledger reads, exit-0-on-all-green)
    - 06-01..06-05 plans (the exact printed strings, spec coverage fields, manifest flags, and CLI flags to assert against)
  </read_first>
  <behavior>
    - Stage A (budget): `explore <v1> --max-tokens 0 --no-dashboard` → asserts the printed stopReason 'budget' AND coverage.stopReason==='budget' in the generated spec AND the spec is non-empty (partial)
    - Stage B (change-detector): `explore <v1-churn-entry>` → asserts modelCallsSkipped > 0 in coverage AND coverage still completes (states discovered ≥ a threshold); dashboard 'skip' counter observed via /events
    - Stage C (recovery, REAL cross-document nav): `explore <v1>` with the flaky endpoint + dead link → asserts the run COMPLETES, the issues counter N>0 (recoverable), NO halting banner/terminal-halt line, and — the key proof — the run traversed a real full-page navigation without dying (server page-hit ledger shows the post-navigation page was observed); /events shows muted 'error' events, no 'halt'
    - Stage D (auth expiry): start `explore <v1>`, poke the 401 switch mid-run → assert the pause prompt appears, the harness drives the auto-submit login + writes Enter to the CLI stdin → the run RESUMES and completes; state count after resume ≥ state count before the pause (monotonic); server saw zero capture during the paused window (the interceptor pass-through path)
    - Stage E (drift + incremental): run against v1, then `explore <v2> --resume` → assert the --resume run starts with the prior state count (incremental) and the auto-diff / `diff <v1session> <v2session>` report contains the added endpoint, the removed page, the changed field type, and ZERO false positives on the unchanged surface
    - Stage F (--allow-writes): `explore <v1> --allow-writes --i-accept-writes` → assert the banner text printed, a write LANDED (server mutations ledger == 1), spec coverage.allowWrites===true; then a default `explore <v1>` → assert mutations ledger unchanged (floor back on) + no allowWrites flag
    - Stage G (real-key smoke): if ANTHROPIC_API_KEY present → a short real-model run + qualitative note; else record deferred-pending-key (do NOT fail)
    - the driver prints a GREEN/RED line per invariant and exits 0 only when all non-deferred invariants are GREEN
  </behavior>
  <action>
    Write run-hardening-verification.mjs (node built-ins only) that boots the target app(s), spawns the REAL CLI
    (`node src/cli/index.ts ...`) per stage against real headed Chromium, subscribes to /events where relevant, reads
    server ledgers + the capture store + the generated spec + drift-report.json, drives stdin for the pause/resume and
    allow-writes prompts, asserts every invariant above, and exits 0 on all-green (deferred real-key excluded). Login
    reuses the Phase-4 handoff (spawn `login` first, auto-submit, ready). Capture stages that need deterministic SIGINT
    exit may run `--no-dashboard`; dashboard-evidence stages keep it on and read /events.
  </action>
  <acceptance_criteria>
    - `node .planning/phases/06-hardening/06-06-live-verification/run-hardening-verification.mjs` exits 0 with every non-deferred invariant GREEN (or documents a real-key deferral)
    - the recovery stage demonstrably crosses a REAL cross-document navigation and survives (an assertion tied to a server-side post-navigation page-hit ledger, not just SPA routing)
    - no `src/` or `test/` file is modified by this task
  </acceptance_criteria>
  <verify><automated>node .planning/phases/06-hardening/06-06-live-verification/run-hardening-verification.mjs</automated></verify>
  <done>Every Phase-6 hardening claim is proven by the real CLI against a live app — budgets, skips, recovery-through-real-navigation, pause/resume, drift, and the sanctioned write bypass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Evidence report + phase-closing bookkeeping</name>
  <files>.planning/phases/06-hardening/06-06-HARDENING-VERIFICATION.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - 05-05-EXPLORE-VERIFICATION.md (report shape + invariant table)
    - .planning/ROADMAP.md (Phase 6 section + Progress table), .planning/REQUIREMENTS.md (COST/FLOOR/CAP/DASH/DRIFT rows + traceability), .planning/STATE.md
  </read_first>
  <behavior>
    - the report records every stage's invariant (GREEN/RED), the reproducible command, the budget-stop reconciliation note (--max-tokens 0 under scripted zero usage), the real-cross-document-navigation recovery proof, and the real-key deferral (if applicable)
    - ROADMAP Phase 6 → 6/6 Complete (dated); STATE → Phase 7 (Open Source Readiness) Open; REQUIREMENTS COST-01..06, FLOOR-08, DASH-08, DRIFT-01/02 → Complete, and CAP-06 → **Complete-with-scope-note** referencing D6-07 (seam, not a bundled local model) — in BOTH the checklist and the traceability table
    - the full suite is green as both the pre- and post-gate; the pre-existing unstaged .gitignore edit is left unstaged
  </behavior>
  <action>
    Write 06-06-HARDENING-VERIFICATION.md with the invariant table + evidence. Update ROADMAP (Phase 6 Complete +
    Progress row 6/6), STATE (Phase 6 closed → Phase 7 next, accumulated decisions for 06-01..06-06), and REQUIREMENTS
    (flip the 11 Phase-6 rows; CAP-06 marked Complete-with-scope-note per D6-07). Run `node --test 'test/**/*.test.ts'`
    as the post-gate and record the count.
  </action>
  <acceptance_criteria>
    - `node --test 'test/**/*.test.ts'` fully green as the post-gate (record the number; ≥ 612 + all Phase-6 additions)
    - ROADMAP Phase 6 shows 6/6 Complete; STATE status = Phase 7 next; REQUIREMENTS all 11 Phase-6 rows Complete with CAP-06 carrying the scope note
    - the report's invariant table is all-GREEN (real-key smoke may read deferred-pending-key)
  </acceptance_criteria>
  <verify><automated>node --test 'test/**/*.test.ts'</automated></verify>
  <done>Phase 6 is closed with autonomous live evidence, the requirements ledger is updated (CAP-06 honestly scope-noted), and the project is queued at Phase 7.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live harness ↔ real CLI + real Chromium | The verification must exercise the REAL, unmodified CLI against a live app in real Chromium — a mock would not prove the context-destroyed fix, the pause pass-through, or the write bypass. The harness lives under .planning/ and touches no src/. |
| recovery proof ↔ real cross-document navigation | The recovery stage MUST use full-page navigations (not pushState); otherwise the D6-03 fix is only demo-grade — the exact 05-05 gap this phase closes. |
| --allow-writes proof ↔ disposable app only | The write that lands is against a throwaway local target with a server-side ledger — never a real account. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-06-19 | Repudiation (unproven hardening claims) | the whole phase | mitigate | The autonomous driver asserts every invariant against server ledgers, the capture store, the spec, and /events, and exits non-zero on any RED — no claim is taken on faith. |
| T-06-20 | Tampering (demo-grade recovery mistaken for real-world-grade) | the recovery stage | mitigate | The stage uses REAL cross-document navigations and ties its pass criterion to a server-side post-navigation page-hit ledger. |
| T-06-21 | Elevation (a real write escaping to a real account) | --allow-writes stage | mitigate | Runs only against the disposable local app; the ledger confirms exactly one write under the flag and zero on the following default run. |

No package installs; the harness is node built-ins only. GATE-03 scans src/ (unchanged); the no-network guard stays green.
</threat_model>

<verification>
- `node .planning/phases/06-hardening/06-06-live-verification/run-hardening-verification.mjs` exits 0 — every non-deferred invariant GREEN (budget stop + partial spec; skip counter up + coverage complete; recovery through a REAL cross-document navigation with a quiet surface; pause→relogin→resume monotonic; drift catches 3 changes with zero false positives + incremental --resume; allow-writes ledger 1 then default ledger 0; real-key deferred-pending-key if no key).
- `node --test 'test/**/*.test.ts'` fully green as both the pre- and post-gate.
- No `src/` or `test/` file touched; the pre-existing unstaged `.gitignore` edit left unstaged.
</verification>

<success_criteria>
- Every Phase-6 success criterion (long-session budget/pacing; change-detector gating; pause/resume; drift-aware re-runs; quiet self-healing error surface) is proven live by the real CLI, autonomously.
- The 05-05 demo→real-world gap is closed on-screen: the loop survives a real cross-document navigation.
- Phase 6 is Complete; STATE advances to Phase 7; REQUIREMENTS reflect it (CAP-06 Complete-with-scope-note per D6-07).
</success_criteria>

<output>
Create `.planning/phases/06-hardening/06-06-SUMMARY.md` when done.
The phase-closing docs commit updates ROADMAP + STATE + REQUIREMENTS. Commit style for any harness
files: `test(06-06): …` / `docs(06-06): …` with the
`Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` trailer. No src/ or test/ changes.
</output>
