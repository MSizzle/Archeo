---
phase: 02-capture-layer-safety-floor
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified: []
autonomous: false
requirements: [FLOOR-01, FLOOR-02, FLOOR-03, FLOOR-04, FLOOR-05, FLOOR-06, FLOOR-07, CAP-01, CAP-02, CAP-03, CAP-04, CAP-05]

must_haves:
  truths:
    - "Against a real, logged-in account, no mutating request reaches the server while the floor is on"
    - "The on-disk store contains redacted records with no secret values, and held-mutation records with full shapes"
    - "A destructive GET prompts before firing; a GraphQL/JSON-RPC mutation is held"
  artifacts: []
  key_links: []
---

<objective>
Human-verify the complete capture layer and safety floor against a real, authenticated web app — the one thing the automated mock-route suites cannot prove: that the live browser path actually holds mutations, captures redacted traffic, prompts on destructive GETs, and never leaks secrets to disk.

Purpose: This is the live proof of the phase goal. The automated suites prove the logic; this checkpoint proves the wiring against a real account before the phase is marked done.

Output: Human confirmation (or a list of issues to feed a gap-closure plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-capture-layer-safety-floor/02-CONTEXT.md
@.planning/phases/02-capture-layer-safety-floor/02-01-SUMMARY.md
@.planning/phases/02-capture-layer-safety-floor/02-02-SUMMARY.md
@.planning/phases/02-capture-layer-safety-floor/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full automated suite as the pre-checkpoint gate</name>
  <files></files>
  <read_first>
    - .planning/phases/02-capture-layer-safety-floor/02-RESEARCH.md § "Validation Architecture" (full-suite command)
  </read_first>
  <action>
    Run the full test suite and confirm every Phase 1 and Phase 2 capture suite is green before asking the human to verify live. If anything is red, stop and surface it — do not proceed to the live checkpoint with a failing suite.
  </action>
  <acceptance_criteria>
    - `node --test 'test/**/*.test.ts'` exits 0 with all capture suites (classifier, redactor, store, interceptor) and all Phase 1 suites passing
  </acceptance_criteria>
  <verify>
    <automated>node --test 'test/**/*.test.ts'</automated>
  </verify>
  <done>Full suite green; ready for live verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live floor verification against a real authenticated account</name>
  <files></files>
  <read_first>
    - .planning/phases/02-capture-layer-safety-floor/02-CONTEXT.md (the floor invariants being verified)
  </read_first>
  <action>
    Human-only step: drive the headed Chromium against a real app the user owns and confirm the live floor behaviour described in how-to-verify. No code changes. If any check fails, capture it for a `--gaps` closure plan.
  </action>
  <what-built>The full capture layer and safety floor wired into the headed Chromium: target-scoped interception, REST/GraphQL/JSON-RPC write-holding, destructive-GET tripwire, redacted JSONL capture store with held-mutation records, shaped synthetic responses, and the dead-end signal.</what-built>
  <how-to-verify>
    1. Pick a web app you own or are authorized to analyze. Log in by hand first if needed (manual auth handoff is Phase 4; for now log in, then run against an authenticated URL).
    2. Run: `node src/cli/index.ts https://<your-app> --i-have-authorization` and accept the authorization gate.
    3. Browse normally: load a few pages that issue GET/XHR reads, then attempt an action that issues a write (save a setting, post a comment) — but on data you are willing to test against.
    4. Confirm READS work: the pages load and `.archeo/captures/session-*/capture.jsonl` accumulates `type:"request-response"` records within seconds (FLOOR-01 read path, CAP-01).
    5. Confirm WRITES are held: the write action returns success in the UI but the change does NOT actually persist on the server (refresh to check); the store shows a `held:true` record with method/URL/headers/body (FLOOR-01/02/05/06). If the app uses GraphQL/JSON-RPC, confirm a mutation is also held while queries still work (FLOOR-03).
    6. Confirm the DESTRUCTIVE-GET tripwire: navigate to or trigger a URL whose path contains a destructive token (e.g. `.../revoke`, `.../delete`); the terminal must prompt `[y/N]` before the request fires. Answer N and confirm nothing happened (FLOOR-04).
    7. Confirm REDACTION: `grep -ri 'authorization' .archeo/captures` shows header NAMES but `[REDACTED]` values; search the JSONL for any real bearer token, cookie value, email, or password from your session — there must be NONE; non-allowlisted field values appear as type names like `"string"` (CAP-02/03/04/05).
    8. (If reproducible) Confirm the DEAD-END signal: after a held write, if the app errors (4xx/5xx) on the next read, a `type:"dead-end"` record with `relatedHeldWriteId` appears (FLOOR-07).
  </how-to-verify>
  <acceptance_criteria>
    - No mutating request (REST write, GraphQL/JSON-RPC mutation) actually changed server state during the session
    - The JSONL store contains redacted records with no secret values and at least one held-mutation record with full shape
    - The destructive-GET prompt fired before any destructive GET
  </acceptance_criteria>
  <verify>
    <human-check>Human confirms all eight verification steps pass against a real authenticated account.</human-check>
  </verify>
  <done>Human has confirmed the live floor holds all mutations, the store is redacted and structured, and the destructive-GET tripwire works — or filed issues for gap closure.</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issue (e.g. a write reached the server, a secret survived redaction, the tripwire did not fire) so a `--gaps` closure plan can be created.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live account → server | The whole phase exists to prove this boundary is never crossed by a mutation |
| live session secrets → disk | The store must contain zero live credentials after a real authenticated session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Tampering | live floor end-to-end | mitigate | Human verifies no mutation reached the server during a real session; any leak routes to a gap-closure plan before phase sign-off. |
| T-02-13 | Information Disclosure | live capture store | mitigate | Human greps the real store for live secrets; presence of any secret is a blocking failure. |

No package installs in this phase — no supply-chain (T-*-SC) checkpoint required.
</threat_model>

<verification>
- Full automated suite green (gate).
- Live: no mutation reaches the server; store carries no live secrets; destructive-GET prompt fires; held-mutation records present.
</verification>

<success_criteria>
- A human confirms, against a real authenticated account, that the floor holds all mutations, the store is redacted and structured, and the destructive-GET tripwire works — closing the Phase 2 goal.
</success_criteria>

<output>
Create `.planning/phases/02-capture-layer-safety-floor/02-04-SUMMARY.md` when done.
</output>
