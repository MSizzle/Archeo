---
phase: 10-vision-drivable-fixtures
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [FIX-01]
files_modified:
  - examples/demo-app/server.mjs
  - examples/demo-app/launch.mjs
  - examples/demo-app/README.md
  - .planning/phases/10-vision-drivable-fixtures/10-01-live-verification/
  - .planning/phases/10-vision-drivable-fixtures/10-01-DRIVABILITY-VERIFICATION.md
  - .planning/phases/10-vision-drivable-fixtures/10-01-SUMMARY.md
  - .planning/ROADMAP.md

must_haves:
  truths:
    - "A canonical, vision-drivable demo target app exists at examples/demo-app/server.mjs — plain node:http, ZERO runtime deps — with a PORT launcher examples/demo-app/launch.mjs (default 127.0.0.1:<PORT>), following the 03-04/05-05/06-06/08-02 fixture pattern"
    - "Navigation is expressed as REAL clickable affordances: every route-to-route move is a real <a href> (or, sanctioned fallback, <a data-spa> pushState) present in the DOM inventory — NOT a JS-only location.href side effect. This is the single property the 03-04 fixture lacked (its go(p) helper set location.href in setTimeout with no clickable element), and the reason it captured 0 autonomous steps"
    - "The app is MULTI-PAGE (>= 3 routes, e.g. /app, /app/users, /app/users/{id}, /app/settings) and each route AUTO-FIRES its /api batch on load — so BOTH a click-driven manual run AND the autonomous frontier-walker capture comparable coverage"
    - "At least one route carries a real FORM (input/select + submit button) so the agent's fill action (obviously-fake syntheticValue) is exercised and a held write is produced"
    - "The protocol surface is complete: REST reads (a list + a detail endpoint that collapse to /{id}), a related model (a reference field → relationship inference), REST held writes (POST/PUT/DELETE), a GraphQL endpoint (POST /graphql, query passes / mutation held), and a JSON-RPC endpoint (POST /rpc, read passes / write held)"
    - "Seed data is deterministic and obviously-fake (example.test emails, demo-prefixed tokens, fixed IDs/timestamps) — NO real secrets — so regenerated example specs are trivially secret-clean; redaction (CAP-05) still runs and is re-asserted, but even pre-redaction nothing is sensitive"
    - "The app is NOT login-walled (compare has no login step and the floor would hold a login POST) — one app drives manual, autonomous, AND compare with zero login friction"
    - "A live drivability harness under .planning/phases/10-vision-drivable-fixtures/10-01-live-verification/ (node built-ins, zero deps, no src/ or test/ edits) boots examples/demo-app via its launcher and runs the REAL, UNMODIFIED CLI (node src/cli/index.ts explore <url> --i-have-authorization --model scripted --max-steps <n>) in real headed Chromium with the floor ON"
    - "The harness asserts from the produced artifacts (archeo-spec.json + agent-step records / stop summary): steps > 0 AND discovered states >= 2 AND the full protocol surface captured (REST + GraphQL POST /graphql + JSON-RPC POST /rpc endpoints present; held writes flagged held:true) — the EXACT assertion 03-04 failed (0 steps, page-1 only)"
    - "FLOOR PROOF: the demo app keeps its own ground-truth ledger (via the .planning/-only ledger-wrap monkeypatch, 08-02 pattern) showing mutations=0 and destructiveHits=0 reached the backend during the explore run (floor ON, no write-enabling flag)"
    - "Full suite green as pre- AND post-gate: baseline 894 (893 pass + 1 documented skip, 0 fail); npx tsc --noEmit exits 0 (QUAL-02 guard). No src/ or test/ file is touched; the app lives under examples/, the harness under .planning/. LICENSE/NOTICE intact"
  artifacts:
    - path: "examples/demo-app/server.mjs"
      provides: "the canonical vision-drivable ORIGINAL demo app: real <a href> multi-page nav + a form + REST/GraphQL/JSON-RPC + held writes + deterministic obviously-fake seed data; node:http, zero deps"
      exports: ["createServer", "makeApp"]
    - path: "examples/demo-app/launch.mjs"
      provides: "PORT launcher (createServer().listen(Number(process.env.PORT||<default>),'127.0.0.1'))"
      exports: []
    - path: "examples/demo-app/README.md"
      provides: "what the app exposes (routes + protocol surface), how to run it (node examples/demo-app/launch.mjs), and that it is the source of the regenerated example specs"
      exports: []
    - path: ".planning/phases/10-vision-drivable-fixtures/10-01-live-verification/"
      provides: "the reproducible drivability harness: the ledger-wrap floor-proof shim + a run script that boots the app, runs the real archeo explore, and asserts steps>0 + states>=2 + full protocol surface + floor-clean ledger"
      exports: []
    - path: ".planning/phases/10-vision-drivable-fixtures/10-01-DRIVABILITY-VERIFICATION.md"
      provides: "the evidence: the exact commands, the explore stop summary (steps/states), the captured endpoint set incl. GraphQL/JSON-RPC, the held-write flags, the floor ledger, and a side-by-side note vs 03-04's 0-step failure"
      exports: []
  key_links:
    - from: "the demo app's real <a href> navigation affordances"
      to: "the autonomous frontier-walker (src/agent/loop.ts classifyInventory → nav frontier items)"
      via: "the scripted provider inventories <a href> elements → non-empty frontier → >0 steps + multiple states (unlike 03-04's empty frontier)"
      pattern: "a href|<a |data-spa|classifyInventory|frontier"
    - from: "the drivability harness"
      to: "the shipped archeo explore CLI (src/cli/explore.ts via src/cli/index.ts) against examples/demo-app/"
      via: "node examples/demo-app/launch.mjs → node src/cli/index.ts explore http://127.0.0.1:<PORT> --i-have-authorization --model scripted → read archeo-spec.json + agent-step records"
      pattern: "archeo explore|examples/demo-app|archeo-spec.json|steps"
---

<objective>
Build the ONE canonical, vision-drivable demo target app at `examples/demo-app/` that closes the
genuine 08-02 finding: an app whose navigation is expressed as **real `<a href>` clickable
affordances** (not JS-only `location.href`), multi-page, auto-firing each route's `/api` batch on
load, with a form and a full REST/GraphQL/JSON-RPC surface and deterministic obviously-fake seed
data — so that BOTH the manual capture path and the autonomous agent drive it and produce
comparable coverage. Prove it with a **live drivability harness** that runs the real, unmodified
`archeo explore` against it and asserts the EXACT thing the 03-04 fixture failed: **steps > 0**,
**multiple discovered states**, and the **full protocol surface captured** — floor ON, ledger
clean. No `src/`/`test/` change; the app lives under `examples/`, the harness under `.planning/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-vision-drivable-fixtures/10-CONTEXT.md
@src/agent/observation.ts
@src/agent/loop.ts
@src/agent/blocklist.ts
@src/cli/explore.ts

<fixture_templates>
Study and distil these proven in-repo templates (do NOT copy wholesale — the demo app must be
clean, self-explanatory, shippable, and non-login-walled):
- `.planning/phases/06-hardening/06-06-live-verification/target-app.mjs` — REAL cross-document
  `<a href>` full-page navigation + per-route auto-fired `/api` batches + REST/GraphQL/JSON-RPC +
  held writes. (It is login-walled + trapped — strip the login wall, the traps, the mid-run
  expiry, and the ledger monkeypatch out of the SHIPPED app.)
- `.planning/phases/08-differential-validation/08-02-live-verification/fallback/app.mjs` — the
  non-login-walled, drivable pattern (SPA `<a data-spa>` + pushState) that self-drove to 18
  endpoints in 4 steps. Use as the fallback nav style if cross-document proves flaky.
- `.planning/phases/03-spec-generator-buildability/03-04-buildability/rebuild/server.js` — the
  clean REST/GraphQL/JSON-RPC + `<a href>` nav + settings `<form>` shape to mirror.
- `.planning/phases/08-differential-validation/08-02-live-verification/apps/ledger-wrap.mjs` — the
  `node:http` monkeypatch that tees a floor-proof ledger at `/__ledger__` (keep in the harness).

Key drivability fact (src/agent/loop.ts classifyInventory + src/agent/observation.ts
INVENTORY_BROWSER_FN): an app is vision-drivable IFF its navigation is a real `<a href>` (or a
button) in the DOM inventory. The 03-04 app's `go(p)` set `location.href` in a `setTimeout` with
no clickable element → empty frontier → 0 steps. The fix is: real `<a href>` links.
</fixture_templates>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pre-gate — full suite green (894) + tsc clean</name>
  <files></files>
  <read_first>
    - .planning/phases/10-vision-drivable-fixtures/10-CONTEXT.md (D10-02/D10-03/D10-06)
  </read_first>
  <behavior>
    - Run `node --test 'test/**/*.test.ts'`; confirm baseline 894 (893 pass + 1 documented skip
      `test/agent/observation.test.ts`, 0 fail). Run `npx tsc --noEmit`; confirm exit 0 (the
      QUAL-02 guard). Confirm LICENSE/NOTICE intact. Record the pre-gate numbers.
  </behavior>
  <action>Run the suite + typecheck; record the clean baseline.</action>
  <acceptance_criteria>
    - Suite green at 894; `tsc --noEmit` exit 0; LICENSE/NOTICE intact.
  </acceptance_criteria>
  <verify><automated>node --test 'test/**/*.test.ts' && npx tsc --noEmit</automated></verify>
  <done>A green baseline before the fixture is added.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build the canonical vision-drivable demo app (examples/demo-app/)</name>
  <files>examples/demo-app/server.mjs, examples/demo-app/launch.mjs, examples/demo-app/README.md</files>
  <read_first>
    - the fixture templates above (06-06 for real <a href> nav + auto-fire; 03-04-rebuild for the
      clean REST/GraphQL/JSON-RPC + settings form shape; 08-02 fallback for the non-login-walled
      pattern)
    - src/agent/observation.ts (INVENTORY_BROWSER_FN — what counts as an interactive element)
    - src/agent/loop.ts (classifyInventory — <a href> → nav frontier; input/select → form)
  </read_first>
  <behavior>
    - Write `examples/demo-app/server.mjs` — plain `node:http`, ZERO deps, exporting
      `createServer()` and `makeApp(opts)` (parity with the fixture shape). It serves:
      * **Multi-page routes** (>= 3; recommend `/app`, `/app/users`, `/app/users/{id}`,
        `/app/settings`) as full HTML documents. **Every route-to-route link is a real `<a href>`**
        rendered in the DOM (a `<nav>` of `<a href>` links). Cross-document `<a href>` is the
        primary choice (representative + exercises observeWithRecovery); the 08-02 `<a data-spa>` +
        pushState SPA pattern is the sanctioned fallback if the live harness proves flaky.
      * **Auto-fire on load**: each route's inline script fires that route's `/api` batch on load
        (so a click-driven manual run and the autonomous walker both capture the surface).
      * **A form** on `/app/settings`: a real `<form>` with a `<select name="theme">` (or
        `<input>`) + a submit button that POSTs `/api/settings` (held write; the agent's `fill`
        exercises it).
      * **REST**: `GET /api/profile` (singleton), `GET /api/users` (list) + `GET /api/users/{id}`
        (detail → `/{id}` collapse), a related `GET /api/teams` (so `user.teamId` → Team
        relationship), `POST /api/users` (held create), `DELETE /api/users/{id}` (held delete),
        `POST|PUT /api/settings` (held).
      * **GraphQL**: `POST /graphql` — a `me` query passes (returns data), a `mutation` is held.
      * **JSON-RPC**: `POST /rpc` — a `get*`/`read*` method passes, a write method is held.
      * **Deterministic obviously-fake seed data**: `example.test` emails, `demo`-prefixed
        cookie/token values, fixed integer IDs + fixed ISO timestamps. NO real-looking secrets.
      * A `GET /__done__` end-of-run beacon (optional, mirrors the fixtures).
    - Write `examples/demo-app/launch.mjs`:
      `import { createServer } from './server.mjs'; createServer().listen(Number(process.env.PORT||<default>), '127.0.0.1', () => console.log('demo-app on http://127.0.0.1:'+<port>))`.
    - Write `examples/demo-app/README.md`: the route map + protocol surface table, the exact run
      command (`node examples/demo-app/launch.mjs`), a note that it is the source of the
      regenerated example specs (10-02), and that seed data is obviously-fake (no secrets).
    - Sanity-boot it locally (`PORT=4700 node examples/demo-app/launch.mjs` + `curl` a couple of
      routes) to confirm it answers before wiring the harness.
  </behavior>
  <action>Write the app + launcher + README; sanity-boot with curl.</action>
  <acceptance_criteria>
    - `examples/demo-app/server.mjs` is node:http zero-dep; navigation is real `<a href>` (verify:
      the served HTML contains `<a href=` nav links, not only `location.href`); multi-page; a form
      present; REST + `POST /graphql` + `POST /rpc` all served; seed data obviously-fake.
    - Booting the launcher and `curl`-ing `/app` returns HTML containing `<a href` nav links; the
      `/api` reads return JSON.
  </acceptance_criteria>
  <verify><automated>PORT=4700 node examples/demo-app/launch.mjs & sleep 1; curl -s http://127.0.0.1:4700/app | grep -c '<a href' ; curl -s http://127.0.0.1:4700/api/users | head -c 200; kill %1</automated></verify>
  <done>The canonical vision-drivable demo app exists and boots, with real clickable navigation.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Live drivability harness — real archeo explore proves steps>0 + multiple states + full protocol surface + floor-clean</name>
  <files>.planning/phases/10-vision-drivable-fixtures/10-01-live-verification/, .planning/phases/10-vision-drivable-fixtures/10-01-DRIVABILITY-VERIFICATION.md</files>
  <read_first>
    - src/cli/explore.ts (the explore run: floor ON, spec auto-gen on close, stop summary line)
    - .planning/phases/08-differential-validation/08-02-live-verification/apps/ledger-wrap.mjs
      (the node:http ledger monkeypatch → /__ledger__)
    - .planning/phases/08-differential-validation/08-02-live-verification/run-fallback.sh
      (the reproducible-harness shape: boot app → run real CLI → read artifacts → assert)
  </read_first>
  <behavior>
    - Build a reproducible harness under `10-01-live-verification/` (node built-ins, zero deps, NO
      src/ or test/ edits) that:
      1. Boots `examples/demo-app/` on a fixed free port (e.g. 4700, 127.0.0.1) WITH the
         `ledger-wrap` monkeypatch installed at the `node:http` layer (floor-proof ledger served
         at `/__ledger__`). Wait for it to answer (poll `GET /app`).
      2. Runs the REAL, UNMODIFIED CLI:
         `node src/cli/index.ts explore http://127.0.0.1:4700/app --i-have-authorization
         --model scripted --max-steps <n> --no-dashboard` — real headed Chromium, floor ON,
         scripted provider, in its own run root (cwd) so `.archeo/captures` resolves there.
      3. Locates the produced `archeo-spec.json` (+ the session's agent-step records / the stdout
         stop summary `exploration stopped: … (N steps, …)`), parses them, and ASSERTS:
         * **steps > 0** (the explore ran more than the initial observe — 03-04 gave 0);
         * **discovered states >= 2** (spec `flows`/`coverage` state count, or graph states — the
           app has multiple routes; 03-04 captured page-1 only);
         * **full protocol surface**: the spec `endpoints` include REST reads/writes AND
           `POST /graphql` AND `POST /rpc`; held writes carry `held:true`;
         * (secret-clean spot-check) the spec passes the strict grep gate (no bearer/cookie/JWT/
           email raw values) — redaction ran (CAP-05).
      4. Reads `GET /__ledger__` AFTER the run and asserts `mutations=0` and `destructiveHits=0`
         (floor held — the app's held writes never reached the backend).
    - Capture VERBATIM into `10-01-DRIVABILITY-VERIFICATION.md`: the exact commands, the stop
      summary (steps/states), the captured endpoint list (calling out `POST /graphql` +
      `POST /rpc` + the held writes), the floor ledger, the secret-clean grep result, and an
      explicit side-by-side note: "03-04 ORIGINAL under this same assertion → 0 steps, page-1 only
      (08-02 finding); demo-app → N steps, S states, full surface — FIX-01 drivability closed."
  </behavior>
  <action>Write + run the harness; collect the artifacts + transcript; write the drivability
    evidence into 10-01-DRIVABILITY-VERIFICATION.md.</action>
  <acceptance_criteria>
    - `archeo explore` ran via the real unmodified CLI against the live demo-app in real Chromium,
      floor ON, scripted provider.
    - Asserted from artifacts: steps > 0; states >= 2; endpoints include `POST /graphql` +
      `POST /rpc` + held writes (`held:true`); spec secret-clean.
    - `GET /__ledger__` after the run: mutations=0, destructiveHits=0.
    - The verification doc records the numbers + the 03-04-vs-demo-app side-by-side.
  </acceptance_criteria>
  <verify><automated>bash .planning/phases/10-vision-drivable-fixtures/10-01-live-verification/run-drivability.sh</automated></verify>
  <done>The demo app is proven vision-drivable by the real autonomous agent — the exact assertion
    03-04 failed now passes, floor clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Post-gate + SUMMARY + ROADMAP tick (10-01)</name>
  <files>.planning/phases/10-vision-drivable-fixtures/10-01-SUMMARY.md, .planning/phases/10-vision-drivable-fixtures/10-01-DRIVABILITY-VERIFICATION.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 10 Details + Progress row — the 2-plan wave list this plan set added)
  </read_first>
  <behavior>
    - Post-gate: `node --test 'test/**/*.test.ts'` green at 894 (app under examples/, harness under
      .planning/ → count unchanged); `npx tsc --noEmit` exit 0; LICENSE/NOTICE intact; no-network
      guard (GATE-03) green within the suite.
    - Finalize `10-01-DRIVABILITY-VERIFICATION.md` and write `10-01-SUMMARY.md` (what shipped: the
      demo app + launcher + README + the drivability harness; the numbers; deviations if the
      cross-document nav fell back to the SPA pattern).
    - **ROADMAP:** tick Phase 10 Wave 1 (`[x] 10-01`); Progress row → `0/2` stays (10-02 pending)
      but mark 10-01 done in the plan list. Do NOT flip the phase to Complete (10-02 closes it).
    - Note for 10-02: the exact `examples/demo-app/` route/endpoint set + the produced spec path,
      so 10-02 can generate the manual + autonomous specs and hand the spec to the builder.
  </behavior>
  <action>Run the post-gate; finalize the verification doc + SUMMARY; tick ROADMAP 10-01.</action>
  <acceptance_criteria>
    - Suite green at 894; tsc exit 0; LICENSE/NOTICE unchanged.
    - ROADMAP Phase 10 Wave 1 ticked; 10-01-SUMMARY.md + 10-01-DRIVABILITY-VERIFICATION.md present.
  </acceptance_criteria>
  <verify><automated>node --test 'test/**/*.test.ts' && npx tsc --noEmit</automated></verify>
  <done>Wave 1 closed: the canonical vision-drivable demo app is shipped and proven drivable; ready
    for 10-02 (spec generation, BUILD-01 re-proof, authentic compare, examples regeneration).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demo app is a fixture ↔ real secrets | Seed data is obviously-fake (`example.test`, `demo`-tokens, fixed IDs). No real secrets. Redaction (CAP-05) still runs; the produced spec is asserted secret-clean by the strict grep. |
| live exploration ↔ the floor | The explore run is floor-ON (no write-enabling flag). The app's held writes (REST mutations, GraphQL mutation, JSON-RPC write) must never reach the backend; the `/__ledger__` assertion proves mutations=0 / destructiveHits=0. |
| harness ↔ src/test | The harness lives under `.planning/` (node built-ins, zero deps) and touches no `src/`/`test/`. The shipped app under `examples/` is `node:http` only — no new runtime dep, no new outbound surface (GATE-03 holds). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-10-01 | Information disclosure (a real secret ships in the demo app / example spec) | examples/demo-app + the produced spec | mitigate | Seed data obviously-fake by construction; redaction runs; the harness asserts the produced spec passes the strict secret-clean grep. |
| T-10-02 | Tampering (a write reaches the live demo backend during explore) | the live explore run | mitigate | Floor ON (no `--allow-writes`); the app's `/__ledger__` asserted mutations=0 / destructiveHits=0 after the run. |
| T-10-03 | Repudiation (claiming drivability without proof) | the drivability assertion | mitigate | Assertions read from the produced `archeo-spec.json` + stop summary (machine-checkable): steps>0, states>=2, GraphQL+JSON-RPC endpoints present; recorded verbatim with the 03-04-vs-demo-app side-by-side. |
| T-10-04 | Denial of service (a fixture flake breaks the suite / typecheck) | baseline 894 + tsc | mitigate | The app + harness live outside `src/`/`test/`; suite count + `tsc --noEmit` exit 0 re-asserted as post-gate. |

No package installs; the harness is `.planning/`-only (node built-ins); the demo app is
`node:http` only; no new outbound surface; the no-network guard is re-run as part of the suite.
</threat_model>

<verification>
- Pre-gate + post-gate: `node --test 'test/**/*.test.ts'` green at 894; `npx tsc --noEmit` exit 0;
  LICENSE/NOTICE intact.
- `examples/demo-app/server.mjs` is node:http zero-dep with REAL `<a href>` multi-page nav, a form,
  and a full REST/GraphQL/JSON-RPC surface + held writes; obviously-fake seed data.
- The real, unmodified `archeo explore` drove it in real Chromium (floor ON, scripted): steps > 0,
  states >= 2, `POST /graphql` + `POST /rpc` + held writes captured, spec secret-clean.
- Floor proof: `/__ledger__` shows mutations=0 / destructiveHits=0 after the run.
- ROADMAP Phase 10 Wave 1 ticked; verification doc + SUMMARY present.
</verification>

<success_criteria>
- The canonical vision-drivable demo app is shipped at `examples/demo-app/` (real `<a href>` nav,
  multi-page, form, full protocol surface, obviously-fake seed data, node:http zero-dep).
- The real autonomous agent drives it — steps > 0 and multiple states — the exact thing the 03-04
  fixture failed (0 steps), proven live with a clean floor ledger.
- The suite (894) and `tsc --noEmit` (exit 0) stay green; no `src/`/`test/` change.
</success_criteria>

<output>
Create `.planning/phases/10-vision-drivable-fixtures/10-01-SUMMARY.md` and
`.planning/phases/10-vision-drivable-fixtures/10-01-DRIVABILITY-VERIFICATION.md` when done, and tick
ROADMAP Phase 10 Wave 1. Commit style: `feat(10-01): …` for the demo app + launcher + harness;
`docs(10-01): …` for the verification doc + SUMMARY + ROADMAP tick — each with the
`Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` trailer.
</output>
