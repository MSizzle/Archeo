---
phase: 10-vision-drivable-fixtures
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
autonomous: true
requirements: [FIX-01]
files_modified:
  - examples/demo-app/rebuild/server.js
  - examples/demo-app/rebuild/package.json
  - examples/demo-app/rebuild/README.md
  - examples/manual-capture-demo-app/archeo-spec.json
  - examples/manual-capture-demo-app/README.md
  - examples/autonomous-explore-demo-app/archeo-spec.json
  - examples/autonomous-explore-demo-app/README.md
  - examples/compare-demo-app/compare-report.json
  - examples/compare-demo-app/self-compare-report.json
  - examples/compare-demo-app/README.md
  - examples/README.md
  - .planning/phases/10-vision-drivable-fixtures/10-02-live-verification/
  - .planning/phases/10-vision-drivable-fixtures/10-02-DOGFOOD-VERIFICATION.md
  - .planning/phases/10-vision-drivable-fixtures/10-02-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md

must_haves:
  truths:
    - "A REAL archeo-spec.json is generated from examples/demo-app/ two ways through the REAL, UNMODIFIED CLI: (a) autonomous — node src/cli/index.ts explore http://127.0.0.1:<PORT>/app --i-have-authorization --model scripted --max-steps <n>; (b) manual — node src/cli/index.ts http://127.0.0.1:<PORT>/app --i-have-authorization driven by a small Playwright click-driver that clicks the app's real <a href> links (each page auto-fires its /api batch → full manual capture), then closes the context → spec auto-generated on close"
    - "BUILD-01 RE-PROOF (spec-only isolation, the 03-04 method): the generated archeo-spec.json ALONE (no target source, no repo, no network) is handed to a FRESH spec-only builder agent; the builder produces a runnable rebuild committed at examples/demo-app/rebuild/ (node:http, zero deps, CommonJS package.json shim, self-listens on PORT) that boots and answers"
    - "The orchestrator spawns the isolated builder; this plan DEFINES the builder's inputs (the spec JSON only), outputs (a runnable server.js + package.json + README), and acceptance (boots on PORT; serves the spec's endpoint set; a spec-vs-rebuild endpoint/model score recorded) — mirroring 03-04's three-stage isolation"
    - "AUTHENTIC COMPARE: node src/cli/index.ts compare http://127.0.0.1:<orig> http://127.0.0.1:<rebuild> --i-have-authorization --model scripted is run through the REAL CLI in real headed Chromium, floor ON BOTH, producing a divergence report whose findings are WHATEVER the builder genuinely got right/wrong (discovered, not injected) — read from compare-report.json (machine-checkable), carrying the honest determinism caveat"
    - "SELF-COMPARE CONTROL: comparing the ORIGINAL against a SECOND INSTANCE of itself (two ports, same examples/demo-app code) yields a FULLY-EMPTY backend-contract divergence report (newEndpoints/removedEndpoints/changedShapes/heldStatusChanges all 0) — proving the comparison is not spuriously noisy and that the authentic-pair findings are trustworthy signal"
    - "FLOOR PROOF: across ALL compare + generation runs, each target's own ground-truth ledger (ledger-wrap, .planning/-only) shows mutations=0 and destructiveHits=0 reached the backend (floor ON everywhere; no write-enabling flag)"
    - "examples/ REGENERATED + provenance: examples/manual-capture-demo-app/archeo-spec.json and examples/autonomous-explore-demo-app/archeo-spec.json are REPLACED with the specs generated against examples/demo-app/; both READMEs re-labeled to cite examples/demo-app/ as the source app (retiring the old 03-04/05-05 fixture provenance); a new examples/compare-demo-app/ carries compare-report.json + self-compare-report.json + a provenance README; examples/README.md rewritten to describe the one demo app + its three regenerated artifacts"
    - "SECRET-CLEAN: every regenerated spec + both compare reports re-pass the strict secret-clean grep gate (no authorization/bearer/cookie/sk-ant-/JWT/raw-email hits) — redaction (CAP-05) ran on every capture"
    - "Full suite green as pre- AND post-gate: baseline 894 (893 pass + 1 documented skip, 0 fail); npx tsc --noEmit exits 0 (QUAL-02 guard). No src/ or test/ file is touched (examples/ + .planning/ only). LICENSE/NOTICE intact"
    - "PHASE CLOSE: ROADMAP Phase 10 → 2/2 Complete (dated 2026-07-04) + Progress row 2/2 Complete; REQUIREMENTS FIX-01 → Complete (checklist + traceability table); STATE → milestone v1.1 executing, current focus Phase 11 (Spec-quality Enrichment), completed_phases +1"
  artifacts:
    - path: "examples/demo-app/rebuild/"
      provides: "the spec-only AUTHENTIC rebuild of examples/demo-app (BUILD-01 re-proof): a runnable node:http server.js built from the spec ALONE + CommonJS package.json shim + provenance README"
      exports: []
    - path: "examples/manual-capture-demo-app/archeo-spec.json + README.md"
      provides: "the regenerated MANUAL-mode spec (real archeo <url> vs examples/demo-app) + re-labeled provenance"
      exports: []
    - path: "examples/autonomous-explore-demo-app/archeo-spec.json + README.md"
      provides: "the regenerated AUTONOMOUS-mode spec (real archeo explore vs examples/demo-app) + re-labeled provenance"
      exports: []
    - path: "examples/compare-demo-app/"
      provides: "the authentic original-vs-rebuild compare-report.json + the clean self-compare-report.json + a provenance README with the determinism caveat"
      exports: []
    - path: "examples/README.md"
      provides: "rewritten top-level examples guide: one demo app (examples/demo-app/) + three regenerated artifacts (manual spec, autonomous spec, authentic compare)"
      exports: []
    - path: ".planning/phases/10-vision-drivable-fixtures/10-02-DOGFOOD-VERIFICATION.md"
      provides: "the evidence: the exact commands (manual + autonomous generation, builder spec-only isolation, authentic compare + self-compare), the BUILD-01 score, the compare-report.json findings mapped to what the builder got right/wrong, the self-compare empty control, the floor ledgers, and the PASS verdict"
      exports: []
  key_links:
    - from: "the spec generated from examples/demo-app/"
      to: "the fresh spec-only builder → examples/demo-app/rebuild/"
      via: "hand archeo-spec.json ONLY (no source/repo/network) to an isolated builder agent → runnable server.js (03-04 BUILD-01 method)"
      pattern: "archeo-spec.json|rebuild/server.js|spec-only|BUILD-01"
    - from: "the authentic original + rebuild pair"
      to: "archeo compare (src/cli/compare.ts) → examples/compare-demo-app/compare-report.json"
      via: "node src/cli/index.ts compare <origUrl> <rebuildUrl> --i-have-authorization --model scripted → diffSpecs → compare-report.json + self-compare control"
      pattern: "archeo compare|compare-report.json|self-compare|diffSpecs"
    - from: "FIX-01"
      to: "ROADMAP Phase 10 Complete + REQUIREMENTS FIX-01 Complete + STATE → Phase 11"
      via: "phase-close bookkeeping after the authentic dogfood + examples regeneration pass"
      pattern: "FIX-01|Phase 10|Complete|Phase 11"
---

<objective>
Prove the WHOLE arc authentically on the vision-drivable demo app, then regenerate the shipped
examples and close Phase 10. (a) Generate a REAL spec from `examples/demo-app/` both ways — a real
`archeo explore` autonomous run and a real `archeo <url>` manual run (click-driven over the app's
real `<a href>` links). (b) **Re-prove BUILD-01** by handing that spec — and ONLY that spec — to a
fresh, spec-only builder agent (the 03-04 isolation) → a runnable rebuild committed at
`examples/demo-app/rebuild/`. (c) Run `archeo compare original rebuild` on THIS **authentic** pair
through the real CLI (floor ON both), with a **fully-empty self-compare control** proving the
comparison is trustworthy. (d) Regenerate `examples/` (manual spec + autonomous spec + the authentic
compare) with provenance, retiring the old fixture-derived examples. Every run floor-ON, every spec
secret-clean, the suite (894) and `tsc --noEmit` (exit 0) green. Then close the phase: ROADMAP
Phase 10 → 2/2 Complete; REQUIREMENTS FIX-01 → Complete; STATE → Phase 11.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-vision-drivable-fixtures/10-CONTEXT.md
@.planning/phases/10-vision-drivable-fixtures/10-01-SUMMARY.md
@src/cli/index.ts
@src/cli/compare.ts
@src/cli/explore.ts
@src/spec/drift.ts

<method_precedents>
- **BUILD-01 spec-only isolation (03-04):** the builder agent received the `archeo-spec.json` ONLY
  — no target source, no repo, no network — and produced `rebuild/server.js` (node:http, zero
  deps, CommonJS `package.json` shim). Scored vs a private ground-truth. Re-use this exact
  three-stage isolation. See `.planning/phases/03-spec-generator-buildability/03-04-buildability/`
  and its rebuild at `.../rebuild/server.js`.
- **archeo compare (08-01/08-02):** `compare <urlA> <urlB>` spawns an isolated `archeo explore`
  child per target (own run root), diffs the two specs with `diffSpecs`, writes `compare-report.json`
  with the determinism caveat. Same-hostname (localhost:portA vs :portB) targets are isolated by
  distinct run roots. Floor ON both; no `--allow-writes` registered.
- **Floor-proof ledger (08-02):** `apps/ledger-wrap.mjs` tees a `node:http` ledger at `/__ledger__`
  — reused (in the harness, `.planning/`-only) to prove mutations=0 / destructiveHits=0.
- **Manual capture (03-04):** `archeo <url>` opens the browser + floor + auto-generates the spec on
  window/context close. A small Playwright click-driver clicks the app's real `<a href>` links
  (each route auto-fires its `/api` batch → full capture), then closes the context → spec written.
  This driver is trivial PRECISELY because the demo app's affordances are real (the FIX-01 fix).
</method_precedents>

<call_beyond_brief>
**D10-04 (the shape decision):** ONE authored original (`examples/demo-app/`) + a SEPARATELY-built
spec-only rebuild — NOT one app playing both roles, and NOT the 08-02 injected-drift twin
(`makeApp({variant})`). The rebuild's divergences are the builder's genuine, discovered
right/wrong — the honest, stronger example. The self-compare control (original vs original-clone →
empty) is the deterministic trust check. If the builder's rebuild is *too* faithful to yield any
interesting compare signal, note that outcome honestly (a faithful rebuild is a GOOD BUILD-01
result); the authentic divergence is expected to be the star (03-04's builder already added an
endpoint + guessed shapes).
</call_beyond_brief>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pre-gate (894 + tsc clean) + generate the real spec (autonomous + manual)</name>
  <files>.planning/phases/10-vision-drivable-fixtures/10-02-live-verification/, examples/manual-capture-demo-app/archeo-spec.json, examples/autonomous-explore-demo-app/archeo-spec.json</files>
  <read_first>
    - 10-01-SUMMARY.md (the demo app's route/endpoint set + the produced spec path)
    - src/cli/explore.ts (autonomous run + spec auto-gen) and src/cli/browser.ts (manual openAndWait)
  </read_first>
  <behavior>
    - Pre-gate: `node --test 'test/**/*.test.ts'` green at 894; `npx tsc --noEmit` exit 0;
      LICENSE/NOTICE intact.
    - Boot `examples/demo-app/` (with the ledger-wrap floor-proof shim) on a fixed port.
    - **Autonomous generation:** run the real `node src/cli/index.ts explore
      http://127.0.0.1:<PORT>/app --i-have-authorization --model scripted --max-steps <n>
      --no-dashboard` → capture the produced `archeo-spec.json`.
    - **Manual generation:** run the real `node src/cli/index.ts http://127.0.0.1:<PORT>/app
      --i-have-authorization` and drive it with a small Playwright click-driver (harness,
      `.planning/`-only) that clicks each real `<a href>` nav link in turn (each route auto-fires
      its `/api` batch), then closes the context → spec auto-generated on close. Capture that
      `archeo-spec.json`.
    - Stage both produced specs for the example dirs (they REPLACE the old ones in Task 4). Assert
      each spec has all 6 ArcheoSpec keys, includes `POST /graphql` + `POST /rpc` + held writes,
      and passes the strict secret-clean grep.
    - Read `/__ledger__` after both runs: assert mutations=0 / destructiveHits=0.
  </behavior>
  <action>Pre-gate; generate the autonomous + manual specs via the real CLI; stage them; assert
    surface + secret-clean + floor.</action>
  <acceptance_criteria>
    - Suite 894 green; tsc exit 0.
    - Two real specs produced (autonomous + manual), each with the full protocol surface + held
      writes, each secret-clean; floor ledgers mutations=0 / destructiveHits=0.
  </acceptance_criteria>
  <verify><automated>node -e "for (const p of [process.env.AUTO_SPEC, process.env.MANUAL_SPEC]){const s=require(p); const ks=['meta','dataModels','endpoints','flows','rules','coverage']; for(const k of ks) if(!(k in s)) throw new Error('missing '+k+' in '+p); console.log(p, 'endpoints', s.endpoints.length)}"</automated></verify>
  <done>Real specs exist from both paths against the drivable demo app — the input to BUILD-01 and
    the regenerated examples.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: BUILD-01 re-proof — spec-only builder → examples/demo-app/rebuild/</name>
  <files>examples/demo-app/rebuild/server.js, examples/demo-app/rebuild/package.json, examples/demo-app/rebuild/README.md, .planning/phases/10-vision-drivable-fixtures/10-02-live-verification/, .planning/phases/10-vision-drivable-fixtures/10-02-DOGFOOD-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/03-spec-generator-buildability/03-04-buildability/rebuild/server.js
      (the target rebuild shape: node:http, zero deps, CommonJS shim, self-listens on PORT)
    - the produced spec from Task 1 (the builder's SOLE input)
  </read_first>
  <behavior>
    - Re-run the 03-04 BUILD-01 method with fresh isolation: the orchestrator spawns a FRESH
      builder agent whose ONLY input is the generated `archeo-spec.json` (autonomous spec preferred
      — it is the FIX-01 star; the manual spec may be provided as the same app's alternate view if
      the builder benefits). The builder gets **no** target source, **no** repo access, **no**
      network — it must reconstruct from the spec alone.
    - Builder OUTPUT (committed at `examples/demo-app/rebuild/`): `server.js` (plain `node:http`,
      zero deps, self-listens on `PORT`, serves the spec's endpoint set incl. `/app*` HTML pages,
      REST, `POST /graphql`, `POST /rpc`), a CommonJS `package.json` shim, and a `README.md`
      recording the spec-only isolation + generating provenance.
    - ACCEPTANCE (recorded in the verification doc): the rebuild boots (`PORT=<n> node
      examples/demo-app/rebuild/server.js`) and answers; a spec-vs-rebuild score is recorded
      (endpoint-path coverage, data-model field coverage, held-write handling) mirroring the 03-04
      scoring — BUILD-01 re-proven on a vision-drivable app.
    - The builder's genuine divergences from the original (added/omitted endpoints, guessed shapes)
      are NOT corrected — they are the authentic compare signal for Task 3. Record them.
  </behavior>
  <action>Spawn the isolated spec-only builder; commit examples/demo-app/rebuild/; boot + score it;
    record the BUILD-01 evidence.</action>
  <acceptance_criteria>
    - `examples/demo-app/rebuild/server.js` exists, is node:http zero-dep, boots on PORT, and
      serves the spec's endpoint set.
    - A BUILD-01 spec-vs-rebuild score is recorded; the builder's spec-only isolation is documented.
  </acceptance_criteria>
  <verify><automated>PORT=4810 node examples/demo-app/rebuild/server.js & sleep 1; curl -s http://127.0.0.1:4810/api/users | head -c 200; curl -s -X POST http://127.0.0.1:4810/graphql -d '{"query":"query Me { me { id } }"}' | head -c 200; kill %1</automated></verify>
  <done>BUILD-01 re-proven on the vision-drivable app: a fresh agent rebuilt it from the spec alone.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Authentic archeo compare (original vs rebuild) + fully-empty self-compare control + floor proof</name>
  <files>.planning/phases/10-vision-drivable-fixtures/10-02-live-verification/, examples/compare-demo-app/compare-report.json, examples/compare-demo-app/self-compare-report.json, .planning/phases/10-vision-drivable-fixtures/10-02-DOGFOOD-VERIFICATION.md</files>
  <read_first>
    - src/cli/compare.ts (how compare spawns per-target explore + writes compare-report.json + the caveat)
    - .planning/phases/08-differential-validation/08-02-DOGFOOD-VERIFICATION.md (the MATCH/FLAG + self-compare + floor-proof evidence shape)
  </read_first>
  <behavior>
    - Stand up the ORIGINAL (`examples/demo-app/`) on port A, the REBUILD
      (`examples/demo-app/rebuild/`) on port B, and a SECOND instance of the ORIGINAL on port C
      (all 127.0.0.1, all with ledger-wrap). Wait for all to answer.
    - **Authentic compare:** `node src/cli/index.ts compare http://127.0.0.1:<A>/app
      http://127.0.0.1:<B>/app --i-have-authorization --model scripted --max-steps <n>` — real
      Chromium, floor ON both. Locate the produced `compare-report.json`; parse it; record its
      divergence findings (whatever the builder genuinely got right/wrong) mapped to the actual
      builder behavior. Honor the determinism caveat (label any page/flow entry as weak,
      frontier-dependent signal, not a backend-contract divergence). Copy this report to
      `examples/compare-demo-app/compare-report.json`.
    - **Self-compare control:** `node src/cli/index.ts compare http://127.0.0.1:<A>/app
      http://127.0.0.1:<C>/app …` (same code, two ports). Assert the backend-contract divergence is
      FULLY EMPTY (newEndpoints/removedEndpoints/changedShapes/heldStatusChanges all 0). Copy to
      `examples/compare-demo-app/self-compare-report.json`.
    - **Floor proof:** after ALL runs (Task 1 + Task 3), read each target's `/__ledger__` and
      assert mutations=0 / destructiveHits=0.
    - Write `examples/compare-demo-app/README.md`: the authentic-dogfood provenance (the exact
      commands, A=original / B=rebuild framing), the honest determinism caveat, and the self-compare
      control result.
  </behavior>
  <action>Run the authentic compare + self-compare; copy both reports into examples/compare-demo-app/;
    collect ledgers; write the compare evidence + provenance README.</action>
  <acceptance_criteria>
    - `archeo compare` ran via the real CLI (floor ON both) on the AUTHENTIC original+rebuild pair;
      compare-report.json exists and carries the caveat.
    - Self-compare control: backend-contract divergence entries = 0 (fully empty).
    - Both target ledgers mutations=0 / destructiveHits=0 across all runs.
    - Both reports are secret-clean and copied into examples/compare-demo-app/.
  </acceptance_criteria>
  <verify><automated>node -e "const r=require(process.env.SELF_COMPARE_REPORT).report; const bad=r.newEndpoints.length+r.removedEndpoints.length+r.changedShapes.length+r.heldStatusChanges.length; if(bad!==0) throw new Error('self-compare not empty: '+bad); console.log('self-compare backend-contract entries:', bad); const a=require(process.env.COMPARE_REPORT).report; console.log('authentic new/removed/changed/held:', a.newEndpoints.length, a.removedEndpoints.length, a.changedShapes.length, a.heldStatusChanges.length)"</automated></verify>
  <done>The authentic capture→spec→rebuild→compare loop is proven on a vision-drivable app, with a
    trustworthy (fully-empty) self-compare control and a clean floor.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Regenerate examples/ + provenance; retire old fixture provenance</name>
  <files>examples/manual-capture-demo-app/archeo-spec.json, examples/manual-capture-demo-app/README.md, examples/autonomous-explore-demo-app/archeo-spec.json, examples/autonomous-explore-demo-app/README.md, examples/compare-demo-app/README.md, examples/README.md</files>
  <read_first>
    - examples/README.md + examples/manual-capture-demo-app/README.md + examples/autonomous-explore-demo-app/README.md (the current provenance to replace)
  </read_first>
  <behavior>
    - REPLACE `examples/manual-capture-demo-app/archeo-spec.json` with the Task-1 manual spec and
      `examples/autonomous-explore-demo-app/archeo-spec.json` with the Task-1 autonomous spec (both
      generated against `examples/demo-app/`).
    - Rewrite both READMEs: source app = `examples/demo-app/` (retire the 03-04/05-05 fixture
      provenance); the exact generating command; the run date (2026-07-04); a secret-clean
      confirmation; and an updated "what the spec shows" block matching the NEW spec's actual
      counts (endpoints / models / states / held writes — derived from the regenerated bytes, not
      guessed).
    - Rewrite `examples/README.md`: one canonical demo app (`examples/demo-app/`) + three
      regenerated artifacts (manual spec, autonomous spec, authentic compare in
      `examples/compare-demo-app/`); update the examples table; keep the secret-clean grep gate
      section and re-run it over all of `examples/`.
    - Re-run the strict secret-clean grep over the WHOLE `examples/` tree; assert zero real-secret
      hits (documentation prose exceptions adjudicated as in 07-02/07-03).
  </behavior>
  <action>Regenerate the two example specs + all provenance READMEs; rewrite examples/README.md;
    re-run the secret-clean gate over examples/.</action>
  <acceptance_criteria>
    - Both example specs are the freshly-generated bytes against examples/demo-app/; both READMEs
      cite examples/demo-app/ as the source (old fixture provenance retired).
    - examples/README.md describes the one demo app + three regenerated artifacts.
    - The strict secret-clean grep over examples/ has no real-secret hits.
  </acceptance_criteria>
  <verify><automated>grep -rniE "authorization|bearer|cookie|set-cookie|sk-ant-|sk-[A-Za-z0-9]{20}|eyJ[A-Za-z0-9_-]{10,}" examples/ | grep -viE "README|\.md:" || echo "secret-clean: no raw-value hits in spec/report JSON"</automated></verify>
  <done>The shipped examples reflect REAL runs against the vision-drivable demo app, with accurate
    provenance and a clean secret gate.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Post-gate + PHASE CLOSE (ROADMAP / REQUIREMENTS / STATE)</name>
  <files>.planning/phases/10-vision-drivable-fixtures/10-02-DOGFOOD-VERIFICATION.md, .planning/phases/10-vision-drivable-fixtures/10-02-SUMMARY.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 10 Details + Progress row; the v1.1 Progress table)
    - .planning/REQUIREMENTS.md (FIX-01 checklist row + traceability table)
    - .planning/STATE.md (progress block; the Phase 9 close format)
  </read_first>
  <behavior>
    - Post-gate: `node --test 'test/**/*.test.ts'` green at 894 (examples/ + .planning/ only → count
      unchanged); `npx tsc --noEmit` exit 0; LICENSE/NOTICE intact; no-network guard (GATE-03) green.
    - Finalize `10-02-DOGFOOD-VERIFICATION.md`: the commands (manual + autonomous generation,
      builder spec-only isolation + score, authentic compare + self-compare + floor ledgers), the
      compare findings mapped to the builder's genuine behavior, and the PASS verdict. Write
      `10-02-SUMMARY.md`.
    - **ROADMAP:** Phase 10 → `[x]` in the v1.1 Phases list; Wave 2 (`[x] 10-02`) ticked; Phase 10
      Progress row → `2/2 | Complete | 2026-07-04`; update the v1.1 status line (current focus →
      Phase 11).
    - **REQUIREMENTS:** FIX-01 → checked in the v1.1 checklist AND flipped in the traceability table
      (`FIX-01 | Phase 10 | Complete`); update the v1.1 requirement tally.
    - **STATE:** Phase 10 CLOSED; `completed_phases +1`; `stopped_at` / current-focus →
      Phase 11 (Spec-quality Enrichment); record the Phase 10 execution decisions in Accumulated
      Context (the demo app location, the D10-04 authentic-rebuild shape, the BUILD-01 re-proof
      result, the compare findings, the examples regeneration).
  </behavior>
  <action>Run the post-gate; finalize the verification doc + SUMMARY; flip ROADMAP + REQUIREMENTS +
    STATE for phase close; commit the bookkeeping.</action>
  <acceptance_criteria>
    - Suite green at 894; tsc exit 0; LICENSE/NOTICE unchanged.
    - ROADMAP Phase 10 = 2/2 Complete; REQUIREMENTS FIX-01 Complete (checklist + traceability);
      STATE → Phase 11, completed_phases incremented.
    - 10-02-DOGFOOD-VERIFICATION.md + 10-02-SUMMARY.md present with the PASS verdict.
  </acceptance_criteria>
  <verify><automated>node --test 'test/**/*.test.ts' && npx tsc --noEmit && grep -nE "FIX-01" .planning/REQUIREMENTS.md</automated></verify>
  <done>Phase 10 is closed with authentic dogfood evidence; FIX-01 Complete; the examples reflect
    real runs against a vision-drivable app; milestone v1.1 advances to Phase 11.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec-only builder ↔ target knowledge | The rebuild is built from the spec ALONE — no target source, repo, or network (the 03-04 isolation). This keeps BUILD-01 honest: the spec, not hidden knowledge, must carry the app. |
| authentic compare ↔ trust | A false positive on the faithfully-rebuilt surface, or a noisy self-compare, would erode trust. The FULLY-EMPTY self-compare control (identical apps, two ports) + the determinism caveat separate real backend-contract divergence from frontier noise. |
| live exploration ↔ the floor | Manual, autonomous, and both compare explorations run floor-ON (no write-enabling flag). The `/__ledger__` assertions prove mutations=0 / destructiveHits=0 across all runs. |
| examples ship secrets ↔ redaction | Seed data is obviously-fake; redaction (CAP-05) runs on every capture; every regenerated spec + both compare reports re-pass the strict secret-clean grep. |
| two-target isolation ↔ cross-contamination | original/rebuild/original-clone are all localhost (ports differ); compare's distinct isolated run roots keep the capture stores + per-hostname profiles separate; the self-compare control would EXPOSE bleed as spurious divergence and asserts fully-empty. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-10-05 | Spoofing (builder secretly reads the target instead of the spec) | the spec-only builder | mitigate | Fresh isolated agent; sole input is the spec JSON; no source/repo/network — the 03-04 three-stage isolation, recorded in the verification doc. |
| T-10-06 | Tampering (a write reaches a live target during compare/generation) | the live explorations | mitigate | Floor ON everywhere (no `--allow-writes`); all target `/__ledger__` asserted mutations=0 / destructiveHits=0 after all runs. |
| T-10-07 | Repudiation (claimed compare findings that aren't real / missed real ones) | compare-report.json findings | mitigate | Findings read from compare-report.json (machine-checkable), mapped to the builder's genuine behavior; the fully-empty self-compare control bounds spurious noise; the caveat labels frontier noise. |
| T-10-08 | Information disclosure (a real secret ships in a regenerated example) | examples/ regenerated specs + reports | mitigate | Obviously-fake seed data; redaction runs; the strict secret-clean grep re-run over the whole examples/ tree. |
| T-10-09 | Denial of service (a fixture/example flake breaks the suite/typecheck) | baseline 894 + tsc | mitigate | All artifacts live under examples/ + .planning/; suite count + `tsc --noEmit` exit 0 re-asserted as post-gate. |

No package installs; the harness is `.planning/`-only (node built-ins); the demo app + rebuild are
`node:http` only; no new outbound surface; the no-network guard is re-run as part of the suite.
</threat_model>

<verification>
- Pre-gate + post-gate: `node --test 'test/**/*.test.ts'` green at 894; `npx tsc --noEmit` exit 0;
  LICENSE/NOTICE intact.
- A real spec generated from examples/demo-app/ both autonomously (archeo explore) and manually
  (archeo <url> click-driven) — each with the full protocol surface, each secret-clean.
- BUILD-01 re-proven: a fresh spec-only builder produced examples/demo-app/rebuild/ (boots on PORT,
  serves the spec's endpoint set); a spec-vs-rebuild score recorded.
- Authentic `archeo compare` (floor ON both) produced compare-report.json with discovered findings
  + the caveat; the self-compare control is fully empty; both ledgers mutations=0 / destructiveHits=0.
- examples/ regenerated (manual spec, autonomous spec, authentic compare) with re-labeled
  provenance; the secret-clean gate over examples/ has no real-secret hits.
- ROADMAP Phase 10 2/2 Complete; REQUIREMENTS FIX-01 Complete; STATE → Phase 11.
</verification>

<success_criteria>
- FIX-01 closed: the whole capture→spec→rebuild→compare arc is proven AUTHENTICALLY on a
  vision-drivable app — a stronger example than v1.0's, re-proving BUILD-01.
- The shipped examples reflect REAL runs against examples/demo-app/, with accurate provenance and a
  clean secret gate; the injected-drift twin is retired in favour of the authentic rebuild.
- The suite (894) and `tsc --noEmit` (exit 0) stay green; no src/ or test/ change; floor held
  across every run; milestone v1.1 advances to Phase 11.
</success_criteria>

<output>
Create `.planning/phases/10-vision-drivable-fixtures/10-02-SUMMARY.md` and
`.planning/phases/10-vision-drivable-fixtures/10-02-DOGFOOD-VERIFICATION.md` when done. This plan
CLOSES Phase 10: update ROADMAP (Phase 10 → 2/2 Complete) + REQUIREMENTS (FIX-01 → Complete) +
STATE (→ Phase 11). Commit style: `feat(10-02): …` for examples/demo-app/rebuild/ + the regenerated
example specs; `docs(10-02): …` for provenance READMEs, verification doc, SUMMARY, and
ROADMAP/REQUIREMENTS/STATE bookkeeping — each with the
`Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` trailer.
</output>
