---
phase: 11-spec-quality-enrichment
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/types/index.ts
  - src/types/spec.ts
  - src/capture/interceptor.ts
  - src/spec/templater.ts
  - src/spec/generator.ts
  - test/capture/interceptor.test.ts
  - test/spec/templater.test.ts
  - test/spec/generator.test.ts
autonomous: true
requirements: [SPEC-09]

must_haves:
  truths:
    - "A GraphQL endpoint in the spec carries a graphqlSchema fragment with operationType, argument NAMES, selection field NAMES, and a VALUE-STRIPPED query string"
    - "A secret planted in a GraphQL variable value AND in an inline argument literal never appears in the stored graphqlSchema, while the argument/field NAMES survive"
    - "The redacted requestBody still strips the planted variable value (redact call ordering unchanged — CAP-05 fail-closed)"
    - "Each endpoint carries bodyEncoding derived from content-type (json/form/text/binary); a JSON body is distinguishable from a plain string"
    - "Polling endpoints carry a pollingIntervalMs when timestamps allow, not just a uniform polling:true"
  artifacts:
    - path: "src/capture/interceptor.ts"
      provides: "extractGraphQLSchemaFragment — pre-redaction schema-level parse (arg/field names + value-stripped query), set on the record"
      exports: ["extractGraphQLSchemaFragment", "handleRoute", "attachInterceptor"]
    - path: "src/spec/templater.ts"
      provides: "groupRecords surfaces graphqlSchema + bodyEncoding + pollingIntervalMs onto EndpointTemplate"
      exports: ["groupRecords"]
    - path: "src/types/index.ts"
      provides: "CaptureRecord.graphqlSchema (schema-level identifier class, parallels graphqlOperationName)"
  key_links:
    - from: "src/capture/interceptor.ts"
      to: "src/spec/templater.ts"
      via: "record.graphqlSchema feeds the endpoint template's graphqlSchema fragment"
      pattern: "graphqlSchema"
---

<objective>
Close SPEC-09 (GraphQL schema depth, builder finding #7) and fold in the request-body-representation
clarity items #1 (bodyEncoding) and #6 (polling interval). Today a GraphQL endpoint's request shape is
just `"string"`; a builder cannot see the operation's arguments or selected fields. Capture the
operation's SHAPE — argument names, selection field names, operation type, and a value-stripped query
string — PRE-redaction, following the exact `graphqlOperationName` parse-before-redact discipline
(03-05), and surface it as a per-operation `graphqlSchema` fragment on the endpoint.

This plan crosses the CAP-05 pre-redaction boundary (the ONLY plan in Phase 11 that does). The
safety-critical deliverable is a regression test proving a planted secret in a GraphQL variable value
AND an inline argument literal is still stripped, while the query STRUCTURE survives. TDD.

Output: `graphqlSchema` fragments on GraphQL endpoints; `bodyEncoding` + `pollingIntervalMs` on all
endpoints; CAP-05 boundary re-asserted loudly. Wave 2 of Phase 11.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-spec-quality-enrichment/11-CONTEXT.md
@.planning/phases/03-spec-generator-buildability/03-05-PLAN.md
@src/capture/interceptor.ts
@src/capture/classifier.ts
@src/capture/redactor.ts
@src/spec/templater.ts
@src/spec/generator.ts
@src/types/index.ts
@src/types/spec.ts

<interfaces>
# extractGraphQLIdentifier (interceptor.ts) is the precedent: it JSON.parses the raw postData,
#   reads parsed.query (a string), strips # comments, extracts the schema-level identifier —
#   ALL before redactBody runs on the body. graphqlOperationName is set on the record; the body
#   is still fully redacted before store.append (CAP-05 fail-closed). extractGraphQLSchemaFragment
#   mirrors this exactly: same parse site (all three call sites — held path, allowWrites path,
#   allowed read path), same "read shape, redact values" discipline.
#
# CAP-05 leak vectors to defeat in the query string:
#   - inline literal args:  mutation { updateProfile(email: "planted@secret.test", token: "SEKRET") }
#   - the variables object: {"query":"...", "variables":{"password":"SEKRET"}}
# The fragment stores arg NAMES (email, token, password-as-$var-name) + a value-stripped query
#   (inline literals → a placeholder). variables stays in the body and is redacted by redactBody.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: extractGraphQLSchemaFragment — pure, pre-redaction, value-stripping (SPEC-09 core)</name>
  <files>src/capture/interceptor.ts, test/capture/interceptor.test.ts, src/types/index.ts</files>
  <read_first>
    - src/capture/interceptor.ts (extractGraphQLIdentifier — the parse-before-redact precedent + its 3 call sites)
    - src/capture/classifier.ts (stripGraphQLComments / detectGraphQLOperation — reuse operation-type detection, do not duplicate)
  </read_first>
  <behavior>
    - extractGraphQLSchemaFragment(body: string|null): GraphQLSchemaFragment | undefined
    - Parses parsed.query; returns undefined for non-GraphQL bodies. Strips # comments first (reuse the CR-03 pattern).
    - operationType: reuse detectGraphQLOperation → 'query'|'mutation'|'introspection'; 'subscription' recognized as its own type.
    - operationName: reuse the existing identifier extraction (named op or first selection field).
    - arguments: TOP-LEVEL argument NAMES only — the identifiers before ':' inside (...) — NEVER the values.
    - fields: selection-set field NAMES (nested flattened, e.g. 'user.email'); reasonable depth cap.
    - query: the query text with inline argument literal VALUES stripped — every `name: <literal>` becomes `name: <redacted>` (string/number/enum/list literals). $variable references keep the $name (they are identifiers, not values). Whitespace normalized.
    - CAP-05: this is a SHAPE read — no value ever enters the fragment. Purely a function of the query STRING; variables are NOT read here (they are redacted in the body).
    - CaptureRecord gains graphqlSchema?: GraphQLSchemaFragment (comment: schema-level identifier class, parallels graphqlOperationName/rpcMethod — never a secret).
  </behavior>
  <action>
    Implement extractGraphQLSchemaFragment as a pure exported function beside extractGraphQLIdentifier.
    A tolerant tokenizer/regex over the stripped query: identify (...) argument blocks and replace
    literal values; collect arg names + selection field names. Do NOT introduce a GraphQL dependency
    (zero-dep). Add graphqlSchema to CaptureRecord.
    RED test first: assert the fragment's names survive and inline literals are stripped from the query.
  </action>
  <acceptance_criteria>
    - Unit: named + anonymous ops, query + mutation, inline literals, $variables → correct arg/field NAMES, value-stripped query
    - The function is pure (no I/O), returns undefined for non-GraphQL bodies
  </acceptance_criteria>
  <verify><automated>node --test 'test/capture/interceptor.test.ts'</automated></verify>
  <done>The GraphQL operation's shape is extractable pre-redaction without any value.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the fragment at all interceptor capture sites + CAP-05 planted-secret regression</name>
  <files>src/capture/interceptor.ts, test/capture/interceptor.test.ts</files>
  <read_first>
    - src/capture/interceptor.ts (the 3 GraphQL identifier sites: held path ~L455, allowWrites path ~L388, allowed read path ~L555)
  </read_first>
  <behavior>
    - At EACH of the 3 sites where graphqlOperationName is already extracted pre-redaction, ALSO extract
      graphqlSchema from the same rawPostData and set it on the record (spread pattern identical to
      the ...graphqlOperationName conditional). Only for cls.protocol === 'GraphQL'.
    - Redaction ordering UNCHANGED: extraction reads rawPostData BEFORE redactBody; the stored
      requestBody is still redactBody(rawBody). No new call reorders redact*().
    - MANDATORY CAP-05 regression: a GraphQL request whose query has an inline literal secret AND whose
      variables carry a secret value → assert (a) record.graphqlSchema has the arg/field NAMES,
      (b) record.graphqlSchema stringified contains ZERO occurrence of either planted secret,
      (c) record.requestBody (redacted) contains ZERO occurrence of the variable secret (value stripped
      to a type keyword by redactBody), (d) a grep/source-inspection assertion that redactBody/redactHeaders
      are still called before store.append at each site (the 03-05 T-03-05a ordering precedent).
  </behavior>
  <action>
    Add the ...graphqlSchema spread at all 3 sites. Add the planted-secret regression test asserting
    loudly across held + allowed paths. Reuse the existing fake-route test harness in interceptor.test.ts.
  </action>
  <acceptance_criteria>
    - node --test 'test/capture/interceptor.test.ts' PASSES incl. the planted-secret test
    - grep confirms no change to redact*() → store.append ordering (CAP-05 intact)
  </acceptance_criteria>
  <verify><automated>node --test 'test/capture/interceptor.test.ts'</automated></verify>
  <done>GraphQL records carry the schema fragment; planted secrets are provably stripped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Surface graphqlSchema + bodyEncoding (#1) + pollingIntervalMs (#6) on EndpointTemplate</name>
  <files>src/spec/templater.ts, src/types/spec.ts, src/spec/generator.ts, test/spec/templater.test.ts, test/spec/generator.test.ts</files>
  <read_first>
    - src/spec/templater.ts (groupRecords — group accumulation + output map)
    - src/types/spec.ts (EndpointTemplate + add GraphQLSchemaFragment interface)
  </read_first>
  <behavior>
    - EndpointTemplate gains: graphqlSchema?: GraphQLSchemaFragment; bodyEncoding?: 'json'|'form'|'text'|'binary'; pollingIntervalMs?: number. GraphQLSchemaFragment defined in src/types/spec.ts.
    - graphqlSchema: taken from the first record in the group carrying one (one endpoint = one operation via the existing key). Passed through generateSpec's normalize step UNCHANGED (it is names + a value-stripped query, not a body shape — normalizeShapeLeaves must NOT touch it).
    - bodyEncoding: derived from the group's request content-type header value (survives redaction — not on AUTH_HEADER_BLOCKLIST): application/json→'json', application/x-www-form-urlencoded or multipart/form-data→'form', text/*→'text', binary types→'binary'. Absent when there is no request body.
    - pollingIntervalMs: when polling:true, the median inter-arrival (ms) between successive record.timestamp values of the repeated concrete URL. Absent when <2 timestamps or non-polling.
    - Determinism + first-seen order preserved. Existing templater tests still pass.
  </behavior>
  <action>
    Extend the group accumulator to capture content-type + per-URL timestamps + graphqlSchema; compute
    the three fields in the output map. Ensure generator's normalize step skips graphqlSchema. Update
    templater + generator tests; add fixtures for a JSON vs text body, a polling series, and a GraphQL
    endpoint carrying a fragment.
  </action>
  <acceptance_criteria>
    - node --test 'test/spec/templater.test.ts' and 'test/spec/generator.test.ts' PASS
    - GraphQL endpoint fixture → endpoint.graphqlSchema present with arg/field NAMES + value-stripped query, and still secret-clean under the recursive assertion
    - JSON-body endpoint → bodyEncoding:'json'; polling series → pollingIntervalMs set
  </acceptance_criteria>
  <verify><automated>node --test 'test/spec/templater.test.ts' 'test/spec/generator.test.ts'</automated></verify>
  <done>Spec endpoints carry per-operation GraphQL depth, unambiguous body encoding, and informative polling.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02a | Information Disclosure | pre-redaction GraphQL query read | mitigate | extractGraphQLSchemaFragment reads the query STRING for SHAPE only; inline argument literals are stripped to a placeholder; the variables object is never read here (it is redacted in the body). Mandatory planted-secret test (inline literal + variable value) asserts zero secret in the fragment AND the redacted body still strips the variable value. |
| T-11-02b | Information Disclosure | graphqlSchema through the generator normalize step | mitigate | normalizeShapeLeaves must NOT recurse into graphqlSchema (it is names + value-stripped query, not a body shape). A recursive no-raw-value assertion over the whole spec (incl. graphqlSchema) pins that no observed value survives. |
| T-11-02c | Information Disclosure | bodyEncoding from content-type | mitigate | content-type value survives redaction because it is non-sensitive (not on AUTH_HEADER_BLOCKLIST); bodyEncoding is a fixed enum keyword derived from it — never a payload value. |
| T-11-02d | Tampering | redact call ordering | mitigate | Ordering-unchanged grep/source-inspection asserts redact*() precede store.append at all 3 sites (03-05 T-03-05a precedent). |

No package installs — no supply-chain checkpoint. This is the ONLY Phase 11 plan that reads pre-redaction (D11-02).
</threat_model>

<verification>
- Full suite green at baseline (894); tsc exit 0.
- Planted-secret regression (variable value + inline literal) proves CAP-05 fail-closed on the new pre-redaction read.
- GraphQL endpoint fixture carries a real per-operation fragment; recursive no-raw-value assertion over the spec passes.
</verification>

<success_criteria>
- SPEC-09 holds: GraphQL endpoints carry an arg-name + selection-shape fragment; body encoding + polling are informative; a planted secret is provably stripped while the operation structure survives.
</success_criteria>

<output>
Create `.planning/phases/11-spec-quality-enrichment/11-02-SUMMARY.md` when done. Tick 11-02 in ROADMAP.md (Phase 11 → 2/4). Do NOT flip SPEC-09 to Complete in REQUIREMENTS until 11-04 verifies end-to-end.
</output>
</content>
