---
phase: 11-spec-quality-enrichment
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - src/types/spec.ts
  - src/spec/generator.ts
  - test/spec/generator.test.ts
autonomous: true
requirements: [SPEC-10]

must_haves:
  truths:
    - "The spec carries a top-level auth block ONLY when auth signal is observed: login/auth endpoint paths, auth header NAMES, token transport (header/cookie), and role/permission field NAMES"
    - "The auth block is recursively secret-clean — no header VALUE, no token, no observed value; header NAMES survive by CAP-04, values are [REDACTED]"
    - "rules.evidence entries are human-readable portable descriptors (e.g. 'GET /api/users/{id} -> 401'), never bare capture-record UUIDs"
    - "A dataModel whose fields heavily overlap another carries a note explaining the overlap; a held endpoint whose response was never seen carries responseUnobserved:true (no fabricated response)"
  artifacts:
    - path: "src/spec/generator.ts"
      provides: "inferAuth + human-readable rule evidence + dataModel overlap note + held responseUnobserved flag"
      exports: ["inferAuth", "generateSpec", "inferRules", "inferDataModels"]
    - path: "src/types/spec.ts"
      provides: "AuthBlock + ArcheoSpec.auth; DataModel.note; EndpointTemplate.responseUnobserved"
      exports: ["AuthBlock", "ArcheoSpec", "DataModel"]
  key_links:
    - from: "src/capture/redactor.ts"
      to: "src/spec/generator.ts"
      via: "AUTH_HEADER_BLOCKLIST names survive redaction (CAP-04) → inferAuth reads header NAMES from already-redacted records"
      pattern: "authHeaderNames|AUTH_HEADER"
---

<objective>
Close SPEC-10 (auth semantics) and fold in the remaining generator-clarity findings: #3 (dataModel
overlap unexplained), #8 (opaque rules.evidence UUIDs), and #2 (held-mutation responses surfaced
better, NOT fabricated). The builder flagged auth SIGNALS with no semantics; infer and surface an
`auth` block — observed login/auth endpoints, the auth header NAME (survives redaction, CAP-04), token
transport (header vs cookie), and role/permission field names — all from ALREADY-redacted records,
values never emitted. Entirely post-redaction: no new capture, no CAP-05 boundary crossing. TDD.

Output: a populated `auth` block; portable human-readable rule evidence; a dataModel overlap `note`; a
factual `responseUnobserved` flag on held endpoints. Wave 3 of Phase 11.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-spec-quality-enrichment/11-CONTEXT.md
@src/spec/generator.ts
@src/capture/redactor.ts
@src/types/spec.ts
@src/types/index.ts

<interfaces>
# inferAuth reads ONLY already-redacted records:
#   - login endpoints: template paths matching /login|/logout|/auth|/signin|/token|/session|/oauth|/mfa
#   - auth header NAMES: intersection of AUTH_HEADER_BLOCKLIST (redactor.ts, CAP-04) with the
#       header NAMES present in redacted requestHeaders (values are already '[REDACTED]')
#   - transport: 'header' if an authorization/x-*-token header name present; 'cookie' if cookie/set-cookie present
#   - role field NAMES: response-shape keys matching role|roles|permission|permissions|scope|scopes|isAdmin|admin|grants
# rules.evidence (inferRules) currently pushes record UUIDs — replace with descriptors.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AuthBlock type + inferAuth (SPEC-10) — post-redaction, secret-clean</name>
  <files>src/types/spec.ts, src/spec/generator.ts, test/spec/generator.test.ts</files>
  <read_first>
    - src/capture/redactor.ts (AUTH_HEADER_BLOCKLIST — the CAP-04 header-name survivor set)
    - src/spec/generator.ts (generateSpec assembly + inferRules auth-required detector for path patterns)
    - src/types/spec.ts (ArcheoSpec top-level)
  </read_first>
  <behavior>
    - AuthBlock { loginEndpoints: string[]; authHeaderNames: string[]; tokenTransport: ('header'|'cookie')[]; roleFieldNames: string[] } added to src/types/spec.ts; ArcheoSpec gains optional auth?: AuthBlock.
    - inferAuth(templates, records): deterministic, reads ONLY already-redacted records.
      - loginEndpoints: distinct TEMPLATED paths matching the auth path pattern.
      - authHeaderNames: distinct header NAMES present in requestHeaders that are on AUTH_HEADER_BLOCKLIST (CAP-04 — names survive, values stripped). Report NAMES only.
      - tokenTransport: 'header' when an authorization/x-*-token header name is present; 'cookie' when cookie/set-cookie present; both may appear; de-duplicated, stable order.
      - roleFieldNames: distinct response-shape keys matching the role/permission name set (NAMES only — the values were already type-normalized).
    - generateSpec sets spec.auth = inferAuth(...) ONLY when at least one signal is non-empty; otherwise auth is omitted (undefined) so non-auth apps get no empty block.
    - NO VALUE is ever emitted: paths, header NAMES, a transport enum, field NAMES only.
  </behavior>
  <action>
    Implement inferAuth (pure, deterministic). Wire into generateSpec after coverage. RED test first
    with an auth-rich fixture (login endpoint + authorization header + a role field) and a non-auth
    fixture (asserts auth omitted). Include a RECURSIVE no-raw-value assertion over the auth block:
    no '[REDACTED]', no observed value, no token — only the four name/path/enum lists.
  </action>
  <acceptance_criteria>
    - Auth-rich fixture → populated auth block with correct login endpoints, header NAMES, transport, role field NAMES
    - Non-auth fixture → spec.auth is undefined (omitted)
    - Recursive no-raw-value assertion over auth passes
  </acceptance_criteria>
  <verify><automated>node --test 'test/spec/generator.test.ts'</automated></verify>
  <done>The spec surfaces auth semantics from redacted records, never a secret.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Portable human-readable rules.evidence (#8)</name>
  <files>src/spec/generator.ts, test/spec/generator.test.ts</files>
  <read_first>
    - src/spec/generator.ts (inferRules — each rule pushes record UUIDs into evidence)
  </read_first>
  <behavior>
    - Rule.evidence entries become human-readable, self-contained descriptors — e.g. auth-required → 'GET /api/users/{id} -> 401'; pagination → 'GET /api/users?page,limit'; resource-crud → 'GET+GET/{id}+held-write on /api/users'; write-held-behavior → 'POST /api/settings (held)'. No bare capture-record UUID appears in any evidence string in the portable spec.
    - Rule.evidence stays string[] (shape unchanged, values changed) — no consumer breaks. Confidence derivation unchanged.
    - Descriptors are deterministic and templated-path based (portable without the capture store).
  </behavior>
  <action>
    Replace the record.id lookups in inferRules with descriptor construction from method + templated
    path + status/param facts already in scope. Update the generator tests (and any test asserting UUID
    evidence) to assert descriptor strings + no UUID pattern.
  </action>
  <acceptance_criteria>
    - node --test 'test/spec/generator.test.ts' PASSES; no evidence string matches a UUID regex
    - Evidence reads meaningfully without the capture store
  </acceptance_criteria>
  <verify><automated>node --test 'test/spec/generator.test.ts'</automated></verify>
  <done>rules.evidence is portable and human-readable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: dataModel overlap note (#3) + held responseUnobserved flag (#2, no fabrication)</name>
  <files>src/spec/generator.ts, src/types/spec.ts, test/spec/generator.test.ts</files>
  <read_first>
    - src/spec/generator.ts (inferDataModels; buildCoverage held-gap logic; template mapping)
    - src/types/spec.ts (DataModel, EndpointTemplate)
  </read_first>
  <behavior>
    - DataModel gains note?: string. When model A's field-name set overlaps model B's by >= a fixed threshold (e.g. 80% of the smaller set), A (and/or B) carries a note: 'shares N/M field names with User; likely a projection/session view'. Deterministic; pairwise over the inferred models; stable wording.
    - EndpointTemplate gains responseUnobserved?: true — set true ONLY on held endpoints whose responseBodyShape is null and statusCodes is empty (a factual marker that the response was never seen). NOT a fabricated response shape or status. coverage.knownGaps per-endpoint entries stay as-is.
    - No change to the held/floor behavior; this is purely a spec-legibility marker.
  </behavior>
  <action>
    Add the overlap-note pass to inferDataModels (or a post-pass in generateSpec over dataModels). Set
    responseUnobserved in the generator's template normalize step (or buildCoverage-adjacent) for held
    endpoints with no observed response. Extend types + tests with a Profile/User-overlap fixture and a
    held-endpoint fixture.
  </action>
  <acceptance_criteria>
    - Overlapping-models fixture → note present and accurate; distinct models → no note
    - Held endpoint with no response → responseUnobserved:true; a normal read endpoint → flag absent; NO fabricated response body/status anywhere
    - Recursive no-raw-value assertion over the whole spec still passes
  </acceptance_criteria>
  <verify><automated>node --test 'test/spec/generator.test.ts'</automated></verify>
  <done>Model overlap is explained; the held-response gap is visible inline without fabrication.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03a | Information Disclosure | auth block | mitigate | inferAuth reads ONLY already-redacted records; header NAMES survive by CAP-04, values are '[REDACTED]'; the block emits paths + NAMES + a transport enum only. A recursive no-raw-value assertion pins that no value, token, or '[REDACTED]' string leaks. No pre-redaction read. |
| T-11-03b | Information Disclosure | rule evidence descriptors | mitigate | Descriptors are built from method + templated path + status/param facts (already non-sensitive); a UUID-regex assertion pins that no opaque record id (or value) is emitted. |
| T-11-03c | Tampering | responseUnobserved as a fabricated response | avoid | responseUnobserved is a boolean FACT (response never seen); no response body or status is invented. Held/floor behavior unchanged. |

No package installs — no supply-chain checkpoint. No pre-redaction read added (fully post-redaction — D11-02).
</threat_model>

<verification>
- Full suite green at baseline (894); tsc exit 0.
- Auth-rich + non-auth fixtures pin SPEC-10; recursive no-raw-value assertion over auth passes.
- Evidence descriptors carry no UUID; overlap note + responseUnobserved fixtures pin #3/#2; #8 pinned by the UUID-free assertion.
</verification>

<success_criteria>
- SPEC-10 holds: a populated, secret-clean auth block from redacted records; evidence is portable; model overlap and the held-response gap are legible — no fabrication, no secret value.
</success_criteria>

<output>
Create `.planning/phases/11-spec-quality-enrichment/11-03-SUMMARY.md` when done. Tick 11-03 in ROADMAP.md (Phase 11 → 3/4). Do NOT flip SPEC-10 to Complete in REQUIREMENTS until 11-04 verifies end-to-end.
</output>
</content>
