---
phase: 11-spec-quality-enrichment
plan: 04
type: execute
wave: 4
depends_on: ["11-03"]
files_modified:
  - .planning/phases/11-spec-quality-enrichment/11-04-ENRICHMENT-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
autonomous: true
requirements: [SPEC-08, SPEC-09, SPEC-10]

must_haves:
  truths:
    - "A single regenerated spec (from a GraphQL + auth + back-nav fixture) simultaneously exhibits templated flow states with kind tags + observed back-edges, per-operation GraphQL schema fragments with real arg/field names, and a populated auth block"
    - "The entire enriched spec is recursively secret-clean — a recursive no-raw-value walk finds zero planted secret, zero raw email/token, zero header value"
    - "The canonical examples/demo-app/ is NOT broken; if extension is risky, a dedicated phase fixture is used instead"
    - "Baseline suite stays green (894 = 893 pass + 1 documented skip); tsc --noEmit exits 0"
  artifacts:
    - path: ".planning/phases/11-spec-quality-enrichment/11-04-ENRICHMENT-VERIFICATION.md"
      provides: "the enriched-spec verification evidence + secret-clean proof + milestone-close record"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 11 Complete + v1.1 milestone COMPLETE"
  key_links:
    - from: "src/spec/generator.ts"
      to: ".planning/phases/11-spec-quality-enrichment/11-04-ENRICHMENT-VERIFICATION.md"
      via: "generateSpec over the enrichment fixture proves SPEC-08/09/10 together"
      pattern: "graphqlSchema|auth|back"
---

<objective>
Verify the three enrichments hold TOGETHER on one spec and CLOSE Phase 11 AND milestone v1.1.
Regenerate a spec from a capture fixture rich in GraphQL (args + selection), auth (login endpoint +
auth header + role field), and back-navigation (a back agent-step and/or an A->B->A return), and assert
the enriched spec simultaneously carries: templated flow states + kind tags + back-edges (SPEC-08);
per-operation GraphQL schema fragments with real arg/field names (SPEC-09); a populated auth block
(SPEC-10) — and that EVERYTHING is recursively secret-clean (recursive no-raw-value assertion). Then do
the full milestone-close bookkeeping.

This plan writes NO `src/` or `test/` code (the enrichments landed in 11-01..11-03). It authors the
verification evidence + the milestone-close bookkeeping. Wave 4 (final) of Phase 11.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-spec-quality-enrichment/11-CONTEXT.md
@.planning/phases/11-spec-quality-enrichment/11-01-SUMMARY.md
@.planning/phases/11-spec-quality-enrichment/11-02-SUMMARY.md
@.planning/phases/11-spec-quality-enrichment/11-03-SUMMARY.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md

<interfaces>
# Verification fixture choice (D11-08 / brief): PREFER a dedicated phase fixture (a hand-authored
#   capture.jsonl + manifest.json under .planning/phases/11-.../11-04-fixture/, node-built-ins/zero-dep)
#   over extending examples/demo-app/, because the canonical demo app is a shipped OSS artifact and
#   extending it risks its provenance + the 10-02 secret-clean gate. The demo app's held-write GraphQL
#   mutation already gives some SPEC-09 surface; the fixture adds the auth + explicit back-nav surface
#   the non-login-walled demo app lacks. If the executor extends demo-app instead, it MUST re-run the
#   10-02 secret-clean grep + drivability harness and confirm both still pass — otherwise use the fixture.
# The verification is a deterministic generateSpec(...) over the fixture — no live browser, no key.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the enrichment verification fixture + assert all three enrichments together</name>
  <files>.planning/phases/11-spec-quality-enrichment/11-04-fixture/ (capture.jsonl + manifest.json), .planning/phases/11-spec-quality-enrichment/11-04-ENRICHMENT-VERIFICATION.md</files>
  <read_first>
    - src/spec/generator.ts (generateSpec — the entry point to run over the fixture)
    - .planning/phases/11-spec-quality-enrichment/11-01-SUMMARY.md / 11-02-SUMMARY.md / 11-03-SUMMARY.md (what each landed)
  </read_first>
  <behavior>
    - Author a deterministic capture fixture (JSONL records + manifest) exercising all three surfaces:
      GraphQL query + mutation with named arguments and a selection set (incl. a PLANTED secret in a
      variable value + an inline literal); an auth surface (a /login or /auth endpoint, an authorization
      or cookie header, a response carrying a role/permission field); and back-navigation (navigation
      records forming A->B->A AND/OR an agent-step record with agentAction:'back').
    - Run the REAL generateSpec over the fixture (via a small .planning/-only node runner, node built-ins,
      zero deps) and assert the produced archeo-spec.json simultaneously satisfies:
        * flows.states are templated (a parameterized page collapses to one {id} state) + carry kind tags; at least one flows.transitions entry has back:true (SPEC-08)
        * at least one GraphQL endpoint carries graphqlSchema with real argument NAMES + selection field NAMES + a value-stripped query; bodyEncoding + pollingIntervalMs present where applicable (SPEC-09)
        * spec.auth is populated: loginEndpoints, authHeaderNames, tokenTransport, roleFieldNames (SPEC-10)
        * dataModel note + rules.evidence descriptors + held responseUnobserved present where applicable
    - RECURSIVE no-raw-value gate over the ENTIRE enriched spec: walk every leaf; assert zero occurrence
      of the planted secrets, zero raw email/token, zero header value — strict grep + structured walk.
  </behavior>
  <action>
    Create the fixture + a .planning/-only runner; capture the produced spec + the assertion output into
    11-04-ENRICHMENT-VERIFICATION.md (numbers table + the secret-clean proof + which fixture path was
    used and why). Follow the 02-04/05-05/08-02/10-02 harness pattern (node built-ins, zero deps, no
    src/test edits).
  </action>
  <acceptance_criteria>
    - The enriched spec exhibits SPEC-08 + SPEC-09 + SPEC-10 simultaneously (documented with concrete values)
    - Recursive no-raw-value gate: 0 hits
    - examples/demo-app/ NOT broken (fixture used, or demo-app re-verified per the interface note)
    - Full suite green (894); tsc --noEmit exit 0 — both recorded as the pre/post gate
  </acceptance_criteria>
  <verify><automated>node --test 'test/**/*.test.ts'</automated></verify>
  <done>All three enrichments proven together on one secret-clean spec.</done>
</task>

<task type="auto">
  <name>Task 2: Close Phase 11 AND milestone v1.1 — full bookkeeping</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md, .planning/PROJECT.md</files>
  <read_first>
    - .planning/ROADMAP.md (v1.1 Phase 11 row + Progress (v1.1) table)
    - .planning/STATE.md (frontmatter progress + Current Position)
    - .planning/REQUIREMENTS.md (SPEC-08/09/10 checklist + traceability table + coverage tallies)
    - .planning/PROJECT.md (milestone status + backlog section)
  </read_first>
  <behavior>
    - ROADMAP.md: Phase 11 row → 4/4 Complete (2026-07-04), each plan ticked; Progress (v1.1) table row
      → Complete; add the "Milestone v1.1 COMPLETE" banner alongside the preserved v1.0 COMPLETE record
      (append, do NOT overwrite v1.0 history).
    - STATE.md: frontmatter completed_phases 3, total_phases 3, completed_plans += 4, percent 100,
      status COMPLETE (milestone v1.1); Current Position + Session Continuity updated to reflect v1.1
      COMPLETE; stopped_at summarizes the Phase 11 enrichment close.
    - REQUIREMENTS.md: SPEC-08/09/10 → [x] Complete in the v1.1 checklist AND the traceability table
      (Phase 11 rows → Complete (11-0N)); update the coverage tallies (v1.1: 7/7 Complete) + the footer.
    - PROJECT.md: milestone v1.1 → COMPLETE; note any items rolled to a v1.2 backlog (the Phase-10
      affordance-drivability / unreachable-vs-absent-in-compare finding + full GraphQL schema
      reconstruction — both explicitly deferred in 11-CONTEXT D11-08).
    - Commit trailer Co-Authored-By: Claude Fable 5 on every commit.
  </behavior>
  <action>
    Apply the bookkeeping edits verbatim-consistent with the 10-02 close pattern (append, preserve
    history). Record the v1.2 backlog roll-forward explicitly in PROJECT.md.
  </action>
  <acceptance_criteria>
    - ROADMAP Phase 11 4/4 Complete + v1.1 COMPLETE; STATE percent 100 / status COMPLETE; REQUIREMENTS SPEC-08/09/10 Complete + tallies correct; PROJECT v1.1 COMPLETE + v1.2 backlog noted
    - v1.0 record preserved (not overwritten)
  </acceptance_criteria>
  <verify><automated>node --test 'test/**/*.test.ts'</automated></verify>
  <done>Phase 11 and milestone v1.1 are formally closed; v1.0 history preserved; backlog rolled to v1.2.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04a | Information Disclosure | regenerated enriched spec | mitigate | The recursive no-raw-value gate over the ENTIRE spec (incl. graphqlSchema + auth) is the acceptance gate; planted secrets in the fixture prove the gate bites. |
| T-11-04b | Tampering | breaking the shipped demo app | avoid | Prefer a dedicated phase fixture; if demo-app is extended, the 10-02 secret-clean grep + drivability harness MUST be re-run and pass, else revert to the fixture. |

No package installs — no supply-chain checkpoint. No src/ or test/ code changes in this plan.
</threat_model>

<verification>
- One enriched spec exhibits SPEC-08 + SPEC-09 + SPEC-10 together, recursively secret-clean.
- Suite green (894); tsc exit 0.
- Milestone bookkeeping consistent + v1.0 preserved.
</verification>

<success_criteria>
- Phase 11 delivers all three enrichments proven together and secret-clean; milestone v1.1 is COMPLETE (3/3 phases, SPEC-08/09/10 Complete), v1.0 history preserved, remaining ideas rolled to v1.2.
</success_criteria>

<output>
Create `.planning/phases/11-spec-quality-enrichment/11-04-SUMMARY.md` when done. This plan closes Phase 11 AND milestone v1.1: ROADMAP Phase 11 4/4 Complete + v1.1 COMPLETE; STATE completed_phases 3/3 + percent 100 + status COMPLETE; REQUIREMENTS SPEC-08/09/10 Complete; PROJECT.md v1.1 COMPLETE + v1.2 backlog noted.
</output>
</content>
