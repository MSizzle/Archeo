---
phase: 09-type-safety-docs-hygiene
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - CONTRIBUTING.md
  # phase-close bookkeeping:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - "The CONTRIBUTING.md test-layout diagram names EXACTLY the directories that exist under test/ at end of Phase 9 — every listed directory exists and none are omitted (DOC-01), derived from `ls test/`, not guessed"
    - "The stale `types/` row is reconciled with reality: after 09-01 created test/types/ (the typecheck guard), the row is kept but its description updated to the QUAL-02 typecheck guard — it is no longer a phantom directory"
    - "The previously-omitted `test/oss/` directory is added to the diagram with an accurate description (OSS/example-spec tests)"
    - "An acceptance check confirms bidirectional match: every directory named in the diagram exists under test/, AND every directory under test/ is named in the diagram (no omissions) — run as a scripted comparison, recorded in the SUMMARY"
    - "Docs-only change: the default suite count is unchanged at 892; no src/ or test/ code touched; LICENSE + NOTICE untouched; GATE-03 no-network guard green"
    - "Phase close bookkeeping: ROADMAP v1.1 Phase 9 → 2/2 Complete; REQUIREMENTS QUAL-01/QUAL-02/DOC-01 → Complete (checklist + traceability); STATE advanced to Phase 10 with Phase 9 recorded complete"
  artifacts:
    - path: "CONTRIBUTING.md"
      provides: "a test-layout diagram that matches the real test/ tree (DOC-01)"
      exports: []
  key_links:
    - from: "CONTRIBUTING.md test-layout diagram"
      to: "the real test/ directory tree"
      via: "every diagram row is a directory under test/ and vice versa; verified by a scripted ls-vs-diagram diff"
      pattern: "test/(agent|capture|cli|dashboard|model|oss|security|spec|types)"
---

<objective>
Fix the one remaining v1.0-close audit nit (AN-2): the CONTRIBUTING.md **test-layout diagram** does
not match the real `test/` tree. Today it lists a `types/` row (which did not exist — until 09-01
created it for the typecheck guard) and omits the present `test/oss/`. Make the diagram an exact
mirror of the end-of-Phase-9 tree and add a small acceptance check that keeps it honest (DOC-01).
Then close Phase 9. Docs-only; the suite is untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-type-safety-docs-hygiene/09-CONTEXT.md
@.planning/phases/09-type-safety-docs-hygiene/09-01-SUMMARY.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@CONTRIBUTING.md

<tree_reference>
Real `test/` subdirectories today (2026-07-04): agent, capture, cli, dashboard, model, oss, security,
spec. After 09-01 lands, `test/types/` also exists (the QUAL-02 guard `test/types/typecheck.guard.ts`).
So the **end-of-Phase-9 tree** the diagram must match is exactly:

  agent  capture  cli  dashboard  model  oss  security  spec  types

The current CONTRIBUTING diagram lists: agent, capture, cli, dashboard, model, security, spec, types —
i.e. it is missing `oss` and its `types` row (previously a phantom) is now real but mis-described
("(minimal) shared-type sanity checks" → should describe the typecheck regression guard). RE-DERIVE
the true tree with `ls -d test/*/` at execution time; do not trust this reference blindly.
</tree_reference>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Derive the real tree and correct the diagram</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - CONTRIBUTING.md — the "## Test suite layout" fenced diagram (currently ~lines 190–201)
    - 09-01-SUMMARY.md — confirm test/types/typecheck.guard.ts was created (so the `types/` row is now
      backed by a real directory)
  </read_first>
  <behavior>
    - Run `ls -d test/*/` to get the authoritative directory list (expect: agent, capture, cli,
      dashboard, model, oss, security, spec, types).
    - Edit the fenced diagram so it lists exactly those directories, each with a short accurate
      description mirroring `src/<layer>/` where applicable:
      - `oss/` — tests for OSS packaging / example specs (add — was omitted)
      - `types/` — the typecheck regression guard (QUAL-02, `typecheck.guard.ts`, run via `npm run
        test:types`) (update the stale "(minimal) shared-type sanity checks" description)
      - keep agent/capture/cli/dashboard/model/security/spec rows, adjusting wording only if inaccurate
    - If the surrounding prose cites a suite baseline (e.g. "858 total"), leave functional numbers
      alone UNLESS they are stated as the current baseline and are wrong — the live baseline is 892
      (891 pass + 1 skip); correct it only if the sentence claims to state the current total. (Do not
      invent numbers; verify against `node --test 'test/**/*.test.ts'` if you touch it.)
  </behavior>
  <action>Update the diagram (and any adjacent stale baseline sentence) to match the real tree + suite.</action>
  <acceptance_criteria>
    - The diagram lists exactly the directories from `ls -d test/*/` — `oss/` present, `types/`
      described as the typecheck guard, nothing phantom.
    - No non-existent directory remains in the diagram.
  </acceptance_criteria>
  <verify><automated>ls -d test/*/; grep -nE "oss/|types/" CONTRIBUTING.md</automated></verify>
  <done>The test-layout diagram is an exact mirror of the real test/ tree.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Acceptance check — bidirectional diagram ↔ tree match</name>
  <files>(scripted check; recorded in SUMMARY)</files>
  <read_first>
    - The corrected diagram from Task 1
  </read_first>
  <behavior>
    - Run a scripted comparison proving DOC-01 both ways:
      1. Every directory named in the diagram exists under `test/` (no phantom rows).
      2. Every directory under `test/` is named in the diagram (no omissions).
    - Suggested form (adapt as needed): extract the directory tokens from the fenced diagram block,
      `comm`/`diff` them against `ls -d test/*/ | xargs -n1 basename | sort`, and assert both set
      differences are empty. Record the command and its empty-diff output in the SUMMARY as the DOC-01
      evidence. This is a docs acceptance check, NOT a new test file (09-02 stays docs-only; the suite
      count must not change).
  </behavior>
  <action>Run the comparison; paste the empty-diff evidence into 09-02-SUMMARY.md.</action>
  <acceptance_criteria>
    - Both set differences (diagram−tree and tree−diagram) are empty.
    - No new file under `test/` (the suite count is unchanged).
  </acceptance_criteria>
  <verify><automated>diff <(grep -oE "(agent|capture|cli|dashboard|model|oss|security|spec|types)/" CONTRIBUTING.md | tr -d '/' | sort -u) <(ls -d test/*/ | xargs -n1 basename | sort -u) && echo "DIAGRAM MATCHES TREE"</automated></verify>
  <done>The diagram is provably exact — and a cheap check documents how it was verified.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Phase 9 close — gates, SUMMARY, bookkeeping</name>
  <files>.planning/phases/09-type-safety-docs-hygiene/09-02-SUMMARY.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - 09-01-SUMMARY.md (QUAL-01/QUAL-02 outcomes) + Tasks 1–2 here
  </read_first>
  <behavior>
    - Final gates (docs plan, but confirm nothing regressed): `npx tsc --noEmit` exits 0;
      `npm run test:types` exits 0; `node --test 'test/**/*.test.ts'` == 892 (891 pass + 1 skip);
      `git diff --stat LICENSE NOTICE` empty.
    - Write 09-02-SUMMARY.md: the real tree, the diagram corrections (added `oss/`, re-described
      `types/`), and the bidirectional match evidence.
    - Bookkeeping (phase close):
      - ROADMAP v1.1 Progress: Phase 9 → 2/2 Complete (date 2026-07-04); tick 09-02; update the v1.1
        progress table row and the "current focus" note (→ Phase 10).
      - REQUIREMENTS: flip QUAL-01, QUAL-02, DOC-01 → Complete in BOTH the v1.1 checklist and the
        traceability table (cite 09-01 for QUAL, 09-02 for DOC-01).
      - STATE: milestone v1.1, Phase 9 COMPLETE (completed_phases 1, completed_plans 2, percent 33),
        current focus → Phase 10; keep v1.0 completion recorded.
  </behavior>
  <action>Run the gates; author the SUMMARY; update ROADMAP + REQUIREMENTS + STATE.</action>
  <acceptance_criteria>
    - tsc exit 0; test:types exit 0; suite 892; LICENSE/NOTICE untouched.
    - ROADMAP Phase 9 row 2/2 Complete; REQUIREMENTS QUAL-01/QUAL-02/DOC-01 Complete (checklist +
      traceability); STATE shows Phase 9 complete, Phase 10 next.
    - `git diff --name-only` for this plan lists only CONTRIBUTING.md and the .planning/ bookkeeping.
  </acceptance_criteria>
  <verify><automated>npx tsc --noEmit; npm run test:types; node --test 'test/**/*.test.ts'</automated></verify>
  <done>DOC-01 is met and Phase 9 is closed; the type-safety + docs-hygiene backlog is cleared and recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs ↔ real tree | A test-layout diagram that lies (phantom `types/`, missing `oss/`) misdirects a new contributor. Mitigation: the diagram is derived from `ls test/` and pinned by a bidirectional acceptance check. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-09-04 | Tampering (docs drift from tree) | CONTRIBUTING test-layout diagram | mitigate | Diagram re-derived from `ls -d test/*/`; a scripted diagram↔tree diff (empty both ways) is the recorded acceptance evidence. |

No code touched; no new outbound surface; suite count unchanged; no-network guard green.
</threat_model>

<verification>
- The CONTRIBUTING test-layout diagram lists exactly the directories under `test/` (agent, capture,
  cli, dashboard, model, oss, security, spec, types) — `oss/` added, `types/` now real + accurately
  described.
- The bidirectional diagram↔tree diff is empty (DOC-01).
- Docs-only: suite stays 892; tsc still 0; test:types still 0; LICENSE/NOTICE untouched.
- Phase 9 closed: ROADMAP 2/2, REQUIREMENTS QUAL-01/QUAL-02/DOC-01 Complete, STATE → Phase 10.
</verification>

<success_criteria>
- DOC-01: the CONTRIBUTING.md test-layout diagram matches the real test/ tree — every listed
  directory exists and none are omitted, proven by a bidirectional check.
- Phase 9 is complete: zero tsc diagnostics (QUAL-01), a typecheck regression guard (QUAL-02), and an
  accurate contributor test-layout diagram (DOC-01) — the v1.0-close hygiene backlog cleared.
</success_criteria>

<output>
Create `.planning/phases/09-type-safety-docs-hygiene/09-02-SUMMARY.md` (real tree, diagram
corrections, bidirectional match evidence, phase-close bookkeeping).
Commit style: `docs(09-02):` for the diagram + bookkeeping, ending with
`Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
</output>
