---
phase: 09-type-safety-docs-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dashboard/types.ts        # NEW — single source-of-truth DashboardHandle
  - src/dashboard/server.ts       # startDashboard return type → imported DashboardHandle
  - src/cli/explore.ts            # delete local DashboardHandle interface; import shared
  - src/cli/index.ts              # annotate dashboardHandle var with shared DashboardHandle
  - test/agent/agent-step-record.test.ts   # double-cast fix (3 sites)
  - test/agent/loop.test.ts                # double-cast fix (8 sites / 11 diagnostics)
  - test/agent/recovery.test.ts            # fake waitForLoadState signature fix
  - test/model/anthropic.test.ts           # RequestInfo → Parameters<typeof fetch>[0] (2 sites)
  - test/types/typecheck.guard.ts          # NEW — QUAL-02 guard (off the fast default path)
  - package.json                           # add test:types script
autonomous: true
requirements: [QUAL-01, QUAL-02]

must_haves:
  truths:
    - "`npx tsc --noEmit` exits 0 — all 18 pre-existing diagnostics are cleared (1 in src/cli/index.ts + 14 Record-cast in test/agent + 1 recovery fake-page + 2 anthropic RequestInfo), verified by running tsc, not assumed (QUAL-01)"
    - "DashboardHandle is unified to ONE exported type (src/dashboard/types.ts); startDashboard returns it, and BOTH src/cli/explore.ts and src/cli/index.ts import it — the duplicate/loose local copies are deleted; this is a type-annotation correction with NO runtime change (the object already carries every method)"
    - "No production type is weakened, widened, or given an index signature to satisfy a test cast — every one of the 17 test diagnostics is fixed on the test side (double-cast `as unknown as Record<string,unknown>`, a Playwright-faithful fake signature, and `Parameters<typeof fetch>[0]`)"
    - "QUAL-02 guard exists: a `test:types` npm script runs a node:test that spawns `tsc --noEmit` and asserts exit 0; it lives at test/types/typecheck.guard.ts so the default `test/**/*.test.ts` glob does NOT pick it up (892-suite runtime unchanged)"
    - "The default full suite stays green at its 892 baseline (891 pass + 1 documented skip test/agent/observation.test.ts); the count is unchanged (test edits fix existing tests; the guard is off the default glob)"
    - "Zero new runtime dependencies; no DOM lib added to tsconfig; no floor/redaction/behavior change; LICENSE + NOTICE untouched; GATE-03 no-network guard green"
  artifacts:
    - path: "src/dashboard/types.ts"
      provides: "the single DashboardHandle interface (full emitter set with precise member types) imported by server.ts, explore.ts, and index.ts"
      exports: ["DashboardHandle"]
    - path: "test/types/typecheck.guard.ts"
      provides: "QUAL-02 regression guard — node:test spawning `tsc --noEmit`, asserting exit 0, off the fast default path"
      exports: []
    - path: "package.json"
      provides: "test:types script that runs the guard explicitly"
      exports: []
  key_links:
    - from: "src/cli/index.ts dashboardHandle variable"
      to: "src/dashboard/types.ts DashboardHandle"
      via: "the variable annotation matches startDashboard's return type, so passing it to runExplore(dashboard?: DashboardHandle) type-checks"
      pattern: "DashboardHandle"
    - from: "package.json test:types"
      to: "test/types/typecheck.guard.ts"
      via: "node --test on the explicit guard path; the guard spawns tsc --noEmit and asserts exit 0"
      pattern: "test:types|tsc --noEmit"
---

<objective>
Drive `npx tsc --noEmit` from **18 diagnostics to 0** (QUAL-01) and add a **typecheck regression
guard** so it never silently drifts back (QUAL-02) — without weakening any production type, adding any
runtime dependency, or changing any runtime behavior. The single `src/` fix unifies the split
`DashboardHandle` type; the rest are narrowest-correct **test-side** fixes. The 892-test default suite
stays green and unchanged in count; the guard runs on a dedicated `test:types` path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-type-safety-docs-hygiene/09-CONTEXT.md
@.planning/PROJECT.md
@CLAUDE.md
@tsconfig.json
@package.json
@src/dashboard/server.ts
@src/cli/index.ts
@src/cli/explore.ts
@src/cli/browser.ts
@test/agent/agent-step-record.test.ts
@test/agent/loop.test.ts
@test/agent/recovery.test.ts
@test/model/anthropic.test.ts

<diagnostics_reference>
The authoritative diagnostic inventory (from a live `npx tsc --noEmit` on 2026-07-04 — RE-RUN it
first and confirm the exact 18 before editing; line numbers may shift as you edit, so fix top-down or
re-run between edits):

  A (1, the only src/ error):
    src/cli/index.ts(349,9)  TS2322  dashboardHandle too narrow for runExplore(dashboard: DashboardHandle)

  B (14, invalid Record<string,unknown> casts — TS2352, some paired with TS2571):
    test/agent/agent-step-record.test.ts  247, 270, 291
    test/agent/loop.test.ts  669, 806, 808(×2), 842, 844(×2), 870, 979, 981(×2)

  C (1, fake page signature):
    test/agent/recovery.test.ts(197,11)  TS2322  waitForLoadState(_state: string) vs Playwright's optional-union param

  D (2, DOM-only name not in libs):
    test/model/anthropic.test.ts(122,14) and (164,37)  TS2552  Cannot find name 'RequestInfo'
</diagnostics_reference>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Confirm the baseline — run tsc and the full suite</name>
  <files>(read-only)</files>
  <read_first>
    - tsconfig.json (lib ES2022, types ["node"], strict, include src+test)
  </read_first>
  <behavior>
    - Run `npx tsc --noEmit` and confirm exactly the 18 diagnostics in the reference (categories
      A/B/C/D). Record the raw output in the SUMMARY as the pre-fix ground truth.
    - Run `node --test 'test/**/*.test.ts'` and confirm the 892 baseline (891 pass + 1 documented
      skip). This is the pre-gate.
  </behavior>
  <action>Capture both outputs into 09-01-SUMMARY.md as the baseline record.</action>
  <acceptance_criteria>
    - The 18 diagnostics are enumerated and match the four categories.
    - The suite is 892 (891 pass + 1 skip, 0 fail) before any edit.
  </acceptance_criteria>
  <verify><automated>npx tsc --noEmit; node --test 'test/**/*.test.ts'</automated></verify>
  <done>The exact pre-fix state is recorded; every later claim is measured against it.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Unify DashboardHandle (Category A — the one src/ fix, type-only)</name>
  <files>src/dashboard/types.ts, src/dashboard/server.ts, src/cli/explore.ts, src/cli/index.ts</files>
  <read_first>
    - src/dashboard/server.ts — the inline return type of `startDashboard` (the full emitter set + its
      IssueLogEntry/ErrorClass/DriftReport imports) is the canonical shape
    - src/cli/explore.ts — the local `interface DashboardHandle` (~lines 99–116) and `runExplore`'s
      `dashboard?: DashboardHandle` param
    - src/cli/index.ts — the two `dashboardHandle` declarations (line ~307 explore path — the erroring
      one; line ~578 manual `<url>` path)
    - src/cli/browser.ts — `openAndWait(dashboard?: { close(): Promise<void> })` (must still accept a
      wider handle)
  </read_first>
  <behavior>
    - Create `src/dashboard/types.ts` exporting a single `interface DashboardHandle` = the full emitter
      set with the PRECISE member types startDashboard already returns (`port: number`, `close`,
      `sendFrame`, `sendState`, `sendTransition`, `sendReasoning`, `sendHeldBeat`, `sendSkip`,
      `sendError(entry: IssueLogEntry)`, `sendHalt(info: { class: ErrorClass; message: string })`,
      `sendDrift(report: DriftReport)`). Import `IssueLogEntry`/`ErrorClass` from
      `../agent/recovery.ts` and `DriftReport` from `../spec/drift.ts` (use `import type`). `.ts`
      extensions; no enums.
    - `src/dashboard/server.ts`: import `DashboardHandle` and change `startDashboard`'s return
      annotation to `Promise<DashboardHandle>`. Do NOT change the returned object.
    - `src/cli/explore.ts`: delete the local `interface DashboardHandle`; import it from
      `../dashboard/types.ts`. `runExplore` signature is otherwise unchanged.
    - `src/cli/index.ts`: annotate the explore-path `dashboardHandle` (line ~307) as
      `DashboardHandle | undefined` (import the type). Unify the manual-path declaration (line ~578)
      too for consistency; VERIFY `openAndWait` still type-checks (it asks for only `{ close }`, which
      a wider handle satisfies).
    - This is type-only. If a type-import cycle appears, fall back to `import type` everywhere or
      re-export the named type from server.ts — the single-source-of-truth is the requirement.
  </behavior>
  <action>Make the four edits; re-run `npx tsc --noEmit` and confirm the src/cli/index.ts(349) error is gone and no NEW error appeared in server.ts/explore.ts/index.ts.</action>
  <acceptance_criteria>
    - `grep -rn "interface DashboardHandle" src/` shows exactly ONE definition (in src/dashboard/types.ts).
    - `npx tsc --noEmit` no longer reports any `src/` error (Category A cleared; only the test
      diagnostics remain).
    - No change to any returned object or runtime path — diff shows only type annotations + imports.
  </acceptance_criteria>
  <verify><automated>grep -rn "interface DashboardHandle" src/; npx tsc --noEmit 2>&1 | grep "src/" || echo "no src/ errors"</automated></verify>
  <done>One DashboardHandle type, imported everywhere; the sole production diagnostic is gone with zero behavior change.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fix the test-side diagnostics (Categories B, C, D — never weaken production types)</name>
  <files>test/agent/agent-step-record.test.ts, test/agent/loop.test.ts, test/agent/recovery.test.ts, test/model/anthropic.test.ts</files>
  <read_first>
    - The B sites read dynamic fields (`agentSource`, `issueCount`) off `CaptureRecord` /
      `ExploreResult & { issueCount: number }` — neither has an index signature
    - recovery.test.ts ~line 197 — the fake page's `waitForLoadState`
    - anthropic.test.ts ~lines 122, 164 — the fake fetch param typed `RequestInfo | URL`, later cast
      `as typeof fetch`
  </read_first>
  <behavior>
    - **Category B (14 diagnostics):** replace each `X as Record<string, unknown>` with
      `X as unknown as Record<string, unknown>` (the compiler-suggested double assertion). Fixing the
      cast at a paired site (808/844/981) also clears its TS2571. Do NOT add an index signature to any
      production type. (An `asRecord(x)` test-local helper is an allowed alternative, but the
      double-cast is the preferred lowest-diff form.)
    - **Category C (1):** change the fake to
      `async waitForLoadState(_state?: 'load' | 'domcontentloaded' | 'networkidle', _options?: { timeout?: number }): Promise<void> { … }`
      so it satisfies Playwright's param type under strictFunctionTypes.
    - **Category D (2):** replace `RequestInfo | URL` with `Parameters<typeof fetch>[0]` at both sites
      (equivalently `string | URL | Request`). Do NOT add the DOM lib to tsconfig; do NOT add a dep.
  </behavior>
  <action>Apply the edits; re-run `npx tsc --noEmit` between files if line numbers drift. Target: 0 diagnostics.</action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 (QUAL-01 met).
    - `git diff` touches only the four test files (plus Task 2's src changes); tsconfig.json is
      unchanged; no production `src/` type gained an index signature.
  </acceptance_criteria>
  <verify><automated>npx tsc --noEmit && echo "TSC CLEAN (exit 0)"</automated></verify>
  <done>All 17 test diagnostics are fixed on the test side; the typecheck is green with production types intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: QUAL-02 — the typecheck regression guard (test:types, off the fast path)</name>
  <files>test/types/typecheck.guard.ts, package.json</files>
  <read_first>
    - package.json scripts (existing `typecheck: "tsc --noEmit"`, `test: "node --test 'test/**/*.test.ts'"`)
  </read_first>
  <behavior>
    - Create `test/types/typecheck.guard.ts` — a `node:test` test that spawns `tsc --noEmit` via
      `node:child_process` (resolve the local binary, e.g. `node_modules/.bin/tsc`, or
      `spawnSync(process.execPath, [require.resolve('typescript/bin/tsc'), '--noEmit'])`; keep it
      dependency-free), asserts `status === 0` with `node:assert/strict`, and on failure includes the
      captured stdout/stderr in the assertion message so a regression is legible. Give the file a
      header comment explaining WHY it is not named `*.test.ts` (kept off the default glob because it
      spawns tsc and is slow) and that it is the QUAL-02 deliverable.
    - **TDD framing:** the guard's RED is the pre-Task-2/3 `tsc` failure (18 diagnostics → non-zero
      exit → the assertion would fail); its GREEN is the post-fix exit 0. Demonstrate this in the
      SUMMARY (e.g. note that running the guard before Tasks 2–3 fails and after passes), then leave it
      green.
    - Add a `test:types` script to package.json: `"test:types": "node --test test/types/typecheck.guard.ts"`.
      Do NOT add it to the default `test` script. `.ts` conventions; no enums; zero new deps.
    - CONFIRM the default glob does not pick it up: `node --test 'test/**/*.test.ts'` must NOT run the
      guard (the file is `.guard.ts`, not `.test.ts`).
  </behavior>
  <action>Write the guard + script; run `npm run test:types` (expect exit 0) and re-run the default suite (expect 892, guard NOT included).</action>
  <acceptance_criteria>
    - `npm run test:types` exits 0 (the guard passes now that tsc is clean).
    - `node --test 'test/**/*.test.ts'` is still 892 (891 pass + 1 skip) — the guard is NOT among them.
    - The guard file carries a header explaining its off-glob naming and QUAL-02 role.
  </acceptance_criteria>
  <verify><automated>npm run test:types; node --test 'test/**/*.test.ts'</automated></verify>
  <done>A one-command regression gate exists; the fast suite runtime and count are unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Full regression gate + SUMMARY + bookkeeping</name>
  <files>.planning/phases/09-type-safety-docs-hygiene/09-01-SUMMARY.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - Tasks 1–4 outputs
  </read_first>
  <behavior>
    - Final gates: `npx tsc --noEmit` exits 0; `npm run test:types` exits 0; `node --test
      'test/**/*.test.ts'` == 892 (891 pass + 1 skip, 0 fail); `git diff --stat LICENSE NOTICE` empty;
      `node --test 'test/security/no-network.test.ts'` green.
    - Write 09-01-SUMMARY.md: the pre-fix 18-diagnostic inventory, the DashboardHandle unification (one
      type, three importers, no runtime change), the per-category test fixes, the guard form + the
      RED→GREEN demonstration, and confirmation the default suite count is unchanged.
    - ROADMAP v1.1 Progress: Phase 9 row → 1/2, mark 09-01 done. STATE: current position → 09-01
      COMPLETE, 09-02 next. (REQUIREMENTS QUAL-01/QUAL-02 flip to Complete at phase close in 09-02 — or
      here if the executor prefers; either is acceptable as long as the traceability table stays
      truthful.)
  </behavior>
  <action>Run the gates; author the SUMMARY; update ROADMAP + STATE.</action>
  <acceptance_criteria>
    - tsc exit 0; test:types exit 0; suite 892; LICENSE/NOTICE untouched; no-network guard green.
    - `git diff --name-only` lists only: src/dashboard/types.ts, src/dashboard/server.ts,
      src/cli/explore.ts, src/cli/index.ts, the four test files, test/types/typecheck.guard.ts,
      package.json, and the .planning/ bookkeeping.
    - 09-01-SUMMARY.md records the inventory, the fixes, and the guard's RED→GREEN.
  </acceptance_criteria>
  <verify><automated>npx tsc --noEmit; npm run test:types; node --test 'test/**/*.test.ts'; git diff --stat LICENSE NOTICE</automated></verify>
  <done>QUAL-01 and QUAL-02 are met and proven; the suite is green and unchanged; the record is preserved for 09-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| types ↔ runtime behavior | A type-only refactor (DashboardHandle) must not silently change what the dashboard emits. Mitigation: the returned object is untouched; the full 892 suite (which exercises the dashboard emitters) is the regression proof. |
| test cast ↔ production type | The temptation is to add an index signature to a production type to make a cast compile — that would weaken the shipped API. Mitigation: D9-02 forbids it; all test diagnostics are fixed on the test side and the diff is checked to touch no production type shape. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-09-01 | Tampering (type change alters behavior) | DashboardHandle unification | mitigate | Type-only edit; returned object unchanged; full suite green pre- and post-. |
| T-09-02 | Tampering (weakened public type) | production types vs test casts | mitigate | Test-side fixes only; `git diff` confirms no src/ type gained an index signature; no DOM lib added. |
| T-09-03 | DoS (slow default suite) | QUAL-02 guard | mitigate | Guard is off the `test/**/*.test.ts` glob on a dedicated `test:types` path; 892-suite runtime unchanged. |

No package installs; no new outbound surface; the GATE-03 no-network guard is re-run.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (was 18 diagnostics) — QUAL-01.
- Exactly one `interface DashboardHandle` in src/ (src/dashboard/types.ts); server.ts, explore.ts,
  index.ts all import it.
- `npm run test:types` exits 0 and is NOT part of the default `node --test 'test/**/*.test.ts'` run
  (still 892 = 891 pass + 1 skip) — QUAL-02.
- No production type weakened; tsconfig unchanged; LICENSE/NOTICE untouched; no-network guard green.
</verification>

<success_criteria>
- QUAL-01: `npx tsc --noEmit` reports zero diagnostics across src/ and test/.
- QUAL-02: a `test:types` guard fails on any reappearing tsc diagnostic, kept off the fast default path.
- The DashboardHandle type is unified to one source of truth with no runtime change; the 892 suite
  stays green and unchanged in count.
</success_criteria>

<output>
Create `.planning/phases/09-type-safety-docs-hygiene/09-01-SUMMARY.md` (pre-fix 18-diagnostic
inventory, the unification, per-category test fixes, the guard + RED→GREEN, unchanged-count proof).
Commit style: `feat(09-01):` (DashboardHandle unification + guard + test:types script),
`test(09-01):` (test-side type fixes), each ending with
`Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
</output>
