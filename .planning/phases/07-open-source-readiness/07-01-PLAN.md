---
phase: 07-open-source-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  # The following src/ file(s) are touched ONLY if the doc-vs-code audit (Task 1)
  # finds a wrong --help/doc string. TDD if testable. Expected: none, but permitted.
  - src/cli/index.ts
autonomous: true
requirements: [OSS-01]

must_haves:
  truths:
    - "Every command and flag documented in README.md exists in src/cli/*.ts — verified by reading the source and by running each command's --help (or the top-level --help); the audit is a recorded acceptance step, not an assumption (D7-01)"
    - "README leads with the KEY-FREE manual path: `archeo <url>` (or the fresh-clone invocation form that actually works) drives the browser manually with the floor ON and auto-generates a spec on window close — NO API key required (src/cli/index.ts <url> action; src/cli/browser.ts openAndWait); the BYO-key autonomous `archeo explore` section comes AFTER (D7-02 §3)"
    - "README never claims `archeo <url>` explores autonomously (the stale claim is removed); autonomy is documented only under `archeo explore` (src/cli/explore.ts)"
    - "The invocation form in the quickstart works in a FRESH CLONE: `bin.archeo`→dist/index.js requires a build, so the README shows `node src/cli/index.ts <url>` (dev script) or an explicit build step — verified against package.json bin/scripts, not assumed"
    - "BYO-key section documents ANTHROPIC_API_KEY (src/cli/index.ts explore action) + `--model provider:model` (src/model/adapter.ts) + the no-bundled-model / no-telemetry posture; the default `scripted` provider is noted as key-free"
    - "The safety-model section explains, in plain language, all of: read-only floor ON by default, redaction fail-closed, destructive-GET tripwire, credential-free login handoff, --allow-writes opt-in+loud, localhost-only dashboard (binds 127.0.0.1), no telemetry/phone-home — each cross-checked against src/ (interceptor/redactor/login/allowWrites/dashboard/no-network guard)"
    - "The spec-contents section matches src/types/spec.ts ArcheoSpec (meta, dataModels, endpoints, flows, rules, coverage) and links to examples/ and CONTRIBUTING.md"
    - "Full test suite stays green at baseline 858 (857 pass + 1 documented skip); LICENSE + NOTICE untouched; no new outbound surface (no-network guard green)"
  artifacts:
    - path: "README.md"
      provides: "the truthful, plain-language OSS README (OSS-01): what-it-is + vendor-escape framing, requirements/install, key-free quickstart, BYO-key autonomous mode, BYO-key config, safety model, spec contents, links to examples/ + CONTRIBUTING"
      exports: []
  key_links:
    - from: "README.md quickstart command(s)"
      to: "src/cli/index.ts command registrations"
      via: "every documented command/flag maps 1:1 to a cli.command(...).option(...) in src/cli/index.ts; the invocation form maps to package.json scripts.dev / bin"
      pattern: "archeo <url>|archeo explore|node src/cli/index.ts"
    - from: "README safety-model section"
      to: "src/capture/interceptor.ts + src/capture/redactor.ts + src/cli/login.ts + src/cli/allowWrites.ts + src/dashboard/server.ts + test/security/no-network.test.ts"
      via: "each plain-language safety claim is backed by a named source file"
      pattern: "floor|redaction|allow-writes|127.0.0.1|no-network"
---

<objective>
Replace the stale, aspirational `README.md` with one that is **true to the shipped command
surface** and readable by a stranger. The current README claims `archeo <url>` autonomously
explores and produces a spec — false: `<url>` is MANUAL capture and `archeo explore` is the
autonomous loop. The rewrite is derived by **reading `src/cli/*.ts`** and cross-checking every
command, flag, env var, and safety claim against the source (a doc-vs-code audit is an acceptance
step). It leads with the **key-free** manual path (the thing that always works), then the BYO-key
autonomous mode, then the safety model in plain language — the trust anchor for a tool strangers
run against their own live accounts.

Output: a rewritten `README.md` (OSS-01). No `src/` change is expected; the ONLY sanctioned code
change is a **minimal, TDD'd fix** if the audit finds a `--help`/doc string in the code that is
itself wrong.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-open-source-readiness/07-CONTEXT.md
@.planning/PROJECT.md
@CLAUDE.md
@README.md
@package.json
@src/cli/index.ts
@src/cli/browser.ts
@src/cli/login.ts
@src/cli/explore.ts
@src/cli/clearSession.ts
@src/model/adapter.ts
@src/types/spec.ts
@test/security/no-network.test.ts

<audit_reference>
The authoritative command/flag list (from src/cli/index.ts — RE-VERIFY by reading it, do not
trust this table blindly):

- `archeo <url>` — MANUAL capture (human drives; floor ON; spec auto-gen on window close).
  Flags: --i-have-authorization, --no-dashboard, --dashboard-port <port> (0),
  --allow-writes, --i-accept-writes, --redaction-model <cmd>.
- `archeo login <url>` — credential-free auth handoff; no capture store, no dashboard.
  Flags: --i-have-authorization.
- `archeo explore <url>` — AUTONOMOUS vision loop (needs a model key for a real model).
  Flags: --i-have-authorization, --no-dashboard, --dashboard-port <port> (0),
  --max-steps <n> (50), --model <spec> (scripted), --model-base-url <url>,
  --max-tokens <n>, --max-cost <usd>, --pace-ms <ms> (500), --resume,
  --allow-writes, --i-accept-writes, --redaction-model <cmd>.
- `archeo spec [captureDir]` — gate-free spec (re)generation; defaults to latest under
  .archeo/captures. No flags.
- `archeo diff <a> [b]` — gate-free drift report between two spec JSON files. No flags.
- `archeo clear-session [target]` — gate-free profile deletion. Flag: --all.
- Global: --help / -h (cli.help()), --version / -v = 0.1.0 (cli.version('0.1.0')).
- BYO-key: ANTHROPIC_API_KEY env var; --model provider:model (parseModelSpec/createProvider
  in src/model/adapter.ts); default provider 'scripted' (no key); anthropic default model
  'claude-haiku-4-5'.
- Invocation: package.json bin.archeo → dist/index.js (needs `npm run build`); dev script
  `npm run dev` = `node src/cli/index.ts`; engines node >=22.0.0.
</audit_reference>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Doc-vs-code audit of the real command surface (recorded, blocking)</name>
  <files>src/cli/index.ts, src/cli/browser.ts, src/cli/login.ts, src/cli/explore.ts, src/cli/clearSession.ts, src/model/adapter.ts, package.json, src/types/spec.ts</files>
  <read_first>
    - src/cli/index.ts — EVERY cli.command(...).option(...) registration (the source of truth for commands + flags + defaults)
    - src/cli/browser.ts (openAndWait: manual capture; spec auto-gen on window close)
    - src/cli/explore.ts (runExplore: autonomous loop; the budget/pace/resume/allow-writes wiring)
    - src/cli/login.ts (openForLogin: capture-isolated auth handoff)
    - src/model/adapter.ts (parseModelSpec/createProvider; ANTHROPIC_API_KEY path; default models)
    - package.json (bin, scripts.dev/build, engines, postinstall)
    - src/types/spec.ts (ArcheoSpec shape for the spec-contents section)
  </read_first>
  <behavior>
    - Produce a written command/flag inventory (in the SUMMARY) enumerating every command, its
      flags with defaults, whether it runs the authorization gate, and whether it needs a key.
    - Confirm the runnable invocation form for a FRESH CLONE by reading package.json bin/scripts
      (there is a dist/ present, but a stranger's clone rebuilds; the honest form is the dev
      script `node src/cli/index.ts ...` OR an explicit `npm run build` first). Decide the form
      the README will show and record why.
    - Optionally corroborate by running the tool's own help: `node src/cli/index.ts --help` and
      `node src/cli/index.ts explore --help` (read-only; no browser, no network).
    - Flag any code doc-string / --help text that is itself WRONG (a mismatch between an option's
      description and its behavior). Expected: none — but if found, note it for Task 3.
  </behavior>
  <action>
    Read the source files above and write the command/flag inventory into 07-01-SUMMARY.md as the
    audit record. Cross-reference the README claims you are about to write against it. Do NOT
    write README prose from memory — write it from this inventory.
  </action>
  <acceptance_criteria>
    - A command/flag inventory exists in the SUMMARY, each entry citing src/cli/index.ts (and the
      handler file) — commands, flags, defaults, gate-or-not, key-or-not.
    - The chosen fresh-clone invocation form is stated with its package.json justification.
    - `node src/cli/index.ts --help` runs clean (read-only corroboration).
  </acceptance_criteria>
  <verify><automated>node src/cli/index.ts --help; node src/cli/index.ts explore --help</automated></verify>
  <done>The exact, real command surface is written down and cited — the README will be built from source truth, not memory.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite README.md — key-free-first, plain-language, source-cited (OSS-01)</name>
  <files>README.md</files>
  <read_first>
    - Task 1's inventory (in the SUMMARY)
    - .planning/PROJECT.md + CLAUDE.md (vendor-escape framing, constraints)
    - src/types/spec.ts (ArcheoSpec sections for the spec-contents block)
  </read_first>
  <behavior>
    Write the six D7-02 sections, in order, in plain language assuming no codebase knowledge:
    1. **What it is** + vendor-escape framing ("rebuild software you own / already pay for," not
       competitor cloning) — sourced from PROJECT.md/CLAUDE.md.
    2. **Requirements + install** — Node engine reconciled with package.json engines (>=22.0.0)
       and the native-TS story (dev script runs `node src/cli/index.ts` with no flag on a
       recent Node; state the honest minimum); `npm install` runs `playwright install chromium`
       via the postinstall (package.json scripts).
    3. **Quickstart — KEY-FREE MANUAL FIRST.** Show the fresh-clone invocation form from Task 1.
       `archeo <url>` (or `node src/cli/index.ts <url>`): authorize → a real Chromium opens →
       you browse the app by hand → close the window → archeo writes a JSON spec. Explicitly say
       "no API key required." Note `--i-have-authorization` for scripted/non-TTY runs. THEN a
       second **Autonomous mode** subsection: `archeo explore <url> --model anthropic:claude-haiku-4-5`
       needs a key; mention `--max-steps`, `--max-tokens`/`--max-cost`, `--pace-ms`, `--resume`,
       `--no-dashboard` as the real knobs. Also briefly document `login`, `spec`, `diff`,
       `clear-session` so the surface is complete.
    4. **BYO-key configuration** — `ANTHROPIC_API_KEY`, `--model provider:model`,
       provider-agnostic adapter, NO bundled/hosted model, NO telemetry; default `scripted`
       provider is key-free (used by tests/offline).
    5. **Safety model — plain language.** Read-only floor ON by default (writes held, nothing
       reaches the server — src/capture/interceptor.ts); redaction fail-closed (no secrets to
       disk — src/capture/redactor.ts, CAP-05); destructive-GET tripwire; credential-free login
       handoff (src/cli/login.ts); `--allow-writes` is opt-in + loud + needs confirmation
       (src/cli/allowWrites.ts); dashboard is localhost-only (binds 127.0.0.1 —
       src/dashboard/server.ts, guarded by test/security/no-network.test.ts); no telemetry /
       phone-home (GATE-03, same guard). Frame it as the trust anchor.
    6. **What the spec contains** — the ArcheoSpec blocks (meta, dataModels, endpoints incl.
       held:true mutations, flows, rules with confidence, mandatory coverage) from
       src/types/spec.ts; link to `examples/` (created in 07-02) and `CONTRIBUTING.md`
       (created in 07-02).
    Keep it skimmable; a code fence for every command. Do NOT document any flag/command not in
    the Task 1 inventory. Forward-links to examples/ and CONTRIBUTING.md are allowed (07-02
    creates them; 07-03 verifies the links resolve).
  </behavior>
  <action>
    Overwrite README.md with the six sections. Every behavioral sentence traces to a source file
    from Task 1. Remove the stale "explore the app and produce a JSON build spec" autonomy claim
    on `archeo <url>`.
  </action>
  <acceptance_criteria>
    - README contains all six D7-02 sections in order; the key-free manual quickstart precedes the
      BYO-key autonomous section.
    - `grep -nE "archeo explore|explore <url>" README.md` shows autonomy is attributed ONLY to
      `explore`; the `archeo <url>` section describes MANUAL capture.
    - Every command/flag mentioned in README appears in the Task 1 inventory (no invented surface).
    - The safety section names each of the seven safety properties.
    - README links to examples/ and CONTRIBUTING.md.
  </acceptance_criteria>
  <verify><automated>grep -nE "explore|manual|ANTHROPIC_API_KEY|allow-writes|127\.0\.0\.1|examples/|CONTRIBUTING" README.md</automated></verify>
  <done>A stranger can read the README top-to-bottom and understand what Archeo does, how to get a spec with no key, how to add a key for autonomy, and why it is safe against their live account.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: (Conditional) Minimal TDD fix if the audit found a wrong --help/doc string</name>
  <files>src/cli/index.ts, test/cli/index.test.ts</files>
  <read_first>
    - Task 1's flagged mismatches (if any)
    - test/cli/index.test.ts (existing CLI option/help assertions — the place to pin a corrected string)
  </read_first>
  <behavior>
    - ONLY runs if Task 1 found a --help/option description in src/cli that is factually wrong
      (contradicts the behavior in the handler). If none, SKIP this task and record "no code fix
      required — docs matched the code" in the SUMMARY.
    - If a fix is needed: write/extend a failing test in test/cli/index.test.ts pinning the
      corrected option description or behavior, then make the minimal source edit to match. No
      behavior change beyond the string/label; zero new deps; no floor change.
  </behavior>
  <action>
    Conditional. If needed: test first (red), minimal fix (green), re-run the suite. Otherwise
    explicitly record the skip and its justification.
  </action>
  <acceptance_criteria>
    - Either: a SUMMARY line "no code fix required" (Task 1 found no mismatch); OR a red→green
      test + minimal fix with `node --test 'test/cli/index.test.ts'` passing.
    - `node --test 'test/**/*.test.ts'` fully green; count only grows if a fix added a test.
    - `node --test 'test/security/no-network.test.ts'` green (no new outbound surface).
  </acceptance_criteria>
  <verify><automated>node --test 'test/cli/index.test.ts' 'test/security/no-network.test.ts'</automated></verify>
  <done>If the code's own self-description was wrong, it now matches the README; otherwise the code was already truthful and untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Full-suite regression gate + SUMMARY</name>
  <files>.planning/phases/07-open-source-readiness/07-01-SUMMARY.md</files>
  <read_first>
    - Tasks 1–3 outputs
  </read_first>
  <behavior>
    - Run the full suite; confirm baseline 858 (857 pass + 1 documented skip) is preserved (or
      grew only by a Task 3 fix test).
    - Confirm LICENSE + NOTICE are byte-unchanged (OSS-04 untouched).
    - Write 07-01-SUMMARY.md: the command/flag audit inventory, the fresh-clone invocation
      decision, whether a code fix was needed, and the README section map.
  </behavior>
  <action>
    `node --test 'test/**/*.test.ts'`; `git diff --stat LICENSE NOTICE` (expect empty). Author the
    SUMMARY.
  </action>
  <acceptance_criteria>
    - Suite green; count == 858 (or 858 + Task 3 test additions).
    - `git diff --name-only` includes README.md (and only src/cli/index.ts + test/cli if Task 3
      ran); LICENSE/NOTICE not listed.
    - 07-01-SUMMARY.md contains the audit inventory.
  </acceptance_criteria>
  <verify><automated>node --test 'test/**/*.test.ts'</automated></verify>
  <done>OSS-01's README is truthful and cited, the suite is green, and the audit record is preserved for 07-03 to re-verify.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs ↔ shipped behavior | A README that overstates autonomy or understates the floor mis-sets a stranger's expectations about a tool they point at a LIVE account. The doc-vs-code audit is the mitigation: no claim ships that the source doesn't back. |
| README ↔ safety posture | Underselling the safety model loses the trust anchor; overselling it (claiming guarantees the code doesn't provide) is worse. Each safety sentence cites the enforcing source file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-07-01 | Information Disclosure (misleading safety claim) | README safety section | mitigate | Every safety claim cites the enforcing file (interceptor/redactor/login/allowWrites/dashboard/no-network guard); no guarantee is stated beyond what the code enforces. |
| T-07-02 | Tampering (aspirational docs drift from code) | README command surface | mitigate | Task 1 doc-vs-code audit + 07-03 fresh-eyes re-audit; the quickstart is actually run in 07-03. |

No package installs; no new outbound surface; the no-network guard is re-run.
</threat_model>

<verification>
- Task 1 inventory exists and cites src/cli — commands, flags, defaults, gate/key status.
- README leads key-free (manual `archeo <url>`), then BYO-key `archeo explore`; autonomy attributed only to explore.
- Every README command/flag is in the inventory; the six D7-02 sections are present and ordered.
- Safety section names all seven properties, each source-cited.
- `node --test 'test/**/*.test.ts'` green at 858 (+ any Task 3 additions); LICENSE/NOTICE untouched.
</verification>

<success_criteria>
- OSS-01: a plain-language README with setup, key-free quickstart, BYO-key config, and the safety
  model — no prior codebase knowledge required, every claim source-true.
- The stale "`archeo <url>` explores autonomously" claim is gone; the real manual/autonomous split
  is documented.
- The suite stays green; the audit inventory is preserved for the 07-03 cold-start to re-verify.
</success_criteria>

<output>
Create `.planning/phases/07-open-source-readiness/07-01-SUMMARY.md` when done (include the
command/flag audit inventory + the fresh-clone invocation decision).
Commit style: `docs(07-01): …` (and `test(07-01):`/`feat(07-01):` only if Task 3 fixed a code
string), each with the `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` trailer.
</output>
