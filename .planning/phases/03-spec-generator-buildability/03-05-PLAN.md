---
phase: 03-spec-generator-buildability
plan: 05
type: execute
wave: 5
depends_on: ["03-04"]
files_modified:
  - src/types/index.ts
  - src/types/spec.ts
  - src/spec/templater.ts
  - src/spec/generator.ts
  - src/capture/interceptor.ts
  - test/spec/templater.test.ts
  - test/spec/generator.test.ts
  - test/capture/interceptor.test.ts
autonomous: true
requirements: [SPEC-01, SPEC-03, SPEC-04, SPEC-07]

must_haves:
  truths:
    - "A GraphQL query and a GraphQL mutation on the same path NEVER merge into one spec endpoint; same for JSON-RPC read vs write methods"
    - "No dataModel field `type` or responseBodyShape leaf ever carries a raw observed value — types are normalized keywords (uuid, datetime, email, url, number, boolean, string, null); observed allowlisted values move to a separate `example` field"
    - "List-envelope responses ({items|data|results: [...]}) produce a model of the array ELEMENT named after the resource, not a model of the envelope"
    - "coverage.knownGaps lists each held endpoint individually, and coverage carries a recordBreakdown explaining sourceRecordCount"
  artifacts:
    - path: "src/spec/templater.ts"
      provides: "grouping key includes operationType + held + (graphqlOperationName ?? rpcMethod ?? templatedPath)"
      exports: ["groupRecords"]
    - path: "src/spec/generator.ts"
      provides: "normalizeFieldType + envelope unwrap + per-endpoint knownGaps + recordBreakdown"
      exports: ["generateSpec"]
    - path: "src/capture/interceptor.ts"
      provides: "anonymous-GraphQL operationName fallback + rpcMethod population (pre-redaction, schema-level identifiers only)"
  key_links:
    - from: "src/capture/interceptor.ts"
      to: "src/spec/templater.ts"
      via: "graphqlOperationName / rpcMethod fields on CaptureRecord feed the grouping key"
      pattern: "graphqlOperationName|rpcMethod"
---

<objective>
Close the spec-quality gaps surfaced by the 03-04 buildability proof (03-04-BUILDABILITY.md § Spec Quality Findings). The builder rebuilt the app successfully, but four generator defects degraded the spec: (1) GraphQL/JSON-RPC reads and writes merged into single contradictory endpoints; (2) data-model types carried raw observed values instead of type keywords; (3) list-envelope responses modeled the envelope instead of the element; (4) coverage gaps were too coarse to act on. All four are deterministic-code fixes — no LLM, no new deps.

Output: corrected templater grouping, generator type normalization + envelope unwrap + granular coverage, interceptor operation-identifier fallbacks, extended test suites. Phase 3 completes when this plan lands green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-spec-generator-buildability/03-04-BUILDABILITY.md
@.planning/phases/03-spec-generator-buildability/03-CONTEXT.md
@src/spec/templater.ts
@src/spec/generator.ts
@src/capture/interceptor.ts
@src/types/index.ts
@src/types/spec.ts

<interfaces>
# Root cause (verified against the 03-04 capture store):
# capture.jsonl seq 20: request-response POST /graphql operationType:read held:false
# capture.jsonl seq 21: held-write        POST /graphql operationType:mutation held:true
# → templater key 'GraphQL:' + (operationName ?? tpath) merged them (operationName was
#   undefined for anonymous operations; key ignores operationType/held). Same for /rpc.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Grouping key correctness — reads and writes never merge (SPEC-01)</name>
  <files>src/types/index.ts, src/types/spec.ts, src/spec/templater.ts, test/spec/templater.test.ts</files>
  <read_first>
    - src/spec/templater.ts (current groupRecords key construction)
    - src/types/spec.ts (EndpointTemplate)
  </read_first>
  <behavior>
    - Two records, same path/method, operationType read vs mutation → TWO EndpointTemplates; the mutation one held:true, the read one held:false
    - GraphQL records with graphqlOperationName group by operation name; without it, by templated path — but still split by operationType+held
    - JSON-RPC records with rpcMethod group by method name (new optional CaptureRecord field rpcMethod?: string); EndpointTemplate carries operationName?: string for BOTH GraphQL op names and RPC method names
    - Existing REST grouping behavior unchanged (regression: all current templater tests still pass, except any that asserted the buggy merge — fix those to assert the split)
  </behavior>
  <action>
    Add rpcMethod?: string to CaptureRecord (src/types/index.ts, comment: schema-level identifier, not a secret — parallels graphqlOperationName). Change groupRecords key to `${protocol}:${method}:${graphqlOperationName ?? rpcMethod ?? templatedPath}:${operationType}:${held}`. Surface operationName on EndpointTemplate (GraphQL op name or RPC method, whichever present).
  </action>
  <acceptance_criteria>
    - node --test 'test/spec/templater.test.ts' PASSES including new split tests
    - A read and a mutation on the same path never share an EndpointTemplate (explicit test)
  </acceptance_criteria>
  <verify><automated>node --test 'test/spec/templater.test.ts'</automated></verify>
  <done>Spec endpoints never carry contradictory operationType/held combinations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Operation-identifier capture fallbacks (interceptor)</name>
  <files>src/capture/interceptor.ts, test/capture/interceptor.test.ts</files>
  <read_first>
    - src/capture/interceptor.ts (where graphqlOperationName is currently parsed — pre-redaction)
    - src/capture/classifier.ts (existing GraphQL/JSON-RPC body parsing to reuse, not duplicate)
  </read_first>
  <behavior>
    - Named GraphQL op → operationName as today
    - ANONYMOUS GraphQL op (`query { me { ... } }` / `mutation { updateProfile(...) }`) → graphqlOperationName falls back to the first top-level selection field name (e.g. 'me', 'updateProfile')
    - JSON-RPC request body {method: 'getBalance', ...} → record.rpcMethod === 'getBalance'
    - Redaction untouched: only these two schema-level identifiers are read pre-redaction; bodies still fully redacted before append (CAP-05 fail-closed)
  </behavior>
  <action>
    Extract the first top-level selection field with a small tolerant regex/parser on the pre-redaction GraphQL query string (strip # comments first — reuse the CR-03 stripping). Parse method from JSON-RPC pre-redaction body. Set both on the record for ALL record types (request-response AND held-write).
  </action>
  <acceptance_criteria>
    - node --test 'test/capture/interceptor.test.ts' PASSES including anonymous-op and rpcMethod tests
    - grep confirms no change to redactHeaders/redactBody call ordering (CAP-05 intact)
  </acceptance_criteria>
  <verify><automated>node --test 'test/capture/interceptor.test.ts'</automated></verify>
  <done>Anonymous GraphQL ops and JSON-RPC methods carry usable grouping identifiers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Generator type normalization, envelope unwrap, granular coverage (SPEC-03/04/07)</name>
  <files>src/spec/generator.ts, src/types/spec.ts, test/spec/generator.test.ts</files>
  <read_first>
    - src/spec/generator.ts (model inference + coverage assembly)
    - .planning/phases/03-spec-generator-buildability/03-04-BUILDABILITY.md § Spec Quality Findings (items 1, 2, 4, 10, 11)
  </read_first>
  <behavior>
    - normalizeFieldType(value): uuid (v1-5 pattern), datetime (ISO 8601 parse), email (x@y.z), url (http/https parse), number, boolean, null, string — raw observed values NEVER appear as a type; when the observed value was an allowlisted real value, it moves to field.example
    - responseBodyShape leaves are normalized the same way (no mixed values/keywords)
    - Envelope unwrap: response shape {items|data|results: [element], total|count|page?: number} → dataModel is the ELEMENT, named from the path resource singular (e.g. /api/items → Item with id/title/... fields); the envelope shape stays on the endpoint's responseBodyShape only. No more 'Rpc'/'Done' noise models: skip model inference for JSON-RPC envelopes ({jsonrpc, id, result}) and for bodies with no object fields
    - coverage.knownGaps: one entry per held endpoint ('held mutation response unobserved: POST /api/settings') plus existing session-level gaps; coverage.recordBreakdown: {requestResponse, heldWrites, navigations, deadEnds, destructiveGetHeld} summing to sourceRecordCount
  </behavior>
  <action>
    Implement in generator.ts (pure, deterministic). Update ArcheoSpec types in src/types/spec.ts (field example?: unknown, coverage.recordBreakdown). Keep generator consuming ONLY redacted store records.
  </action>
  <acceptance_criteria>
    - node --test 'test/spec/generator.test.ts' PASSES including: uuid/datetime/email normalization; no raw value as type anywhere in a generated fixture spec (recursive assertion); Item element model from envelope fixture; no Rpc/Done models from a JSON-RPC fixture; per-endpoint knownGaps; recordBreakdown sums to sourceRecordCount
  </acceptance_criteria>
  <verify><automated>node --test 'test/spec/generator.test.ts'</automated></verify>
  <done>Generated specs are type-normalized, element-modeled, and gap-granular.</done>
</task>

<task type="auto">
  <name>Task 4: End-to-end regression — regenerate the buildability spec fixture</name>
  <files>test/spec/generator.test.ts</files>
  <read_first>
    - .planning/phases/03-spec-generator-buildability/03-04-buildability/ (target-app.mjs + archeo-spec.json — the OLD spec exhibiting the bugs)
  </read_first>
  <action>
    Add an integration test that feeds a fixture capture (modeled on the 03-04 session: anonymous GraphQL query + held mutation on /graphql, JSON-RPC read + held write on /rpc, /api/items list envelope) through groupRecords + generateSpec and asserts: separate GraphQL read (held:false) and mutation (held:true) endpoints; separate RPC endpoints named by method; Item element model; zero raw values as types; per-endpoint gaps. Do NOT modify the committed 03-04 artifacts (they document the before-state).
  </action>
  <acceptance_criteria>
    - Full suite node --test 'test/**/*.test.ts' PASSES 100% (baseline 255 + new tests)
  </acceptance_criteria>
  <verify><automated>node --test 'test/**/*.test.ts'</automated></verify>
  <done>The exact bug pattern from 03-04 is pinned by a regression test.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05a | Information Disclosure | pre-redaction identifier reads in interceptor | mitigate | Only the GraphQL first selection field name and JSON-RPC method string are read pre-redaction — schema-level identifiers, never values; bodies still redacted before append (CAP-05). Test asserts redaction ordering unchanged. |
| T-03-05b | Information Disclosure | field.example values in spec | mitigate | example is populated ONLY from already-redacted record bodies (allowlisted survivors); the generator never sees raw traffic. Recursive no-raw-value-as-type test guards shape leaves. |

No package installs — no supply-chain checkpoint required.
</threat_model>

<verification>
- Full suite green; regression test pins the 03-04 bug pattern.
- Generated fixture spec: no read/write merges, no raw values as types, element models, granular coverage.
</verification>

<success_criteria>
- The four builder-blocking spec defects from 03-04 are fixed and regression-pinned; Phase 3 requirements SPEC-01/03/04/07 hold on the buildability fixture.
</success_criteria>

<output>
Create `.planning/phases/03-spec-generator-buildability/03-05-SUMMARY.md` when done. This plan closes Phase 3: mark Phase 3 complete in ROADMAP.md (5/5) and STATE.md.
</output>
