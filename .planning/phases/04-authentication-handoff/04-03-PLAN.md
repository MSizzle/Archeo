---
phase: 04-authentication-handoff
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - .planning/phases/04-authentication-handoff/04-03-live-verification/target-app.mjs
  - .planning/phases/04-authentication-handoff/04-03-live-verification/run-auth-verification.mjs
  - .planning/phases/04-authentication-handoff/04-03-live-verification/run-output.log
  - .planning/phases/04-authentication-handoff/04-03-AUTH-VERIFICATION.md
  - .planning/phases/04-authentication-handoff/04-03-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]

must_haves:
  truths:
    - "The verification drives the REAL, unmodified CLI (node src/cli/index.ts …) as a child process against a real local login-walled target app through real headed Chromium — not mocks (D4-06)"
    - "Stage 1 (login run): the profile dir exists afterward; NOTHING was captured during login; the planted password appears nowhere under .archeo/ (AUTH-01 / D4-01 proven live)"
    - "Stage 2 (authenticated capture run): authenticated pages load WITHOUT re-login; reads captured; a held write still held; session cookie value absent from the store; 401s absent (AUTH-02)"
    - "Stage 3 (second capture run): still authenticated — persistence across runs (AUTH-02)"
    - "Stage 4 (clear-session): profile gone; a fresh capture run hits the 401/login wall (AUTH-03 — the clear actually cleared)"
    - "This plan CLOSES Phase 4: ROADMAP Phase 4 → 3/3 Complete, STATE → Phase 5, REQUIREMENTS AUTH-01/02/03 → Complete"
    - "No src/ or test/ files are modified — the harness lives under .planning/ (GATE-03 scans src/ only, as in 02-04/03-04)"
  artifacts:
    - path: ".planning/phases/04-authentication-handoff/04-03-live-verification/target-app.mjs"
      provides: "login-walled copy of the 02-04/03-04 target app: login form + HttpOnly session cookie + fake MFA step + 401 on /api/* without the cookie; auto-fills+submits to simulate the human"
      exports: ["createServer", "ledger", "donePromise"]
    - path: ".planning/phases/04-authentication-handoff/04-03-live-verification/run-auth-verification.mjs"
      provides: "four-stage autonomous proof driving the real CLI (login → capture → capture → clear-session → capture)"
      exports: []
    - path: ".planning/phases/04-authentication-handoff/04-03-AUTH-VERIFICATION.md"
      provides: "the verification report with the four-stage invariant table and evidence (mirrors 03-04-BUILDABILITY.md)"
      exports: []
  key_links:
    - from: "run-auth-verification.mjs"
      to: "src/cli/index.ts (login / <url> / clear-session)"
      via: "spawns the unmodified CLI per stage; answers the login Enter prompt on stdin; inspects .archeo/ between stages"
      pattern: "spawn|login|clear-session"
    - from: "run-auth-verification.mjs"
      to: "target-app.mjs ledger"
      via: "asserts nothing captured during login + session cookie / password never on disk + 401 wall returns after clear"
      pattern: "ledger|session|401"
---

<objective>
Prove the whole authentication handoff end-to-end, **autonomously**, with the **real CLI** —
replacing the human checkpoint per user directive (D4-06, mirroring 02-04 and 03-04). Extend a
copy of the 02-04/03-04 target app with a **session-cookie login wall** (login form → HttpOnly
cookie; `/api/*` → 401 without it; a fake MFA second step), then run the unmodified CLI through
four stages and assert the AUTH-01/02/03 invariants against real traffic and the target server's
own ledger.

Purpose: close the value+safety loop for Phase 4 before it is declared done — login records
nothing, the authenticated context persists across runs without re-login, the floor still holds
writes while authenticated, no session secret reaches disk, and `clear-session` genuinely
restores the 401 wall. This plan also performs the phase-closing bookkeeping.

Output: a `04-03-live-verification/` harness (target app + driver + captured run log), a
`04-03-AUTH-VERIFICATION.md` report, a `04-03-SUMMARY.md`, and the phase-closing updates to
`ROADMAP.md`, `STATE.md`, and `REQUIREMENTS.md`. **No `src/` or `test/` files are touched.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-authentication-handoff/04-CONTEXT.md
@.planning/phases/04-authentication-handoff/04-01-PLAN.md
@.planning/phases/04-authentication-handoff/04-02-PLAN.md

# Proven harness technique to reuse (real CLI subprocess + auto-firing pages + prompt-answer + SIGINT flush)
@.planning/phases/02-capture-layer-safety-floor/02-04-live-verification/run-verification.mjs
@.planning/phases/02-capture-layer-safety-floor/02-04-live-verification/target-app.mjs
@.planning/phases/03-spec-generator-buildability/03-04-SUMMARY.md

<interfaces>
<!-- Harness contract (mjs under .planning/ — NOT shipped code; GATE-03 scans src/ only). -->

# target-app.mjs (copy of 02-04/03-04 app + login wall)
createServer(): http.Server
  // /login (GET form; POST credentials → set HttpOnly session cookie); /mfa (fake second step);
  //   /app (authenticated SPA shell that auto-fires reads + a held write once the session cookie is set);
  //   /api/* → 401 unless the session cookie is present; ledger records mutations/destructive/login attempts;
  //   the login page AUTO-FILLS + SUBMITS after a short delay to simulate the human (CLI owns the browser).
ledger: { allRequests, mutations, destructiveHits, logins, done }
donePromise: Promise<void>   // resolves when the authenticated page finishes its sequence (/__done__ beacon)

# run-auth-verification.mjs stages (each spawns the UNMODIFIED CLI, cwd = the harness dir so .archeo lands there)
//   PASSWORD/COOKIE planted constants grepped across .archeo/ between + after stages.
//   Stage 1: `node <repo>/src/cli/index.ts login <url> --i-have-authorization` → answer Enter on stdin.
//   Stage 2/3: `node <repo>/src/cli/index.ts <url> --i-have-authorization` (no re-login).
//   Stage 4: `node <repo>/src/cli/index.ts clear-session <url>` then a final capture run.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Login-walled target app + four-stage live driver (D4-06)</name>
  <files>.planning/phases/04-authentication-handoff/04-03-live-verification/target-app.mjs, .planning/phases/04-authentication-handoff/04-03-live-verification/run-auth-verification.mjs, .planning/phases/04-authentication-handoff/04-03-live-verification/run-output.log</files>
  <read_first>
    - .planning/phases/02-capture-layer-safety-floor/02-04-live-verification/target-app.mjs (base app to COPY and extend — do NOT modify the original)
    - .planning/phases/02-capture-layer-safety-floor/02-04-live-verification/run-verification.mjs (spawn + auto-fire + prompt-answer + SIGINT-flush + capture-store inspection pattern to mirror)
    - src/cli/index.ts (the login / <url> / clear-session commands to drive; profile dir is .archeo/profiles/<hostname>/)
    - src/cli/login.ts (the ready prompt text to detect on stdout before writing Enter)
    - .planning/phases/04-authentication-handoff/04-CONTEXT.md §D4-06 (the exact four stages + assertions)
  </read_first>
  <behavior>
    - target-app.mjs is a COPY of the 02-04/03-04 app plus: a /login form that POSTs credentials and sets an
      HttpOnly session cookie; a fake /mfa second step (proves arbitrary manual steps work); /api/* returns 401
      without the session cookie; the login page auto-fills + submits after a short delay (simulating the human);
      the authenticated /app auto-fires reads + at least one held write once logged in. A PLANTED password and a
      known session cookie value are used so they can be grepped for on disk.
    - Stage 1 — login run: spawn `login <url> --i-have-authorization`; wait for the ready-prompt text on stdout,
      write '\n' (Enter) to the child's stdin; on exit assert: .archeo/profiles/<hostname>/ exists; .archeo/captures
      is absent OR unchanged (NOTHING captured during login); grep the planted PASSWORD across ALL of .archeo/ → 0 hits.
    - Stage 2 — authenticated capture run: spawn `<url> --i-have-authorization` (NO re-login); the /app page loads
      authenticated (no redirect to /login); assert reads captured; at least one held write is held; the session
      cookie VALUE is absent from the capture store (grep); no 401 records (proves the run was authenticated). SIGINT-flush.
    - Stage 3 — second capture run: spawn `<url> --i-have-authorization` again; still authenticated (no 401, no
      /login redirect) — persistence across runs.
    - Stage 4 — clear-session then wall returns: spawn `clear-session <url>`; assert the profile dir is gone; then
      spawn one more `<url> --i-have-authorization` capture run and assert it now hits the 401 / login wall
      (unauthenticated), proving the clear actually cleared.
    - Overall: the driver prints PASS/FAIL per invariant and exits 0 only if ALL pass; run-output.log captures the run.
  </behavior>
  <action>
    Create the 04-03-live-verification/ directory. Copy the 02-04 (or 03-04) target-app.mjs into it and extend it
    with the login wall described above — the ORIGINAL 02-04/03-04 apps stay untouched. Model
    run-auth-verification.mjs on 02-04's run-verification.mjs: import createServer/ledger/donePromise from the local
    target-app.mjs; listen on a random loopback port; use `const REPO` + `const CLI = join(REPO,'src/cli/index.ts')`
    and `cwd` set to the harness dir so .archeo/ (captures + profiles) lands under the scratch harness dir, not the
    repo root. Run the four stages sequentially, spawning the UNMODIFIED CLI each time; detect the login ready-prompt
    on stdout and answer Enter on stdin (mirror how 02-04 detected the [y/N] prompt and wrote 'n'); between stages,
    read/inspect .archeo/profiles and .archeo/captures directly (existsSync, readdirSync, readFileSync of capture.jsonl),
    and grep the planted PASSWORD + session-cookie value across every file under .archeo/. Use the same check(id,label,
    pass,evidence) reporter + JSON dump pattern as 02-04. Redirect the full run into run-output.log (tee to stdout).
    Clean any prior .archeo under the harness dir at the start so each run is inspected in isolation. The harness is
    .mjs under .planning/ — it may use node:http and node:fs freely (GATE-03 scans src/ only; this is the accepted
    T-04-12 posture, as in 02-04/03-04). Do NOT modify any file under src/ or test/.
  </action>
  <acceptance_criteria>
    - `node .planning/phases/04-authentication-handoff/04-03-live-verification/run-auth-verification.mjs` prints OVERALL: ALL GREEN and exits 0
    - Stage 1 invariants PASS: profile dir exists; captures empty/absent during login; planted password grep = 0 across .archeo/
    - Stage 2 invariants PASS: authenticated load without re-login; reads captured; a held write held; session cookie value grep = 0 in the store; no 401 records
    - Stage 3 invariant PASS: second run still authenticated (no 401 / login redirect)
    - Stage 4 invariants PASS: profile dir gone after clear-session; the subsequent capture run hits the 401/login wall
    - `git status --porcelain src test` is EMPTY (no shipped code touched)
    - run-output.log is committed as the surviving evidence of the run
  </acceptance_criteria>
  <verify>
    <automated>node .planning/phases/04-authentication-handoff/04-03-live-verification/run-auth-verification.mjs</automated>
  </verify>
  <done>All four AUTH stages pass live against the real CLI: login captures nothing, persistence holds across runs without re-login, the floor still holds writes, no session secret reaches disk, and clear-session restores the 401 wall.</done>
</task>

<task type="auto">
  <name>Task 2: Verification report + plan summary (mirror 03-04)</name>
  <files>.planning/phases/04-authentication-handoff/04-03-AUTH-VERIFICATION.md, .planning/phases/04-authentication-handoff/04-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/03-spec-generator-buildability/03-04-BUILDABILITY.md (report structure to mirror: invariant table, evidence, threat flags, verdict)
    - .planning/phases/03-spec-generator-buildability/03-04-SUMMARY.md (SUMMARY frontmatter + section shape)
    - the run-output.log produced by Task 1 (the evidence to cite)
  </read_first>
  <behavior>
    - 04-03-AUTH-VERIFICATION.md records: the four-stage invariant table with PASS/FAIL + evidence (record counts,
      grep results, ledger state), the login-wall + MFA design, the isolation attestation (unmodified CLI, real
      Chromium, real login POST), threat flags (T-04-10..13), and an explicit AUTH-01/02/03 verdict.
    - 04-03-SUMMARY.md records what was done, verification commands, key findings, deviations, threat flags, and a self-check — mirroring 03-04-SUMMARY.md.
  </behavior>
  <action>
    Write 04-03-AUTH-VERIFICATION.md mirroring 03-04-BUILDABILITY.md: a one-liner verdict, the four-stage table
    (stage → invariants → evidence → PASS), the target-app login-wall/MFA description, the isolation attestation
    (drives `node src/cli/index.ts` unmodified; real headed Chromium; real login POST sets a real HttpOnly cookie;
    planted password + session cookie grepped to zero on disk), the AUTH-01/02/03 mapping to the stages, and the
    threat-flag dispositions. Write 04-03-SUMMARY.md mirroring 03-04-SUMMARY.md's frontmatter + sections
    (dependency_graph requires [04-01,04-02]; key_files created under 04-03-live-verification/; decisions: autonomous
    per user directive; metrics). Both are docs only.
  </action>
  <acceptance_criteria>
    - 04-03-AUTH-VERIFICATION.md exists with the four-stage table, isolation attestation, and an explicit AUTH-01/02/03 PASS verdict
    - 04-03-SUMMARY.md exists mirroring 03-04-SUMMARY.md structure
    - Both cite the run-output.log evidence and the reproduce command
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/04-authentication-handoff/04-03-AUTH-VERIFICATION.md && test -f .planning/phases/04-authentication-handoff/04-03-SUMMARY.md && echo OK</automated>
  </verify>
  <done>The autonomous verification is documented with evidence and an explicit AUTH-01/02/03 verdict, mirroring the 03-04 checkpoint record.</done>
</task>

<task type="auto">
  <name>Task 3: Phase-closing bookkeeping — ROADMAP 3/3, STATE → Phase 5, REQUIREMENTS AUTH-01/02/03 Complete</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 4 section + Progress table — set to 3/3 Complete, mirror how Phases 2/3 were closed)
    - .planning/STATE.md (status/stopped_at/current-position/progress + Accumulated Context — add Phase 04 execution decisions, advance to Phase 5)
    - .planning/REQUIREMENTS.md (traceability table — flip AUTH-01/02/03 to Complete; the checkbox list at the top too)
    - .planning/phases/03-spec-generator-buildability/03-05-SUMMARY.md (how the previous phase-close recorded decisions in STATE)
  </read_first>
  <behavior>
    - ROADMAP Phase 4 detail lists the three plans as [x] under their waves; the Progress table row reads `4. Authentication Handoff | 3/3 | Complete | 2026-07-03`
    - STATE.md status/current-position advance to Phase 5 (Autonomous Agent Loop); completed_phases 3→4; completed_plans updated; a `Phase 04-0N execution decisions` block is appended
    - REQUIREMENTS.md: AUTH-01/02/03 checkboxes flip to [x] and their traceability rows read Complete
  </behavior>
  <action>
    Update ROADMAP.md Phase 4 Details: convert the 3-plan wave list to [x] and set the Progress table row to
    `3/3 | Complete | 2026-07-03`. Update STATE.md: bump completed_phases to 4, completed_plans accordingly, set
    status/stopped_at/current-position to "Phase 4 complete — ready for Phase 5 (Autonomous Agent Loop + Full
    Dashboard)", and append a `Phase 04 execution decisions` block summarizing the launchPersistentContext refactor,
    the D4-01 login-isolation module, clear-session containment guard, and the autonomous 4-stage verification.
    Update REQUIREMENTS.md: flip AUTH-01/02/03 to [x] in the Authentication Handoff list and to Complete in the
    traceability table. Docs only — no src/ or test/ changes. (These edits are part of THIS plan's own commits;
    they are distinct from the planner's one-time ROADMAP "Plans TBD → 3-plan wave list / 0/3 Not started" edit.)
  </action>
  <acceptance_criteria>
    - ROADMAP Progress row for Phase 4 reads `3/3 | Complete`
    - STATE.md reflects Phase 4 complete and points to Phase 5 as next
    - REQUIREMENTS.md shows AUTH-01/02/03 Complete in both the list and the traceability table
    - `git status --porcelain src test` EMPTY (no shipped code touched by this plan)
  </acceptance_criteria>
  <verify>
    <automated>grep -n "4. Authentication Handoff" .planning/ROADMAP.md</automated>
  </verify>
  <done>Phase 4 is closed in the planning system: ROADMAP 3/3 Complete, STATE advanced to Phase 5, AUTH-01/02/03 marked Complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live credentials ↔ disk profile (login run) | The login POST carries a real password/MFA; only the persistent Chrome profile may hold session state — nothing may reach the capture store |
| login-mode browser ↔ capture store | Must be proven live to have NO link: the login run must produce zero capture records |
| harness http client ↔ GATE-03 | The .mjs harness uses node:http/node:fs; it must live under .planning/ so the src/-scoped no-network guard is not affected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Information Disclosure | credential captured during the login run | mitigate (prove live) | Stage 1 asserts .archeo/captures is empty/absent during login and greps the planted password to zero across all of .archeo/ — D4-01 proven end-to-end, not just structurally. |
| T-04-11 | Information Disclosure | session cookie value persisted in the store | mitigate (prove live) | Stage 2 greps the known session-cookie value across the capture store → zero (redactor strips cookies; CAP-02/CAP-05 upheld under authenticated traffic). |
| T-04-12 | (harness) outbound network under .planning/ | node:http/node:fs in the harness | accept | The harness is not shipped code; GATE-03 scans src/ only (same accepted posture as 02-04 T-*-SC and 03-04 T-03-15). No src/ or test/ file is modified. |
| T-04-13 | Spoofing / mock pipeline | verification faking a pass | mitigate | The unmodified CLI is spawned as a child process; a real login POST sets a real HttpOnly cookie; /api/* genuinely 401s without it; the target server's own ledger + real capture.jsonl are the evidence (same anti-mock posture as 02-04/03-04). |

No package installs in this plan — no supply-chain checkpoint required.
</threat_model>

<verification>
- `node .planning/phases/04-authentication-handoff/04-03-live-verification/run-auth-verification.mjs` → OVERALL: ALL GREEN, exit 0.
- Stage 1 (AUTH-01/D4-01): profile persists; captures empty during login; planted password absent from .archeo/.
- Stage 2 (AUTH-02): authenticated without re-login; reads captured; held write held; session cookie absent from store; no 401s.
- Stage 3 (AUTH-02): second run still authenticated (persistence across runs).
- Stage 4 (AUTH-03): profile gone after clear-session; a fresh capture run hits the 401/login wall.
- `git status --porcelain src test` empty (no shipped code touched).
- Phase-close bookkeeping applied: ROADMAP 3/3 Complete, STATE → Phase 5, REQUIREMENTS AUTH-01/02/03 Complete.
</verification>

<success_criteria>
- The user can log in manually (including MFA) and signal ready; Archeo never handles credentials and records nothing during login (AUTH-01 — proven live).
- The authenticated context persists so subsequent runs explore from the logged-in state without re-login (AUTH-02 — proven across two runs).
- The persisted session is one gitignored location, absent from store and spec, and clearable — with clear-session proven to restore the login wall (AUTH-03 — proven live).
</success_criteria>

<output>
Create `.planning/phases/04-authentication-handoff/04-03-SUMMARY.md` and
`.planning/phases/04-authentication-handoff/04-03-AUTH-VERIFICATION.md` when done, and apply the
phase-closing ROADMAP/STATE/REQUIREMENTS updates (Task 3).
Commit style for the harness/report/bookkeeping: `test(04-03): …` / `docs(04-03): …` as
appropriate, each with the `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` trailer.
This plan touches NO src/ or test/ files.
</output>
