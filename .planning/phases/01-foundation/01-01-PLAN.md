---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - tsup.config.ts
  - .gitignore
  - src/types/index.ts
  - LICENSE
  - NOTICE
  - README.md
  - test/oss/license.test.ts
autonomous: true
requirements: [OSS-04]
must_haves:
  truths:
    - "`npm install` completes with only cac, playwright (deps) and typescript, tsup (devDeps) — no glob, no tsx"
    - "`npm run typecheck` exits 0 against the scaffolded sources"
    - "An Apache-2.0 LICENSE (full text) and a NOTICE file exist at repo root"
    - "`node --test test/oss/license.test.ts` passes, asserting the license artifacts"
  artifacts:
    - path: "package.json"
      provides: "Formalized ESM package with name, version, type:module, bin, scripts, engines, Apache-2.0 license"
      contains: "\"type\": \"module\""
    - path: "tsconfig.json"
      provides: "ESM + Bundler moduleResolution + strict typecheck config"
      contains: "\"moduleResolution\": \"Bundler\""
    - path: "LICENSE"
      provides: "Apache-2.0 full license text"
      contains: "Apache License"
    - path: "NOTICE"
      provides: "Attribution notice required by Apache-2.0"
      contains: "archeo"
    - path: "src/types/index.ts"
      provides: "Shared ArcheoOptions contract consumed by gate + CLI plans"
      exports: ["ArcheoOptions"]
    - path: "test/oss/license.test.ts"
      provides: "Automated OSS-04 verification"
  key_links:
    - from: "package.json"
      to: "tsup.config.ts"
      via: "build script -> tsup"
      pattern: "\"build\": \"tsup\""
    - from: "package.json"
      to: "test/**/*.test.ts"
      via: "test script -> node:test glob"
      pattern: "node --test"
---

<objective>
Formalize the greenfield Archeo repository into a clean, lean TypeScript ESM project and deliver the OSI-approved license artifacts (OSS-04).

This is the scaffold layer of the Walking Skeleton. It replaces the scratch `package.json` (a bare `dependencies` block left over from research live-testing) with a properly formalized package, establishes the build/typecheck/test tooling every later phase inherits, and ships the Apache-2.0 LICENSE + NOTICE + README stub.

Purpose: Establish the inherited baseline (ESM, native Node TS, node:test, cac, Apache-2.0). Every later phase builds on these choices, so they must be correct and lean here.
Output: A repo that installs, typechecks, and runs `node --test` with a green OSS-04 license test. No runnable CLI yet (gate + browser arrive in plans 02 and 03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md

<interfaces>
<!-- Contract this plan establishes for plans 02 and 03. Executor creates src/types/index.ts with: -->
ArcheoOptions: an interface describing parsed CLI options.
  - iHaveAuthorization?: boolean   (cac camelCases --i-have-authorization)
  - allowWrites?: boolean          (reserved; --allow-writes lands in a later phase, type it now)
Use string union types or `as const` objects for any enumerations — NOT TypeScript `enum`
(native Node TS stripping cannot strip enums; see RESEARCH "Limitation of native TS stripping").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Formalize package.json and tooling config</name>
  <read_first>
    - package.json (the scratch file: bare dependencies block with cac, glob, tsup, tsx, typescript — must be replaced)
    - .planning/phases/01-foundation/01-RESEARCH.md (Standard Stack, Pattern 5 tsconfig, Pattern 6 tsup.config, package.json bin/scripts block, Open Questions 2-3 on package manager + engines)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-07 Apache-2.0, D-08 archeo &lt;url&gt;, D-09 cac, D-10 node:test, D-11 ESM)
  </read_first>
  <files>package.json, tsconfig.json, tsup.config.ts, .gitignore, src/types/index.ts</files>
  <action>
    Replace the scratch package.json entirely. Set: `"name": "archeo"`, `"version": "0.1.0"`, `"type": "module"`, `"license": "Apache-2.0"`, `"description"` (one line, vendor-escape framing), `"bin": { "archeo": "dist/index.js" }`, and `"engines": { "node": ">=22.0.0" }`.
    Dependencies (runtime): `cac` ^7.0.0, `playwright` ^1.61.1. devDependencies: `typescript` ^6.0.3, `tsup` ^8.5.1. REMOVE `glob` (node:test has a built-in glob) and REMOVE `tsx` (Node 24+ strips TS natively; this keeps the dependency surface lean per CLAUDE.md). Document in README (Task 2) that Node 22-23 contributors prepend `NODE_OPTIONS=--experimental-strip-types`; Node 24+ needs no flag.
    Scripts: `"dev": "node src/cli/index.ts"`, `"build": "tsup"`, `"typecheck": "tsc --noEmit"`, `"test": "node --test 'test/**/*.test.ts'"`, `"postinstall": "playwright install chromium"`. The single quotes in the test glob are required (Node handles the glob, not the shell).
    Create tsconfig.json per RESEARCH Pattern 5: target ES2022, module ESNext, `moduleResolution: "Bundler"` (NOT NodeNext — Bundler permits `.ts` import extensions required by native stripping), strict true, noEmit true, skipLibCheck true, include src and test.
    Create tsup.config.ts per RESEARCH Pattern 6: entry `src/cli/index.ts`, format esm, target node22, outDir dist, clean true, dts false, splitting false. (The entry file arrives in Plan 03; `npm run build` is verified there, not here.)
    Create .gitignore with: node_modules/, dist/, *.log, .DS_Store, and .archeo/ (the future gitignored session dir, reserved per AUTH-03). Keep package-lock.json committed.
    Create src/types/index.ts exporting the `ArcheoOptions` interface from the &lt;interfaces&gt; block. Use a string union or `as const` object for any exit-code constants — never an `enum`.
    Run `npm install` to regenerate the lockfile against the reconciled dependency set.
  </action>
  <verify>
    <automated>npm install &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"type": "module"`, `"license": "Apache-2.0"`, `"bin"` mapping `archeo` to `dist/index.js`, and `"engines"` with node `>=22.0.0`
    - package.json dependencies contain `cac` and `playwright`; devDependencies contain `typescript` and `tsup`; package.json contains neither `glob` nor `tsx`
    - tsconfig.json contains `"moduleResolution": "Bundler"` and `"strict": true`
    - `src/types/index.ts` exports `ArcheoOptions`; grep for `enum ` in src/ returns no matches (`grep -rn 'enum ' src/ || true`)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Repo installs cleanly with a lean dependency set, typechecks green, and the shared ArcheoOptions contract exists for downstream plans.</done>
</task>

<task type="auto">
  <name>Task 2: Apache-2.0 LICENSE, NOTICE, README stub, and OSS-04 test</name>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (OSS-04 row in Phase Requirements; "Apache-2.0 NOTICE file (minimal)" code example)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-07 rationale; Deferred Ideas — DISCLAIMER/SECURITY + AS-IS language belong to Phase 7, NOT here; README is a stub only)
    - archeo-build-prompt (5).md §1.1 (vendor-escape framing for README copy), §12 Phase 0 ("OSI-approved license and a README stub")
  </read_first>
  <files>LICENSE, NOTICE, README.md, test/oss/license.test.ts</files>
  <action>
    Write LICENSE containing the complete, verbatim Apache License Version 2.0 text (the full standard text including the "Apache License", "Version 2.0, January 2004", terms 1-9, and the appendix). Do not paraphrase or truncate.
    Write NOTICE per the RESEARCH minimal example: project name `archeo`, `Copyright 2026 Archeo Contributors`, and the standard "This product includes software developed by the Archeo Contributors." line.
    Write README.md as a STUB only (one short paragraph): what Archeo is (one line of vendor-escape framing from build-spec §1.1), a `## Quickstart` placeholder noting `archeo <url>`, a one-line note that `npm install` runs `playwright install chromium` via postinstall, and the Node engine note (Node 24+ native; Node 22-23 use `NODE_OPTIONS=--experimental-strip-types`). Do NOT add DISCLAIMER/SECURITY/AS-IS sections — those are explicitly deferred to Phase 7.
    Write test/oss/license.test.ts using `node:test` + `node:assert/strict`: assert LICENSE exists and its contents include both "Apache License" and "Version 2.0"; assert NOTICE exists and is non-empty; assert package.json `license` field equals "Apache-2.0".
  </action>
  <verify>
    <automated>node --test 'test/oss/license.test.ts'</automated>
  </verify>
  <acceptance_criteria>
    - LICENSE contains the strings "Apache License" and "Version 2.0"
    - NOTICE exists and contains "archeo"
    - README.md exists, leads with vendor-escape framing, and contains NO "DISCLAIMER", "SECURITY", or "AS IS" heading (deferred to Phase 7)
    - `node --test 'test/oss/license.test.ts'` reports all tests passing, 0 failing
  </acceptance_criteria>
  <done>OSS-04 is satisfied and automatically verified: Apache-2.0 LICENSE + NOTICE present, README stub shipped, license test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local install | Third-party package code executes at install time (postinstall) and runtime |
| repo → contributor | License/NOTICE define the legal posture distributed with the code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs (cac, playwright, typescript, tsup) | mitigate | RESEARCH Package Legitimacy Audit shows all four [OK]/Approved with no postinstall scripts of their own; only project-authored postinstall is `playwright install chromium` (the documented browser fetch). No [ASSUMED]/[SUS] packages → no blocking checkpoint required. |
| T-01-01 | Information Disclosure | postinstall script | accept | `playwright install chromium` downloads the Chromium binary from Microsoft's CDN — expected, documented in README, and the only network activity introduced by install. Not telemetry. |
| T-01-02 | Repudiation | LICENSE/NOTICE absence | mitigate | Apache-2.0 full text + NOTICE shipped and asserted by an automated test; provides the liability disclaimer + patent grant the dual-use posture depends on (D-07). |
</threat_model>

<verification>
- `npm install` succeeds against the reconciled dependency set (no glob, no tsx).
- `npm run typecheck` exits 0.
- `node --test 'test/oss/license.test.ts'` passes.
- `grep -rn 'enum ' src/` returns nothing (native-TS-stripping compatibility).
</verification>

<success_criteria>
The project scaffold installs, typechecks, and runs the test runner; Apache-2.0 LICENSE + NOTICE + README stub exist and are verified by an automated test. The ArcheoOptions contract is available for plans 02 and 03. No CLI is runnable yet (intended — gate and browser arrive next).
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done.
</output>
