---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/cli/browser.ts
  - src/cli/index.ts
  - test/cli/index.test.ts
autonomous: false
requirements: [GATE-03]
must_haves:
  truths:
    - "`archeo <url>` parses a positional URL, runs the gate, then opens a real headed Chromium at that URL (D-08 positional `archeo <url>` command shape; D-09 cac arg parsing)"
    - "An invalid URL is rejected with a clear message and a non-zero exit, before any browser launches"
    - "The process stays alive until the user closes the browser window or presses Ctrl+C, then exits with code 0 (D-06 headed Chromium, stays alive until window close/Ctrl+C, clean exit 0)"
    - "The tool opens connections only to the user's target URL — no telemetry, no allowlist check (GATE-03)"
  artifacts:
    - path: "src/cli/browser.ts"
      provides: "Headed Chromium launch, navigate, disconnected/SIGINT lifecycle, clean exit 0; URL validation helper"
      exports: ["openAndWait", "isValidUrl"]
    - path: "src/cli/index.ts"
      provides: "cac CLI entry: positional <url>, --i-have-authorization, dispatch gate -> browser"
      exports: []
    - path: "test/cli/index.test.ts"
      provides: "Automated CLI-level coverage: no-args usage error, non-TTY gate error (D-05/GATE-01), invalid-URL rejection"
  key_links:
    - from: "src/cli/index.ts"
      to: "src/cli/gate.ts"
      via: "await runAuthorizationGate before any browser code"
      pattern: "runAuthorizationGate"
    - from: "src/cli/index.ts"
      to: "src/cli/browser.ts"
      via: "await openAndWait after gate passes"
      pattern: "openAndWait"
    - from: "src/cli/browser.ts"
      to: "playwright chromium"
      via: "chromium.launch({ headless: false })"
      pattern: "headless: false"
---

<objective>
Make the Walking Skeleton walk. Wire the cac CLI entry point so `archeo <url>` parses the positional URL (D-08), runs the authorization gate from Plan 02, validates the URL, then launches a real headed Chromium at the target and stays alive until the user closes the window or presses Ctrl+C — exiting cleanly with code 0 (D-06, SC#4).

This is the final vertical slice of Phase 1: after this plan a real user can run the tool end to end and watch a browser open their target. It also closes the GATE-03 guarantee on the browser side — Playwright opens connections only to the user's target URL; the tool adds no telemetry or allowlist call.

Purpose: Deliver SC#4 (real Chromium opens target, exits cleanly) and the browser-side of GATE-03 (zero outbound to non-target). Wire D-08 (positional url) and D-09 (cac) into a working binary.
Output: A runnable `archeo <url>` (`node src/cli/index.ts <url>`), `npm run build` producing `dist/index.js`, automated CLI-level tests, and a human-verified headed-browser smoke.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md

<interfaces>
<!-- Consumes from prior plans: -->
From src/types/index.ts (Plan 01):  ArcheoOptions { iHaveAuthorization?: boolean; allowWrites?: boolean }
From src/cli/gate.ts (Plan 02):     runAuthorizationGate(iHaveAuthorization: boolean): Promise<void>

<!-- This plan creates: -->
From src/cli/browser.ts:
  isValidUrl(url: string): boolean    — pure: true iff `new URL(url)` does not throw (V5 input validation).
  openAndWait(url: string): Promise<void>
     — launches headed Chromium, navigates to url, resolves/exits 0 on browser 'disconnected' or SIGINT.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Browser lifecycle + URL validation</name>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 3 + "Browser lifecycle" code example; Pitfall 1 playwright install chromium, Pitfall 5 disconnected reliability + page.on('close') fallback, Pitfall 6 .ts import extensions; Security Domain V5 URL validation)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-06 headed, stays alive until window-close/Ctrl+C, exit 0)
    - src/types/index.ts (ArcheoOptions)
  </read_first>
  <files>src/cli/browser.ts</files>
  <action>
    Create src/cli/browser.ts exporting `isValidUrl` and `openAndWait`.
    `isValidUrl(url)`: return true iff `new URL(url)` does not throw (V5 input validation — caught before any Playwright call so a malformed URL exits cleanly, not as a Playwright stack trace).
    `openAndWait(url)` per RESEARCH Pattern 3: `chromium.launch({ headless: false })`, `browser.newPage()`, `page.goto(url, { waitUntil: 'domcontentloaded' })`. Register `process.on('SIGINT', sigintHandler)` where the handler `await browser.close()` then `process.exit(0)`. Await a Promise that resolves on `browser.on('disconnected', resolve)`; also attach `page.on('close', resolve)` as the secondary trigger (Pitfall 5). After the await, `process.off('SIGINT', sigintHandler)` (Pitfall: removing the handler prevents a hang) then `process.exit(0)`.
    Import ONLY `playwright` (chromium) and `node:` built-ins — no HTTP client (keeps test/security/no-network.test.ts green; GATE-03). Use `.ts` import extensions for local imports. No `enum`.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; node --test 'test/security/no-network.test.ts'</automated>
  </verify>
  <acceptance_criteria>
    - src/cli/browser.ts exports `isValidUrl` and `openAndWait`
    - browser.ts contains `headless: false` and registers both a `disconnected` and a `page.on('close'` trigger
    - browser.ts imports only `playwright` and `node:` modules (no fetch/http/https/axios/undici) — `node --test 'test/security/no-network.test.ts'` stays green
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>The headed-browser lifecycle module exists, typechecks, validates URLs, and adds no phone-home surface.</done>
</task>

<task type="auto">
  <name>Task 2: cac CLI entry, end-to-end wiring, and CLI-level tests</name>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 1 cac CLI + "CLI entry point" code example; Pitfall 4 wrap cli.parse() in try/catch; package.json bin/scripts)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-08 positional url, D-09 cac, D-03 flag, D-05 non-TTY)
    - src/cli/gate.ts (runAuthorizationGate), src/cli/browser.ts (isValidUrl, openAndWait)
    - test/cli/gate.test.ts (node:test style to mirror)
  </read_first>
  <files>src/cli/index.ts, test/cli/index.test.ts</files>
  <action>
    Create src/cli/index.ts per RESEARCH Pattern 1. Build a cac('archeo') command `<url>` with description, `.option('--i-have-authorization', ...)`. In the action handler: `await runAuthorizationGate(opts.iHaveAuthorization ?? false)` FIRST (gate before any browser work — GATE-01 ordering), then if `!isValidUrl(url)` write a clear error to stderr and `process.exit(1)`, else `await openAndWait(url)`. Call `cli.help()` and `cli.version('0.1.0')`. Wrap `cli.parse()` in try/catch: on a CACError write `Error: <message>` to stderr, `cli.outputHelp()`, `process.exit(1)` (Pitfall 4). Use `.ts` import extensions; no `enum`.
    Create test/cli/index.test.ts (node:test) that spawns the CLI as a child process via `node:child_process` against `src/cli/index.ts` and asserts CLI-level behavior WITHOUT launching a browser:
      (a) no args → non-zero exit and usage/help text on output;
      (b) a URL with stdin NOT a TTY and NO flag → exit code 1 AND the attestation text appears in output (proves GATE-01 attestation-first + D-05 non-TTY error end-to-end). Run the child with stdin set to 'ignore' so isTTY is false.
      (c) an invalid URL (e.g. `not a url`) WITH `--i-have-authorization` → exit code 1 and an error message, without launching Chromium (URL validated before openAndWait).
    Do NOT add an automated test that reaches openAndWait — the headed lifecycle is human-verified in Task 3 (per VALIDATION Manual-Only Verifications).
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node --test 'test/**/*.test.ts'</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 and produces `dist/index.js`
    - src/cli/index.ts awaits `runAuthorizationGate` before any `isValidUrl`/`openAndWait` call (source order verifiable)
    - `node src/cli/index.ts` with no args exits non-zero and prints usage
    - `node src/cli/index.ts https://example.com < /dev/null` (no TTY, no flag) exits 1 and the attestation text appears in its output
    - `node src/cli/index.ts 'not a url' --i-have-authorization` exits 1 with an error and does NOT open a browser
    - `node --test 'test/**/*.test.ts'` reports 0 failing across the whole suite
  </acceptance_criteria>
  <done>`archeo <url>` is wired end to end: gate runs first, URL is validated, valid URLs reach the browser. Build produces a bin, and CLI-level behavior is automatically tested up to (but not including) the headed launch.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human smoke test — the skeleton walks</name>
  <read_first>
    - .planning/phases/01-foundation/01-VALIDATION.md (Manual-Only Verifications row — headed Chromium lifecycle)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-06)
  </read_first>
  <what-built>
    The full Phase 1 Walking Skeleton: `archeo <url>` prints the authorization attestation, runs the gate, then opens a real headed Chromium at the target URL and stays alive until the window is closed or Ctrl+C, exiting with code 0. This verifies SC#4, which cannot be deterministically automated in CI (a visible window + human close action).
  </what-built>
  <how-to-verify>
    1. Run: `node src/cli/index.ts https://example.com --i-have-authorization`
       Expect: the attestation text (vendor-escape framing + risk line) prints FIRST, then a visible Chromium window opens and loads example.com. Confirm the attestation printed even though the flag was passed (GATE-02).
    2. Close the Chromium window. Expect: the process exits on its own. Check `echo $?` → expect `0`.
    3. Run it again: `node src/cli/index.ts https://example.com --i-have-authorization`, then press Ctrl+C in the terminal. Expect: the browser closes and the process exits 0; your shell prompt is NOT left in raw mode (typed characters echo normally).
    4. Run WITHOUT the flag in your interactive terminal: `node src/cli/index.ts https://example.com`. Expect: attestation prints, prompt `Continue? [y/N]`; press `n` → "Cancelled.", exit 0, NO browser opens. Run once more and press `y` → browser opens.
  </how-to-verify>
  <resume-signal>Type "approved" if all four checks pass, or describe what happened (e.g. process hung after window close, terminal left in raw mode, attestation did not print).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-supplied URL → Playwright | An unvalidated/malformed URL string crosses into the browser engine |
| process → network | Playwright connects to the target; GATE-03 forbids any other outbound connection |
| user terminal → process | SIGINT during browser session must exit cleanly without leaving the TTY broken |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Denial of Service | malformed URL crashes Playwright | mitigate | `isValidUrl` (`new URL(url)`) validates before `page.goto`; invalid input exits 1 with a clear message, not a stack trace. CLI-level test (c) asserts this. |
| T-01-08 | Information Disclosure | telemetry / non-target outbound (GATE-03) | mitigate | browser.ts imports only `playwright` + `node:` built-ins; Playwright connects only to the target URL the user supplied. test/security/no-network.test.ts scans browser.ts for forbidden network tokens. No allowlist or phone-home call exists. **Block-worthy:** any outbound call beyond the user's target URL. |
| T-01-09 | Spoofing / Repudiation | gate ordering in index.ts | mitigate | The action handler `await`s runAuthorizationGate before isValidUrl/openAndWait; CLI test (b) asserts the attestation appears and a non-TTY-no-flag run exits 1 before any browser launch (GATE-01). |
| T-01-10 | Denial of Service | process hang / raw-mode after SIGINT | mitigate | `process.off('SIGINT', ...)` after the disconnected/close await prevents a hang; the gate's SIGINT handler (Plan 02) restores raw mode. Human smoke check 3 verifies the terminal is not left broken. |
</threat_model>

<verification>
- `npm run build` exits 0 and emits `dist/index.js`.
- `node --test 'test/**/*.test.ts'` — full suite green (license + gate + no-network + CLI).
- `npm run typecheck` exits 0.
- Human smoke test (Task 3): headed Chromium opens target, exits 0 on window-close and on Ctrl+C, attestation prints first, y/N gating works.
</verification>

<success_criteria>
Phase 1 success criteria #4 and the browser-side of #3 are met: a real headed Chromium opens the target URL and the process exits cleanly (code 0) on window-close or Ctrl+C, with zero outbound calls to non-target URLs. `archeo <url>` is a working end-to-end binary; the Walking Skeleton walks.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done.
</output>
