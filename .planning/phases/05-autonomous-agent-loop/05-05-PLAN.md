---
phase: 05-autonomous-agent-loop
plan: 05
type: execute
wave: 5
depends_on: ["05-04"]
files_modified:
  - .planning/phases/05-autonomous-agent-loop/05-05-live-verification/target-app.mjs
  - .planning/phases/05-autonomous-agent-loop/05-05-live-verification/run-explore-verification.mjs
  - .planning/phases/05-autonomous-agent-loop/05-05-live-verification/run-output.log
  - .planning/phases/05-autonomous-agent-loop/05-05-EXPLORE-VERIFICATION.md
  - .planning/phases/05-autonomous-agent-loop/05-05-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [AGENT-08]

must_haves:
  truths:
    - "The verification drives the REAL, unmodified CLI (node src/cli/index.ts explore …) as a child process against a real local trapped-SPA target app through real headed Chromium with the scripted provider and the real floor — not mocks (D5-05)"
    - "Logout is NEVER clicked: the target's server-side logout counter is 0 after the run AND the persisted profile is still valid afterward (AGENT-07a proven live)"
    - "The oscillation trap is escaped: steps stay bounded and the run completes with a recorded stop reason (plateau or empty-frontier), not a hang (AGENT-07b + AGENT-05 proven live)"
    - "Zero mutations reached the server (target ledger mutations=0, destructiveHits=0) — the floor held every write during autonomous exploration (FLOOR-01 under autonomy)"
    - "The dashboard SSE carried screencast frames + coverage-map growth (state/transition) + verbatim reasoning lines + at least one held beat — the harness subscribes to /events and records the evidence (DASH-04..07 proven live)"
    - "AGENT-08 parity: the autonomous spec's endpoints and dataModels are ≥ the committed 03-04 baseline (superset-or-equal on the comparable surface) and its flows/states are STRICTLY greater; the comparison table is written into the report"
    - "This plan CLOSES Phase 5: ROADMAP 5/5 Complete, STATE → Phase 6, REQUIREMENTS MODEL-01/AGENT-01..08/DASH-04..07 → Complete, PLUS the housekeeping fix of the stale Phase-3 rows (SPEC-01..07, BUILD-01, DASH-01..03 → Complete)"
    - "No src/ or test/ files are modified — the harness lives under .planning/ (GATE-03 scans src/ only, as in 02-04/03-04/04-03)"
  artifacts:
    - path: ".planning/phases/05-autonomous-agent-loop/05-05-live-verification/target-app.mjs"
      provides: "trapped-SPA copy of the 02-04/04-03 app: login wall + logout link (server-side counter) + oscillation trap + paginated list + validating form + REST/GraphQL/JSON-RPC endpoints"
      exports: ["createServer", "ledger", "donePromise"]
    - path: ".planning/phases/05-autonomous-agent-loop/05-05-live-verification/run-explore-verification.mjs"
      provides: "the autonomous proof: login handoff (Phase-4 flow) → `archeo explore` (scripted provider) → SSE evidence capture → invariant assertions → AGENT-08 parity vs the 03-04 baseline → optional real-key smoke"
      exports: []
    - path: ".planning/phases/05-autonomous-agent-loop/05-05-EXPLORE-VERIFICATION.md"
      provides: "the verification report: invariant table + the AGENT-08 comparison table + dashboard-evidence summary (mirrors 04-03-AUTH-VERIFICATION.md)"
      exports: []
  key_links:
    - from: "run-explore-verification.mjs"
      to: "src/cli/index.ts (login / explore)"
      via: "spawns the unmodified CLI: `login` (answer Enter on stdin) then `explore <url> --i-have-authorization --dashboard-port <p> --max-steps N` with the scripted provider"
      pattern: "spawn|explore|login"
    - from: "run-explore-verification.mjs"
      to: "the dashboard /events SSE + the target-app ledger"
      via: "subscribe to /events (record frame/state/transition/reasoning/held evidence); assert ledger.mutations===0, ledger.logoutHits===0"
      pattern: "/events|ledger|logout"
    - from: "run-explore-verification.mjs"
      to: ".planning/phases/03-spec-generator-buildability/03-04-buildability/archeo-spec.json"
      via: "AGENT-08 parity — compare autonomous spec endpoints/dataModels (≥) and flows/states (strictly greater)"
      pattern: "archeo-spec|endpoints|states"
---

<objective>
Prove the whole autonomous agent loop end-to-end, **autonomously**, with the **real CLI** — the
same class of checkpoint verified autonomously in 02-04 / 03-04 / 04-03 (D5-05). Build a
**trapped-SPA** copy of the app family (login wall + a never-click logout link with a server-side
counter + an oscillation trap + a paginated list + a validating form + the existing
REST/GraphQL/JSON-RPC endpoints), run `archeo explore` with the **scripted provider**, **real
headed Chromium**, and the **real floor**, capture the dashboard SSE evidence, and assert every
Phase-5 safety + coverage invariant. Then run the **AGENT-08 parity** comparison against the
committed 03-04 baseline spec, attempt the **conditional real-key smoke**, and **close the phase**
(including the stale-Phase-3 REQUIREMENTS housekeeping).

Purpose: close the value + safety loop for Phase 5 before it is declared done — the agent explores
authenticated, never logs itself out, escapes the oscillation trap, stops deliberately, lets no
write reach the server, drives the full dashboard, and produces a spec that meets or beats the
manual baseline.

Output: a `05-05-live-verification/` harness (trapped-SPA target + driver + run log), a
`05-05-EXPLORE-VERIFICATION.md` report (invariants + AGENT-08 table + dashboard evidence), a
`05-05-SUMMARY.md`, and the phase-closing updates to `ROADMAP.md`, `STATE.md`, and
`REQUIREMENTS.md`. **No `src/` or `test/` files are touched.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-autonomous-agent-loop/05-CONTEXT.md
@.planning/phases/05-autonomous-agent-loop/05-01-PLAN.md
@.planning/phases/05-autonomous-agent-loop/05-02-PLAN.md
@.planning/phases/05-autonomous-agent-loop/05-03-PLAN.md
@.planning/phases/05-autonomous-agent-loop/05-04-PLAN.md

# Proven harness technique to reuse (real CLI subprocess + auto-firing pages + prompt-answer + SIGINT flush + ledger)
@.planning/phases/04-authentication-handoff/04-03-live-verification/target-app.mjs
@.planning/phases/04-authentication-handoff/04-03-live-verification/run-auth-verification.mjs
@.planning/phases/02-capture-layer-safety-floor/02-04-live-verification/run-verification.mjs
# The AGENT-08 baseline (committed manual spec)
@.planning/phases/03-spec-generator-buildability/03-04-buildability/archeo-spec.json
@.planning/phases/04-authentication-handoff/04-03-SUMMARY.md

<interfaces>
<!-- Harness contract (.mjs under .planning/ — NOT shipped code; GATE-03 scans src/ only). -->

# target-app.mjs (copy of 04-03 app + planted traps)
createServer(): http.Server
  // login wall (reuse the 04-03 flow: POST creds → HttpOnly session cookie; /api/* → 401 without it) so the
  //   explore run starts from the persisted authenticated profile; then an SPA-ish multi-page authenticated app:
  //     - a prominent LOGOUT link (/logout) that increments ledger.logoutHits and clears the session — MUST NEVER be hit;
  //     - an OSCILLATION TRAP: /ping and /pong linking ONLY to each other (no new discovery beyond the pair);
  //     - a PAGINATED list (/items?page=1..N) exercising templated endpoints;
  //     - a FORM with server-side validation (rejects a bad value, accepts the synthetic 'Archeo Test'/email shape) — submit is a write, so the floor HOLDS it;
  //     - the existing REST + GraphQL + JSON-RPC read/mutation endpoints from the app family.
  //   Pages carry stable, clickable links/buttons with visible text so the scripted frontier walker can navigate.
ledger: { allRequests, mutations, destructiveHits, logins, logoutHits, done }
donePromise: Promise<void>   // resolves when the app has served enough of its surface (a /__done__ beacon or a timeout)

# run-explore-verification.mjs stages (spawns the UNMODIFIED CLI; cwd = the harness dir so .archeo lands there)
//   Stage A (login): `node <repo>/src/cli/index.ts login <url> --i-have-authorization` → answer Enter on stdin (Phase-4 handoff).
//   Stage B (explore): `node <repo>/src/cli/index.ts explore <url> --i-have-authorization --dashboard-port <p> --max-steps 40`
//     with (default) the scripted provider; the harness OPENS an SSE client on http://127.0.0.1:<p>/events and records
//     which typed events arrived (frame/state/transition/reasoning/held). The run ends on plateau/empty-frontier (bounded).
//   Assertions: ledger.logoutHits===0 AND the profile still authenticates afterward; ledger.mutations===0 AND
//     ledger.destructiveHits===0; the printed stop reason ∈ {plateau,empty-frontier}; SSE evidence present for all four event kinds.
//   AGENT-08: load the generated spec + the 03-04 baseline; assert endpoints ⊇ / dataModels ⊇ (≥) and flows/states strictly greater; write the table.
//   Real-key smoke: if process.env.ANTHROPIC_API_KEY, run `explore … --model anthropic:claude-haiku-4-5 --max-steps 10` and record qualitative notes; else record deferred-pending-key.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trapped-SPA target app + autonomous explore driver + dashboard SSE evidence (D5-05)</name>
  <files>.planning/phases/05-autonomous-agent-loop/05-05-live-verification/target-app.mjs, .planning/phases/05-autonomous-agent-loop/05-05-live-verification/run-explore-verification.mjs, .planning/phases/05-autonomous-agent-loop/05-05-live-verification/run-output.log</files>
  <read_first>
    - .planning/phases/04-authentication-handoff/04-03-live-verification/target-app.mjs (login-walled base app to COPY and extend — do NOT modify the original)
    - .planning/phases/04-authentication-handoff/04-03-live-verification/run-auth-verification.mjs (spawn + login-prompt-answer + ledger-gated + capture-store inspection pattern to mirror)
    - .planning/phases/02-capture-layer-safety-floor/02-04-live-verification/run-verification.mjs (SIGINT-flush + real-CLI subprocess pattern)
    - src/cli/index.ts (the login + explore commands to drive; explore flags: --max-steps, --model, --dashboard-port)
    - src/dashboard/server.ts (the /events typed event names to subscribe to: snapshot/record/frame/state/transition/reasoning/held)
    - 05-CONTEXT.md §D5-05 (the exact traps + assertions + parity + real-key rule)
  </read_first>
  <behavior>
    - target-app.mjs is a COPY of the 04-03 app plus the four planted traps (logout link w/ server-side counter, oscillation trap /ping↔/pong, paginated list, validating form) and keeps the REST/GraphQL/JSON-RPC endpoints; the login wall is reused so the run explores authenticated
    - run-explore-verification.mjs Stage A logs in via the real CLI (Phase-4 handoff) so the profile is authenticated
    - Stage B runs the real `archeo explore` with the scripted provider + real headed Chromium + the real floor + the dashboard, bounded by --max-steps; the harness subscribes to /events and records the arrival of frame/state/transition/reasoning/held events
    - after the run: ledger.logoutHits===0; the profile still authenticates (a follow-up authenticated read succeeds); ledger.mutations===0 AND ledger.destructiveHits===0; the printed stop reason is plateau or empty-frontier; a spec was written
    - run-output.log captures the whole session for the record
  </behavior>
  <action>
    Create the 05-05-live-verification/ harness. target-app.mjs: copy 04-03's login-walled app; add the four traps
    exactly as in <interfaces>, each with visible clickable text so the scripted frontier walker reaches them, and
    a ledger that counts logoutHits/mutations/destructiveHits/logins. The logout link, when hit, increments
    logoutHits and clears the session (so a later authenticated read would 401 — the check that logout was avoided).
    run-explore-verification.mjs: Stage A spawns `login` and answers Enter on stdin once the app ledger shows the
    login landed (observed state, not a delay); Stage B spawns `explore <url> --i-have-authorization --dashboard-port
    <p> --max-steps 40`, opens an SSE client on /events, records the typed events seen, waits for the CLI to exit
    (bounded), then asserts every invariant against the ledger + the generated spec + the SSE evidence. Tee all
    output to run-output.log. The harness lives entirely under .planning/ — it modifies NO src/ or test/ file. Use
    node:http + node:child_process + node:fs only; no new deps.
  </action>
  <acceptance_criteria>
    - `node .planning/phases/05-autonomous-agent-loop/05-05-live-verification/run-explore-verification.mjs` exits 0 with every invariant GREEN (logout avoided + profile still valid; oscillation escaped + bounded stop reason; mutations=0 + destructiveHits=0; SSE frame/state/transition/reasoning/held all observed; spec written)
    - the printed stop reason is plateau or empty-frontier (never a hang / never max-steps-only-because-stuck)
    - run-output.log is committed as the surviving evidence
    - `grep -REn "logoutHits|mutations|destructiveHits" .planning/phases/05-autonomous-agent-loop/05-05-live-verification/` shows the ledger assertions
    - NO file under src/ or test/ is modified by this task (verify with `git status --porcelain src test` → empty)
  </acceptance_criteria>
  <verify>
    <automated>node .planning/phases/05-autonomous-agent-loop/05-05-live-verification/run-explore-verification.mjs</automated>
  </verify>
  <done>The real CLI autonomously explores a trapped authenticated SPA: it never logs out, escapes oscillation, stops deliberately, lets no write reach the server, and drives the full dashboard — proven against the target's own ledger and the live SSE stream.</done>
</task>

<task type="auto">
  <name>Task 2: AGENT-08 parity vs the 03-04 baseline + conditional real-key smoke</name>
  <files>.planning/phases/05-autonomous-agent-loop/05-05-live-verification/run-explore-verification.mjs, .planning/phases/05-autonomous-agent-loop/05-05-EXPLORE-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/03-spec-generator-buildability/03-04-buildability/archeo-spec.json (the committed manual baseline: endpoints=19, dataModels=6, flows=2 — the parity target)
    - .planning/phases/04-authentication-handoff/04-03-AUTH-VERIFICATION.md (report format to mirror)
    - 05-CONTEXT.md §D5-05 (parity rule: endpoints/dataModels ≥ superset-or-equal; flows/states strictly greater; real-key deferred-pending-key if no key)
  </read_first>
  <behavior>
    - the autonomous spec's endpoints are a superset-or-equal of the baseline's on the comparable app surface (every baseline endpoint template present; count ≥)
    - the autonomous spec's dataModels are ≥ the baseline (superset-or-equal)
    - the autonomous spec's flows/states are STRICTLY greater than the baseline (the autonomous run explores more pages)
    - the comparison is written as a table in 05-05-EXPLORE-VERIFICATION.md (baseline vs autonomous, per dimension, with PASS/FAIL)
    - real-key smoke: if ANTHROPIC_API_KEY is present, a 10-step anthropic run is executed and its qualitative result recorded; if absent (expected), the report + SUMMARY + STATE record it as deferred-pending-key WITHOUT failing the phase
  </behavior>
  <action>
    Extend run-explore-verification.mjs with the AGENT-08 comparison: parse the generated spec + the 03-04 baseline,
    compute endpoint-template set inclusion, dataModel-name inclusion, and flow/state counts, and assert
    superset-or-equal (endpoints/dataModels) + strictly-greater (flows/states). Add the conditional real-key branch:
    if process.env.ANTHROPIC_API_KEY, spawn `explore … --model anthropic:claude-haiku-4-5 --max-steps 10` and capture
    qualitative notes; else emit a clear deferred-pending-key marker. Write 05-05-EXPLORE-VERIFICATION.md mirroring
    04-03-AUTH-VERIFICATION.md: the invariant table (Task 1), the AGENT-08 comparison table, the dashboard-evidence
    summary, and the real-key disposition. No src/ or test/ change.
  </action>
  <acceptance_criteria>
    - the parity assertions PASS: endpoints ⊇ baseline (count ≥), dataModels ⊇ baseline (count ≥), flows/states strictly greater
    - 05-05-EXPLORE-VERIFICATION.md contains the baseline-vs-autonomous comparison table with a PASS verdict for AGENT-08
    - the real-key smoke is either executed-and-recorded OR explicitly documented as deferred-pending-key (no ANTHROPIC_API_KEY in this environment — expected); the phase does NOT fail on its absence
    - the report mirrors the 04-03 format (invariant table + evidence)
  </acceptance_criteria>
  <verify>
    <automated>node .planning/phases/05-autonomous-agent-loop/05-05-live-verification/run-explore-verification.mjs</automated>
  </verify>
  <done>The autonomous spec provably meets-or-beats the manual Phase-3 baseline (AGENT-08), and the real-model path is either smoke-tested or honestly deferred-pending-key.</done>
</task>

<task type="auto">
  <name>Task 3: Phase close — ROADMAP / STATE / REQUIREMENTS (incl. Phase-3 housekeeping)</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md, .planning/phases/05-autonomous-agent-loop/05-05-SUMMARY.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 5 section + the Progress table row to flip to 5/5 Complete)
    - .planning/STATE.md (status/stopped_at/progress + Accumulated Context — add the Phase-5 close entry, advance to Phase 6)
    - .planning/REQUIREMENTS.md (the checkbox list + the traceability table — flip Phase-5 rows AND the stale Phase-3 rows)
    - .planning/phases/04-authentication-handoff/04-03-SUMMARY.md (the phase-close bookkeeping format to mirror)
    - 05-CONTEXT.md §D5-05 (the exact close scope incl. the Phase-3 housekeeping list)
  </read_first>
  <behavior>
    - ROADMAP Phase 5 "Plans: TBD" → the 5-plan wave list; the Progress table row → "5/5 | Complete | <date>"; Phase 5 checkbox → [x]
    - STATE: status/stopped_at advanced to "Phase 5 complete → Phase 6 next"; completed_phases 4→5, completed_plans updated, percent recomputed; an Accumulated-Context "Phase 05-05 execution decisions (PHASE 5 CLOSE)" entry added; the housekeeping todo removed from Pending Todos
    - REQUIREMENTS: MODEL-01, AGENT-01..08, DASH-04..07 → Complete (both the checkbox list and the traceability table)
    - REQUIREMENTS housekeeping: the stale Phase-3 rows SPEC-01..07, BUILD-01, DASH-01..03 → Complete (checkbox list + traceability), per the 04-03 finding
  </behavior>
  <action>
    Update the three planning docs. ROADMAP: replace Phase 5 "Plans: TBD" with the wave list (05-01..05-05 with
    their titles + requirement tags) and mark the phase [x]; flip the Progress row to 5/5 Complete with the
    completion date. STATE: advance the front-matter status/progress, add the Phase-5 close decision block to
    Accumulated Context (the invariants proven, AGENT-08 verdict, real-key disposition), point Next → Phase 6, and
    clear the Phase-3 housekeeping todo. REQUIREMENTS: check off MODEL-01/AGENT-01..08/DASH-04..07 AND the stale
    Phase-3 rows (SPEC-01..07, BUILD-01, DASH-01..03) in both the list and the traceability table. Write
    05-05-SUMMARY.md (what shipped across 05-01..05-05, the live invariants, the AGENT-08 table verdict, the
    real-key deferral, suite count). This is a docs-only task — NO src/ or test/ change.
  </action>
  <acceptance_criteria>
    - ROADMAP shows Phase 5 = 5/5 Complete with the wave list; `grep -n "5/5" .planning/ROADMAP.md` non-empty
    - STATE front-matter shows completed_phases: 5 and Next → Phase 6; the Phase-3 housekeeping todo is gone
    - `grep -nE "MODEL-01|AGENT-08|DASH-07" .planning/REQUIREMENTS.md` shows Complete rows; `grep -nE "SPEC-01|BUILD-01|DASH-01" .planning/REQUIREMENTS.md` shows Complete (housekeeping applied)
    - no `[ ]` remains for any Phase-3, -4, or -5 requirement in REQUIREMENTS.md
    - `git status --porcelain src test` is EMPTY (no shipped-code change in this plan)
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "5/5|MODEL-01|AGENT-08|SPEC-01|BUILD-01" .planning/ROADMAP.md .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>Phase 5 is closed with clean books: ROADMAP 5/5, STATE at Phase 6, all Phase-5 requirements Complete, and the stale Phase-3 rows corrected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| autonomous agent → live authenticated account | The whole point of this plan is to prove the agent cannot harm the account — no logout, no writes, no runaway. |
| screencast frames → localhost dashboard | The harness subscribes to the loopback SSE only; frames never leave the box (verified by binding + the dashboard-no-outbound guard from 05-04). |
| harness ↔ shipped code | The harness lives under .planning/; it must not modify src/ or test/ (GATE-03 scans src/ only). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-19 | Elevation of Privilege | the agent logging itself out mid-run | verify | Server-side logout counter asserted 0 AND the profile still authenticates after the run — live proof of AGENT-07a. |
| T-05-20 | Denial of Service | the oscillation trap hanging the run | verify | The run completes with a bounded stop reason (plateau/empty-frontier); the trap is escaped — live proof of AGENT-07b + AGENT-05. |
| T-05-21 | Tampering | a synthetic form submit mutating the account during autonomy | verify | Target ledger mutations=0 AND destructiveHits=0 — the floor held every write under autonomous exploration (FLOOR-01). |
| T-05-22 | Information Disclosure | the harness accidentally editing shipped code / the real-key smoke leaking a key | mitigate | The harness is .planning-only (git-porcelain check); the real-key smoke reads ANTHROPIC_API_KEY from env and records only qualitative notes (never the key), and is skipped-with-note when absent. |

No package installs; the harness uses node built-ins only. The only outbound surface in the whole
system remains the pinned provider endpoint (05-01), and the scripted-provider run makes NO
outbound call at all.
</threat_model>

<verification>
- `node .planning/.../05-05-live-verification/run-explore-verification.mjs` exits 0 with every invariant GREEN.
- Live invariants: logout avoided (counter 0 + profile valid); oscillation escaped (bounded stop reason); mutations=0 + destructiveHits=0; SSE frame/state/transition/reasoning/held all observed; spec written.
- AGENT-08 parity PASS: endpoints/dataModels ≥ baseline, flows/states strictly greater; table in the report.
- Real-key smoke executed-or-deferred-pending-key (expected deferred — no key in this environment).
- `git status --porcelain src test` empty (no shipped-code change).
- Phase close applied: ROADMAP 5/5, STATE → Phase 6, REQUIREMENTS Phase-5 + stale Phase-3 rows Complete.
- The full unit suite (`node --test 'test/**/*.test.ts'`) remains green as the pre-close gate.
</verification>

<success_criteria>
- AGENT-08: the autonomous spec matches-or-beats the manual Phase-3 spec in endpoint and data-model coverage, with strictly more flows/states — proven by the committed comparison table.
- The Phase-5 success criteria are proven live: unattended exploration from a persisted authenticated context; no logout / no account switch / no oscillation; the full dashboard (browser view, coverage map, verbatim reasoning, held beat); zero writes reaching the server.
- Phase 5 is closed and the books (ROADMAP/STATE/REQUIREMENTS, incl. Phase-3 housekeeping) are clean.
</success_criteria>

<output>
Create `.planning/phases/05-autonomous-agent-loop/05-05-SUMMARY.md` and
`.planning/phases/05-autonomous-agent-loop/05-05-EXPLORE-VERIFICATION.md` when done.
Commit style: `test(05-05): …` / `feat(05-05): …` / `docs(05-05): …` as appropriate for the
harness + report + phase-close bookkeeping, each with the
`Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` trailer.
</output>
</content>
